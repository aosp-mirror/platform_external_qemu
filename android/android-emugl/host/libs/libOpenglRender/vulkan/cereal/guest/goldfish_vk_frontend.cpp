// Module: goldfish_vk_frontend (impl) Autogenerated by CerealGenerator


#include "guest/goldfish_vk_frontend.h"

#include "guest/goldfish_vk_encoder.h"

#ifdef VK_VERSION_1_0

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateInstance len None optional? False staticArrExpr  0) */
void validate_vkCreateInstance(
    VkResult* validateResult,
    const VkInstanceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkInstance* pInstance)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateInstance len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateInstance(
    const VkInstanceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkInstance* pInstance)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateInstance(&validateResult, pCreateInfo, pAllocator, pInstance);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateInstance(vkStream, pCreateInfo, pAllocator, pInstance);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyInstance len None optional? False staticArrExpr  0) */
void validate_vkDestroyInstance(
    VkResult* validateResult,
    VkInstance instance,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyInstance len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyInstance(
    VkInstance instance,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyInstance(&validateResult, instance, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyInstance(vkStream, instance, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumeratePhysicalDevices len None optional? False staticArrExpr  0) */
void validate_vkEnumeratePhysicalDevices(
    VkResult* validateResult,
    VkInstance instance,
    uint32_t* pPhysicalDeviceCount,
    VkPhysicalDevice* pPhysicalDevices)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumeratePhysicalDevices len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkEnumeratePhysicalDevices(
    VkInstance instance,
    uint32_t* pPhysicalDeviceCount,
    VkPhysicalDevice* pPhysicalDevices)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumeratePhysicalDevices(&validateResult, instance, pPhysicalDeviceCount, pPhysicalDevices);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkEnumeratePhysicalDevices(vkStream, instance, pPhysicalDeviceCount, pPhysicalDevices);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFeatures len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceFeatures(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures* pFeatures)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFeatures len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceFeatures(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures* pFeatures)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFeatures(&validateResult, physicalDevice, pFeatures);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceFeatures(vkStream, physicalDevice, pFeatures);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFormatProperties len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceFormatProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties* pFormatProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFormatProperties len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceFormatProperties(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties* pFormatProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFormatProperties(&validateResult, physicalDevice, format, pFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceFormatProperties(vkStream, physicalDevice, format, pFormatProperties);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceImageFormatProperties len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceImageFormatProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkImageTiling tiling,
    VkImageUsageFlags usage,
    VkImageCreateFlags flags,
    VkImageFormatProperties* pImageFormatProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceImageFormatProperties len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceImageFormatProperties(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkImageTiling tiling,
    VkImageUsageFlags usage,
    VkImageCreateFlags flags,
    VkImageFormatProperties* pImageFormatProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceImageFormatProperties(&validateResult, physicalDevice, format, type, tiling, usage, flags, pImageFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceImageFormatProperties(vkStream, physicalDevice, format, type, tiling, usage, flags, pImageFormatProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceProperties len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties* pProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceProperties len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceProperties(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceProperties(&validateResult, physicalDevice, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceProperties(vkStream, physicalDevice, pProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceQueueFamilyProperties len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceQueueFamilyProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties* pQueueFamilyProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceQueueFamilyProperties len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceQueueFamilyProperties(
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties* pQueueFamilyProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceQueueFamilyProperties(&validateResult, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceQueueFamilyProperties(vkStream, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceMemoryProperties len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceMemoryProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties* pMemoryProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceMemoryProperties len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceMemoryProperties(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties* pMemoryProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceMemoryProperties(&validateResult, physicalDevice, pMemoryProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceMemoryProperties(vkStream, physicalDevice, pMemoryProperties);
}

static /* VulkanType for return type: (vulkantype PFN_vkVoidFunction nonconst paramName vkGetInstanceProcAddr len None optional? False staticArrExpr  0) */
void validate_vkGetInstanceProcAddr(
    VkResult* validateResult,
    VkInstance instance,
    const char* pName)
{
}

/* VulkanType for return type: (vulkantype PFN_vkVoidFunction nonconst paramName vkGetInstanceProcAddr len None optional? False staticArrExpr  0) */
PFN_vkVoidFunction goldfish_frontend_vkGetInstanceProcAddr(
    VkInstance instance,
    const char* pName)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetInstanceProcAddr(&validateResult, instance, pName);
    if (validateResult != VK_SUCCESS)
    {
        return (PFN_vkVoidFunction)0;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    PFN_vkVoidFunction res = encode_vkGetInstanceProcAddr(vkStream, instance, pName);
    return res;
}

static /* VulkanType for return type: (vulkantype PFN_vkVoidFunction nonconst paramName vkGetDeviceProcAddr len None optional? False staticArrExpr  0) */
void validate_vkGetDeviceProcAddr(
    VkResult* validateResult,
    VkDevice device,
    const char* pName)
{
}

/* VulkanType for return type: (vulkantype PFN_vkVoidFunction nonconst paramName vkGetDeviceProcAddr len None optional? False staticArrExpr  0) */
PFN_vkVoidFunction goldfish_frontend_vkGetDeviceProcAddr(
    VkDevice device,
    const char* pName)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceProcAddr(&validateResult, device, pName);
    if (validateResult != VK_SUCCESS)
    {
        return (PFN_vkVoidFunction)0;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    PFN_vkVoidFunction res = encode_vkGetDeviceProcAddr(vkStream, device, pName);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDevice len None optional? False staticArrExpr  0) */
void validate_vkCreateDevice(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkDeviceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDevice* pDevice)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDevice len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateDevice(
    VkPhysicalDevice physicalDevice,
    const VkDeviceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDevice* pDevice)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDevice(&validateResult, physicalDevice, pCreateInfo, pAllocator, pDevice);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateDevice(vkStream, physicalDevice, pCreateInfo, pAllocator, pDevice);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDevice len None optional? False staticArrExpr  0) */
void validate_vkDestroyDevice(
    VkResult* validateResult,
    VkDevice device,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDevice len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyDevice(
    VkDevice device,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDevice(&validateResult, device, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyDevice(vkStream, device, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumerateInstanceExtensionProperties len None optional? False staticArrExpr  0) */
void validate_vkEnumerateInstanceExtensionProperties(
    VkResult* validateResult,
    const char* pLayerName,
    uint32_t* pPropertyCount,
    VkExtensionProperties* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumerateInstanceExtensionProperties len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkEnumerateInstanceExtensionProperties(
    const char* pLayerName,
    uint32_t* pPropertyCount,
    VkExtensionProperties* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumerateInstanceExtensionProperties(&validateResult, pLayerName, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkEnumerateInstanceExtensionProperties(vkStream, pLayerName, pPropertyCount, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumerateDeviceExtensionProperties len None optional? False staticArrExpr  0) */
void validate_vkEnumerateDeviceExtensionProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const char* pLayerName,
    uint32_t* pPropertyCount,
    VkExtensionProperties* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumerateDeviceExtensionProperties len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkEnumerateDeviceExtensionProperties(
    VkPhysicalDevice physicalDevice,
    const char* pLayerName,
    uint32_t* pPropertyCount,
    VkExtensionProperties* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumerateDeviceExtensionProperties(&validateResult, physicalDevice, pLayerName, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkEnumerateDeviceExtensionProperties(vkStream, physicalDevice, pLayerName, pPropertyCount, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumerateInstanceLayerProperties len None optional? False staticArrExpr  0) */
void validate_vkEnumerateInstanceLayerProperties(
    VkResult* validateResult,
    uint32_t* pPropertyCount,
    VkLayerProperties* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumerateInstanceLayerProperties len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkEnumerateInstanceLayerProperties(
    uint32_t* pPropertyCount,
    VkLayerProperties* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumerateInstanceLayerProperties(&validateResult, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkEnumerateInstanceLayerProperties(vkStream, pPropertyCount, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumerateDeviceLayerProperties len None optional? False staticArrExpr  0) */
void validate_vkEnumerateDeviceLayerProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkLayerProperties* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumerateDeviceLayerProperties len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkEnumerateDeviceLayerProperties(
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkLayerProperties* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumerateDeviceLayerProperties(&validateResult, physicalDevice, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkEnumerateDeviceLayerProperties(vkStream, physicalDevice, pPropertyCount, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetDeviceQueue len None optional? False staticArrExpr  0) */
void validate_vkGetDeviceQueue(
    VkResult* validateResult,
    VkDevice device,
    uint32_t queueFamilyIndex,
    uint32_t queueIndex,
    VkQueue* pQueue)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetDeviceQueue len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetDeviceQueue(
    VkDevice device,
    uint32_t queueFamilyIndex,
    uint32_t queueIndex,
    VkQueue* pQueue)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceQueue(&validateResult, device, queueFamilyIndex, queueIndex, pQueue);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetDeviceQueue(vkStream, device, queueFamilyIndex, queueIndex, pQueue);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkQueueSubmit len None optional? False staticArrExpr  0) */
void validate_vkQueueSubmit(
    VkResult* validateResult,
    VkQueue queue,
    uint32_t submitCount,
    const VkSubmitInfo* pSubmits,
    VkFence fence)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkQueueSubmit len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkQueueSubmit(
    VkQueue queue,
    uint32_t submitCount,
    const VkSubmitInfo* pSubmits,
    VkFence fence)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueSubmit(&validateResult, queue, submitCount, pSubmits, fence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkQueueSubmit(vkStream, queue, submitCount, pSubmits, fence);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkQueueWaitIdle len None optional? False staticArrExpr  0) */
void validate_vkQueueWaitIdle(
    VkResult* validateResult,
    VkQueue queue)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkQueueWaitIdle len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkQueueWaitIdle(
    VkQueue queue)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueWaitIdle(&validateResult, queue);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkQueueWaitIdle(vkStream, queue);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkDeviceWaitIdle len None optional? False staticArrExpr  0) */
void validate_vkDeviceWaitIdle(
    VkResult* validateResult,
    VkDevice device)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkDeviceWaitIdle len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkDeviceWaitIdle(
    VkDevice device)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDeviceWaitIdle(&validateResult, device);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkDeviceWaitIdle(vkStream, device);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAllocateMemory len None optional? False staticArrExpr  0) */
void validate_vkAllocateMemory(
    VkResult* validateResult,
    VkDevice device,
    const VkMemoryAllocateInfo* pAllocateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDeviceMemory* pMemory)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAllocateMemory len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkAllocateMemory(
    VkDevice device,
    const VkMemoryAllocateInfo* pAllocateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDeviceMemory* pMemory)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkAllocateMemory(&validateResult, device, pAllocateInfo, pAllocator, pMemory);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkAllocateMemory(vkStream, device, pAllocateInfo, pAllocator, pMemory);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkFreeMemory len None optional? False staticArrExpr  0) */
void validate_vkFreeMemory(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceMemory memory,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkFreeMemory len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkFreeMemory(
    VkDevice device,
    VkDeviceMemory memory,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkFreeMemory(&validateResult, device, memory, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkFreeMemory(vkStream, device, memory, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkMapMemory len None optional? False staticArrExpr  0) */
void validate_vkMapMemory(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceMemory memory,
    VkDeviceSize offset,
    VkDeviceSize size,
    VkMemoryMapFlags flags,
    void** ppData)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkMapMemory len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkMapMemory(
    VkDevice device,
    VkDeviceMemory memory,
    VkDeviceSize offset,
    VkDeviceSize size,
    VkMemoryMapFlags flags,
    void** ppData)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkMapMemory(&validateResult, device, memory, offset, size, flags, ppData);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkMapMemory(vkStream, device, memory, offset, size, flags, ppData);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkUnmapMemory len None optional? False staticArrExpr  0) */
void validate_vkUnmapMemory(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceMemory memory)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkUnmapMemory len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkUnmapMemory(
    VkDevice device,
    VkDeviceMemory memory)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkUnmapMemory(&validateResult, device, memory);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkUnmapMemory(vkStream, device, memory);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkFlushMappedMemoryRanges len None optional? False staticArrExpr  0) */
void validate_vkFlushMappedMemoryRanges(
    VkResult* validateResult,
    VkDevice device,
    uint32_t memoryRangeCount,
    const VkMappedMemoryRange* pMemoryRanges)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkFlushMappedMemoryRanges len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkFlushMappedMemoryRanges(
    VkDevice device,
    uint32_t memoryRangeCount,
    const VkMappedMemoryRange* pMemoryRanges)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkFlushMappedMemoryRanges(&validateResult, device, memoryRangeCount, pMemoryRanges);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkFlushMappedMemoryRanges(vkStream, device, memoryRangeCount, pMemoryRanges);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkInvalidateMappedMemoryRanges len None optional? False staticArrExpr  0) */
void validate_vkInvalidateMappedMemoryRanges(
    VkResult* validateResult,
    VkDevice device,
    uint32_t memoryRangeCount,
    const VkMappedMemoryRange* pMemoryRanges)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkInvalidateMappedMemoryRanges len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkInvalidateMappedMemoryRanges(
    VkDevice device,
    uint32_t memoryRangeCount,
    const VkMappedMemoryRange* pMemoryRanges)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkInvalidateMappedMemoryRanges(&validateResult, device, memoryRangeCount, pMemoryRanges);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkInvalidateMappedMemoryRanges(vkStream, device, memoryRangeCount, pMemoryRanges);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetDeviceMemoryCommitment len None optional? False staticArrExpr  0) */
void validate_vkGetDeviceMemoryCommitment(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceMemory memory,
    VkDeviceSize* pCommittedMemoryInBytes)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetDeviceMemoryCommitment len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetDeviceMemoryCommitment(
    VkDevice device,
    VkDeviceMemory memory,
    VkDeviceSize* pCommittedMemoryInBytes)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceMemoryCommitment(&validateResult, device, memory, pCommittedMemoryInBytes);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetDeviceMemoryCommitment(vkStream, device, memory, pCommittedMemoryInBytes);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindBufferMemory len None optional? False staticArrExpr  0) */
void validate_vkBindBufferMemory(
    VkResult* validateResult,
    VkDevice device,
    VkBuffer buffer,
    VkDeviceMemory memory,
    VkDeviceSize memoryOffset)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindBufferMemory len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkBindBufferMemory(
    VkDevice device,
    VkBuffer buffer,
    VkDeviceMemory memory,
    VkDeviceSize memoryOffset)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindBufferMemory(&validateResult, device, buffer, memory, memoryOffset);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkBindBufferMemory(vkStream, device, buffer, memory, memoryOffset);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindImageMemory len None optional? False staticArrExpr  0) */
void validate_vkBindImageMemory(
    VkResult* validateResult,
    VkDevice device,
    VkImage image,
    VkDeviceMemory memory,
    VkDeviceSize memoryOffset)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindImageMemory len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkBindImageMemory(
    VkDevice device,
    VkImage image,
    VkDeviceMemory memory,
    VkDeviceSize memoryOffset)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindImageMemory(&validateResult, device, image, memory, memoryOffset);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkBindImageMemory(vkStream, device, image, memory, memoryOffset);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetBufferMemoryRequirements len None optional? False staticArrExpr  0) */
void validate_vkGetBufferMemoryRequirements(
    VkResult* validateResult,
    VkDevice device,
    VkBuffer buffer,
    VkMemoryRequirements* pMemoryRequirements)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetBufferMemoryRequirements len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetBufferMemoryRequirements(
    VkDevice device,
    VkBuffer buffer,
    VkMemoryRequirements* pMemoryRequirements)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetBufferMemoryRequirements(&validateResult, device, buffer, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetBufferMemoryRequirements(vkStream, device, buffer, pMemoryRequirements);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageMemoryRequirements len None optional? False staticArrExpr  0) */
void validate_vkGetImageMemoryRequirements(
    VkResult* validateResult,
    VkDevice device,
    VkImage image,
    VkMemoryRequirements* pMemoryRequirements)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageMemoryRequirements len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetImageMemoryRequirements(
    VkDevice device,
    VkImage image,
    VkMemoryRequirements* pMemoryRequirements)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageMemoryRequirements(&validateResult, device, image, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetImageMemoryRequirements(vkStream, device, image, pMemoryRequirements);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageSparseMemoryRequirements len None optional? False staticArrExpr  0) */
void validate_vkGetImageSparseMemoryRequirements(
    VkResult* validateResult,
    VkDevice device,
    VkImage image,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements* pSparseMemoryRequirements)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageSparseMemoryRequirements len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetImageSparseMemoryRequirements(
    VkDevice device,
    VkImage image,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements* pSparseMemoryRequirements)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageSparseMemoryRequirements(&validateResult, device, image, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetImageSparseMemoryRequirements(vkStream, device, image, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceSparseImageFormatProperties len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceSparseImageFormatProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkSampleCountFlagBits samples,
    VkImageUsageFlags usage,
    VkImageTiling tiling,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties* pProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceSparseImageFormatProperties len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceSparseImageFormatProperties(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkSampleCountFlagBits samples,
    VkImageUsageFlags usage,
    VkImageTiling tiling,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSparseImageFormatProperties(&validateResult, physicalDevice, format, type, samples, usage, tiling, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceSparseImageFormatProperties(vkStream, physicalDevice, format, type, samples, usage, tiling, pPropertyCount, pProperties);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkQueueBindSparse len None optional? False staticArrExpr  0) */
void validate_vkQueueBindSparse(
    VkResult* validateResult,
    VkQueue queue,
    uint32_t bindInfoCount,
    const VkBindSparseInfo* pBindInfo,
    VkFence fence)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkQueueBindSparse len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkQueueBindSparse(
    VkQueue queue,
    uint32_t bindInfoCount,
    const VkBindSparseInfo* pBindInfo,
    VkFence fence)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueBindSparse(&validateResult, queue, bindInfoCount, pBindInfo, fence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkQueueBindSparse(vkStream, queue, bindInfoCount, pBindInfo, fence);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateFence len None optional? False staticArrExpr  0) */
void validate_vkCreateFence(
    VkResult* validateResult,
    VkDevice device,
    const VkFenceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateFence len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateFence(
    VkDevice device,
    const VkFenceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateFence(&validateResult, device, pCreateInfo, pAllocator, pFence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateFence(vkStream, device, pCreateInfo, pAllocator, pFence);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyFence len None optional? False staticArrExpr  0) */
void validate_vkDestroyFence(
    VkResult* validateResult,
    VkDevice device,
    VkFence fence,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyFence len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyFence(
    VkDevice device,
    VkFence fence,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyFence(&validateResult, device, fence, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyFence(vkStream, device, fence, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkResetFences len None optional? False staticArrExpr  0) */
void validate_vkResetFences(
    VkResult* validateResult,
    VkDevice device,
    uint32_t fenceCount,
    const VkFence* pFences)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkResetFences len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkResetFences(
    VkDevice device,
    uint32_t fenceCount,
    const VkFence* pFences)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkResetFences(&validateResult, device, fenceCount, pFences);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkResetFences(vkStream, device, fenceCount, pFences);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetFenceStatus len None optional? False staticArrExpr  0) */
void validate_vkGetFenceStatus(
    VkResult* validateResult,
    VkDevice device,
    VkFence fence)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetFenceStatus len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetFenceStatus(
    VkDevice device,
    VkFence fence)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetFenceStatus(&validateResult, device, fence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetFenceStatus(vkStream, device, fence);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkWaitForFences len None optional? False staticArrExpr  0) */
void validate_vkWaitForFences(
    VkResult* validateResult,
    VkDevice device,
    uint32_t fenceCount,
    const VkFence* pFences,
    VkBool32 waitAll,
    uint64_t timeout)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkWaitForFences len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkWaitForFences(
    VkDevice device,
    uint32_t fenceCount,
    const VkFence* pFences,
    VkBool32 waitAll,
    uint64_t timeout)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkWaitForFences(&validateResult, device, fenceCount, pFences, waitAll, timeout);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkWaitForFences(vkStream, device, fenceCount, pFences, waitAll, timeout);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSemaphore len None optional? False staticArrExpr  0) */
void validate_vkCreateSemaphore(
    VkResult* validateResult,
    VkDevice device,
    const VkSemaphoreCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSemaphore* pSemaphore)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSemaphore len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateSemaphore(
    VkDevice device,
    const VkSemaphoreCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSemaphore* pSemaphore)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSemaphore(&validateResult, device, pCreateInfo, pAllocator, pSemaphore);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateSemaphore(vkStream, device, pCreateInfo, pAllocator, pSemaphore);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySemaphore len None optional? False staticArrExpr  0) */
void validate_vkDestroySemaphore(
    VkResult* validateResult,
    VkDevice device,
    VkSemaphore semaphore,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySemaphore len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroySemaphore(
    VkDevice device,
    VkSemaphore semaphore,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySemaphore(&validateResult, device, semaphore, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroySemaphore(vkStream, device, semaphore, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateEvent len None optional? False staticArrExpr  0) */
void validate_vkCreateEvent(
    VkResult* validateResult,
    VkDevice device,
    const VkEventCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkEvent* pEvent)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateEvent len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateEvent(
    VkDevice device,
    const VkEventCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkEvent* pEvent)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateEvent(&validateResult, device, pCreateInfo, pAllocator, pEvent);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateEvent(vkStream, device, pCreateInfo, pAllocator, pEvent);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyEvent len None optional? False staticArrExpr  0) */
void validate_vkDestroyEvent(
    VkResult* validateResult,
    VkDevice device,
    VkEvent event,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyEvent len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyEvent(
    VkDevice device,
    VkEvent event,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyEvent(&validateResult, device, event, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyEvent(vkStream, device, event, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetEventStatus len None optional? False staticArrExpr  0) */
void validate_vkGetEventStatus(
    VkResult* validateResult,
    VkDevice device,
    VkEvent event)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetEventStatus len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetEventStatus(
    VkDevice device,
    VkEvent event)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetEventStatus(&validateResult, device, event);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetEventStatus(vkStream, device, event);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkSetEvent len None optional? False staticArrExpr  0) */
void validate_vkSetEvent(
    VkResult* validateResult,
    VkDevice device,
    VkEvent event)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkSetEvent len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkSetEvent(
    VkDevice device,
    VkEvent event)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkSetEvent(&validateResult, device, event);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkSetEvent(vkStream, device, event);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkResetEvent len None optional? False staticArrExpr  0) */
void validate_vkResetEvent(
    VkResult* validateResult,
    VkDevice device,
    VkEvent event)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkResetEvent len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkResetEvent(
    VkDevice device,
    VkEvent event)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkResetEvent(&validateResult, device, event);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkResetEvent(vkStream, device, event);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateQueryPool len None optional? False staticArrExpr  0) */
void validate_vkCreateQueryPool(
    VkResult* validateResult,
    VkDevice device,
    const VkQueryPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkQueryPool* pQueryPool)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateQueryPool len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateQueryPool(
    VkDevice device,
    const VkQueryPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkQueryPool* pQueryPool)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateQueryPool(&validateResult, device, pCreateInfo, pAllocator, pQueryPool);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateQueryPool(vkStream, device, pCreateInfo, pAllocator, pQueryPool);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyQueryPool len None optional? False staticArrExpr  0) */
void validate_vkDestroyQueryPool(
    VkResult* validateResult,
    VkDevice device,
    VkQueryPool queryPool,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyQueryPool len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyQueryPool(
    VkDevice device,
    VkQueryPool queryPool,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyQueryPool(&validateResult, device, queryPool, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyQueryPool(vkStream, device, queryPool, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetQueryPoolResults len None optional? False staticArrExpr  0) */
void validate_vkGetQueryPoolResults(
    VkResult* validateResult,
    VkDevice device,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount,
    size_t dataSize,
    void* pData,
    VkDeviceSize stride,
    VkQueryResultFlags flags)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetQueryPoolResults len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetQueryPoolResults(
    VkDevice device,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount,
    size_t dataSize,
    void* pData,
    VkDeviceSize stride,
    VkQueryResultFlags flags)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetQueryPoolResults(&validateResult, device, queryPool, firstQuery, queryCount, dataSize, pData, stride, flags);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetQueryPoolResults(vkStream, device, queryPool, firstQuery, queryCount, dataSize, pData, stride, flags);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateBuffer len None optional? False staticArrExpr  0) */
void validate_vkCreateBuffer(
    VkResult* validateResult,
    VkDevice device,
    const VkBufferCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkBuffer* pBuffer)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateBuffer len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateBuffer(
    VkDevice device,
    const VkBufferCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkBuffer* pBuffer)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateBuffer(&validateResult, device, pCreateInfo, pAllocator, pBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateBuffer(vkStream, device, pCreateInfo, pAllocator, pBuffer);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyBuffer len None optional? False staticArrExpr  0) */
void validate_vkDestroyBuffer(
    VkResult* validateResult,
    VkDevice device,
    VkBuffer buffer,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyBuffer len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyBuffer(
    VkDevice device,
    VkBuffer buffer,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyBuffer(&validateResult, device, buffer, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyBuffer(vkStream, device, buffer, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateBufferView len None optional? False staticArrExpr  0) */
void validate_vkCreateBufferView(
    VkResult* validateResult,
    VkDevice device,
    const VkBufferViewCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkBufferView* pView)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateBufferView len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateBufferView(
    VkDevice device,
    const VkBufferViewCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkBufferView* pView)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateBufferView(&validateResult, device, pCreateInfo, pAllocator, pView);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateBufferView(vkStream, device, pCreateInfo, pAllocator, pView);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyBufferView len None optional? False staticArrExpr  0) */
void validate_vkDestroyBufferView(
    VkResult* validateResult,
    VkDevice device,
    VkBufferView bufferView,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyBufferView len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyBufferView(
    VkDevice device,
    VkBufferView bufferView,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyBufferView(&validateResult, device, bufferView, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyBufferView(vkStream, device, bufferView, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateImage len None optional? False staticArrExpr  0) */
void validate_vkCreateImage(
    VkResult* validateResult,
    VkDevice device,
    const VkImageCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkImage* pImage)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateImage len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateImage(
    VkDevice device,
    const VkImageCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkImage* pImage)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateImage(&validateResult, device, pCreateInfo, pAllocator, pImage);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateImage(vkStream, device, pCreateInfo, pAllocator, pImage);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyImage len None optional? False staticArrExpr  0) */
void validate_vkDestroyImage(
    VkResult* validateResult,
    VkDevice device,
    VkImage image,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyImage len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyImage(
    VkDevice device,
    VkImage image,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyImage(&validateResult, device, image, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyImage(vkStream, device, image, pAllocator);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageSubresourceLayout len None optional? False staticArrExpr  0) */
void validate_vkGetImageSubresourceLayout(
    VkResult* validateResult,
    VkDevice device,
    VkImage image,
    const VkImageSubresource* pSubresource,
    VkSubresourceLayout* pLayout)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageSubresourceLayout len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetImageSubresourceLayout(
    VkDevice device,
    VkImage image,
    const VkImageSubresource* pSubresource,
    VkSubresourceLayout* pLayout)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageSubresourceLayout(&validateResult, device, image, pSubresource, pLayout);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetImageSubresourceLayout(vkStream, device, image, pSubresource, pLayout);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateImageView len None optional? False staticArrExpr  0) */
void validate_vkCreateImageView(
    VkResult* validateResult,
    VkDevice device,
    const VkImageViewCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkImageView* pView)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateImageView len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateImageView(
    VkDevice device,
    const VkImageViewCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkImageView* pView)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateImageView(&validateResult, device, pCreateInfo, pAllocator, pView);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateImageView(vkStream, device, pCreateInfo, pAllocator, pView);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyImageView len None optional? False staticArrExpr  0) */
void validate_vkDestroyImageView(
    VkResult* validateResult,
    VkDevice device,
    VkImageView imageView,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyImageView len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyImageView(
    VkDevice device,
    VkImageView imageView,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyImageView(&validateResult, device, imageView, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyImageView(vkStream, device, imageView, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateShaderModule len None optional? False staticArrExpr  0) */
void validate_vkCreateShaderModule(
    VkResult* validateResult,
    VkDevice device,
    const VkShaderModuleCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkShaderModule* pShaderModule)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateShaderModule len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateShaderModule(
    VkDevice device,
    const VkShaderModuleCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkShaderModule* pShaderModule)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateShaderModule(&validateResult, device, pCreateInfo, pAllocator, pShaderModule);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateShaderModule(vkStream, device, pCreateInfo, pAllocator, pShaderModule);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyShaderModule len None optional? False staticArrExpr  0) */
void validate_vkDestroyShaderModule(
    VkResult* validateResult,
    VkDevice device,
    VkShaderModule shaderModule,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyShaderModule len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyShaderModule(
    VkDevice device,
    VkShaderModule shaderModule,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyShaderModule(&validateResult, device, shaderModule, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyShaderModule(vkStream, device, shaderModule, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreatePipelineCache len None optional? False staticArrExpr  0) */
void validate_vkCreatePipelineCache(
    VkResult* validateResult,
    VkDevice device,
    const VkPipelineCacheCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkPipelineCache* pPipelineCache)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreatePipelineCache len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreatePipelineCache(
    VkDevice device,
    const VkPipelineCacheCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkPipelineCache* pPipelineCache)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreatePipelineCache(&validateResult, device, pCreateInfo, pAllocator, pPipelineCache);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreatePipelineCache(vkStream, device, pCreateInfo, pAllocator, pPipelineCache);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyPipelineCache len None optional? False staticArrExpr  0) */
void validate_vkDestroyPipelineCache(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineCache pipelineCache,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyPipelineCache len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyPipelineCache(
    VkDevice device,
    VkPipelineCache pipelineCache,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyPipelineCache(&validateResult, device, pipelineCache, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyPipelineCache(vkStream, device, pipelineCache, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPipelineCacheData len None optional? False staticArrExpr  0) */
void validate_vkGetPipelineCacheData(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineCache pipelineCache,
    size_t* pDataSize,
    void* pData)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPipelineCacheData len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPipelineCacheData(
    VkDevice device,
    VkPipelineCache pipelineCache,
    size_t* pDataSize,
    void* pData)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPipelineCacheData(&validateResult, device, pipelineCache, pDataSize, pData);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPipelineCacheData(vkStream, device, pipelineCache, pDataSize, pData);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkMergePipelineCaches len None optional? False staticArrExpr  0) */
void validate_vkMergePipelineCaches(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineCache dstCache,
    uint32_t srcCacheCount,
    const VkPipelineCache* pSrcCaches)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkMergePipelineCaches len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkMergePipelineCaches(
    VkDevice device,
    VkPipelineCache dstCache,
    uint32_t srcCacheCount,
    const VkPipelineCache* pSrcCaches)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkMergePipelineCaches(&validateResult, device, dstCache, srcCacheCount, pSrcCaches);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkMergePipelineCaches(vkStream, device, dstCache, srcCacheCount, pSrcCaches);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateGraphicsPipelines len None optional? False staticArrExpr  0) */
void validate_vkCreateGraphicsPipelines(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineCache pipelineCache,
    uint32_t createInfoCount,
    const VkGraphicsPipelineCreateInfo* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkPipeline* pPipelines)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateGraphicsPipelines len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateGraphicsPipelines(
    VkDevice device,
    VkPipelineCache pipelineCache,
    uint32_t createInfoCount,
    const VkGraphicsPipelineCreateInfo* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkPipeline* pPipelines)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateGraphicsPipelines(&validateResult, device, pipelineCache, createInfoCount, pCreateInfos, pAllocator, pPipelines);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateGraphicsPipelines(vkStream, device, pipelineCache, createInfoCount, pCreateInfos, pAllocator, pPipelines);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateComputePipelines len None optional? False staticArrExpr  0) */
void validate_vkCreateComputePipelines(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineCache pipelineCache,
    uint32_t createInfoCount,
    const VkComputePipelineCreateInfo* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkPipeline* pPipelines)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateComputePipelines len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateComputePipelines(
    VkDevice device,
    VkPipelineCache pipelineCache,
    uint32_t createInfoCount,
    const VkComputePipelineCreateInfo* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkPipeline* pPipelines)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateComputePipelines(&validateResult, device, pipelineCache, createInfoCount, pCreateInfos, pAllocator, pPipelines);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateComputePipelines(vkStream, device, pipelineCache, createInfoCount, pCreateInfos, pAllocator, pPipelines);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyPipeline len None optional? False staticArrExpr  0) */
void validate_vkDestroyPipeline(
    VkResult* validateResult,
    VkDevice device,
    VkPipeline pipeline,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyPipeline len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyPipeline(
    VkDevice device,
    VkPipeline pipeline,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyPipeline(&validateResult, device, pipeline, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyPipeline(vkStream, device, pipeline, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreatePipelineLayout len None optional? False staticArrExpr  0) */
void validate_vkCreatePipelineLayout(
    VkResult* validateResult,
    VkDevice device,
    const VkPipelineLayoutCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkPipelineLayout* pPipelineLayout)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreatePipelineLayout len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreatePipelineLayout(
    VkDevice device,
    const VkPipelineLayoutCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkPipelineLayout* pPipelineLayout)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreatePipelineLayout(&validateResult, device, pCreateInfo, pAllocator, pPipelineLayout);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreatePipelineLayout(vkStream, device, pCreateInfo, pAllocator, pPipelineLayout);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyPipelineLayout len None optional? False staticArrExpr  0) */
void validate_vkDestroyPipelineLayout(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineLayout pipelineLayout,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyPipelineLayout len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyPipelineLayout(
    VkDevice device,
    VkPipelineLayout pipelineLayout,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyPipelineLayout(&validateResult, device, pipelineLayout, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyPipelineLayout(vkStream, device, pipelineLayout, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSampler len None optional? False staticArrExpr  0) */
void validate_vkCreateSampler(
    VkResult* validateResult,
    VkDevice device,
    const VkSamplerCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSampler* pSampler)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSampler len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateSampler(
    VkDevice device,
    const VkSamplerCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSampler* pSampler)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSampler(&validateResult, device, pCreateInfo, pAllocator, pSampler);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateSampler(vkStream, device, pCreateInfo, pAllocator, pSampler);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySampler len None optional? False staticArrExpr  0) */
void validate_vkDestroySampler(
    VkResult* validateResult,
    VkDevice device,
    VkSampler sampler,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySampler len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroySampler(
    VkDevice device,
    VkSampler sampler,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySampler(&validateResult, device, sampler, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroySampler(vkStream, device, sampler, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDescriptorSetLayout len None optional? False staticArrExpr  0) */
void validate_vkCreateDescriptorSetLayout(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorSetLayout* pSetLayout)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDescriptorSetLayout len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateDescriptorSetLayout(
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorSetLayout* pSetLayout)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDescriptorSetLayout(&validateResult, device, pCreateInfo, pAllocator, pSetLayout);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateDescriptorSetLayout(vkStream, device, pCreateInfo, pAllocator, pSetLayout);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDescriptorSetLayout len None optional? False staticArrExpr  0) */
void validate_vkDestroyDescriptorSetLayout(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorSetLayout descriptorSetLayout,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDescriptorSetLayout len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyDescriptorSetLayout(
    VkDevice device,
    VkDescriptorSetLayout descriptorSetLayout,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDescriptorSetLayout(&validateResult, device, descriptorSetLayout, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyDescriptorSetLayout(vkStream, device, descriptorSetLayout, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDescriptorPool len None optional? False staticArrExpr  0) */
void validate_vkCreateDescriptorPool(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorPool* pDescriptorPool)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDescriptorPool len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateDescriptorPool(
    VkDevice device,
    const VkDescriptorPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorPool* pDescriptorPool)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDescriptorPool(&validateResult, device, pCreateInfo, pAllocator, pDescriptorPool);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateDescriptorPool(vkStream, device, pCreateInfo, pAllocator, pDescriptorPool);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDescriptorPool len None optional? False staticArrExpr  0) */
void validate_vkDestroyDescriptorPool(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorPool descriptorPool,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDescriptorPool len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyDescriptorPool(
    VkDevice device,
    VkDescriptorPool descriptorPool,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDescriptorPool(&validateResult, device, descriptorPool, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyDescriptorPool(vkStream, device, descriptorPool, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkResetDescriptorPool len None optional? False staticArrExpr  0) */
void validate_vkResetDescriptorPool(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorPool descriptorPool,
    VkDescriptorPoolResetFlags flags)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkResetDescriptorPool len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkResetDescriptorPool(
    VkDevice device,
    VkDescriptorPool descriptorPool,
    VkDescriptorPoolResetFlags flags)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkResetDescriptorPool(&validateResult, device, descriptorPool, flags);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkResetDescriptorPool(vkStream, device, descriptorPool, flags);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAllocateDescriptorSets len None optional? False staticArrExpr  0) */
void validate_vkAllocateDescriptorSets(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorSetAllocateInfo* pAllocateInfo,
    VkDescriptorSet* pDescriptorSets)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAllocateDescriptorSets len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkAllocateDescriptorSets(
    VkDevice device,
    const VkDescriptorSetAllocateInfo* pAllocateInfo,
    VkDescriptorSet* pDescriptorSets)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkAllocateDescriptorSets(&validateResult, device, pAllocateInfo, pDescriptorSets);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkAllocateDescriptorSets(vkStream, device, pAllocateInfo, pDescriptorSets);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkFreeDescriptorSets len None optional? False staticArrExpr  0) */
void validate_vkFreeDescriptorSets(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorPool descriptorPool,
    uint32_t descriptorSetCount,
    const VkDescriptorSet* pDescriptorSets)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkFreeDescriptorSets len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkFreeDescriptorSets(
    VkDevice device,
    VkDescriptorPool descriptorPool,
    uint32_t descriptorSetCount,
    const VkDescriptorSet* pDescriptorSets)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkFreeDescriptorSets(&validateResult, device, descriptorPool, descriptorSetCount, pDescriptorSets);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkFreeDescriptorSets(vkStream, device, descriptorPool, descriptorSetCount, pDescriptorSets);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkUpdateDescriptorSets len None optional? False staticArrExpr  0) */
void validate_vkUpdateDescriptorSets(
    VkResult* validateResult,
    VkDevice device,
    uint32_t descriptorWriteCount,
    const VkWriteDescriptorSet* pDescriptorWrites,
    uint32_t descriptorCopyCount,
    const VkCopyDescriptorSet* pDescriptorCopies)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkUpdateDescriptorSets len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkUpdateDescriptorSets(
    VkDevice device,
    uint32_t descriptorWriteCount,
    const VkWriteDescriptorSet* pDescriptorWrites,
    uint32_t descriptorCopyCount,
    const VkCopyDescriptorSet* pDescriptorCopies)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkUpdateDescriptorSets(&validateResult, device, descriptorWriteCount, pDescriptorWrites, descriptorCopyCount, pDescriptorCopies);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkUpdateDescriptorSets(vkStream, device, descriptorWriteCount, pDescriptorWrites, descriptorCopyCount, pDescriptorCopies);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateFramebuffer len None optional? False staticArrExpr  0) */
void validate_vkCreateFramebuffer(
    VkResult* validateResult,
    VkDevice device,
    const VkFramebufferCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFramebuffer* pFramebuffer)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateFramebuffer len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateFramebuffer(
    VkDevice device,
    const VkFramebufferCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFramebuffer* pFramebuffer)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateFramebuffer(&validateResult, device, pCreateInfo, pAllocator, pFramebuffer);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateFramebuffer(vkStream, device, pCreateInfo, pAllocator, pFramebuffer);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyFramebuffer len None optional? False staticArrExpr  0) */
void validate_vkDestroyFramebuffer(
    VkResult* validateResult,
    VkDevice device,
    VkFramebuffer framebuffer,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyFramebuffer len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyFramebuffer(
    VkDevice device,
    VkFramebuffer framebuffer,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyFramebuffer(&validateResult, device, framebuffer, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyFramebuffer(vkStream, device, framebuffer, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateRenderPass len None optional? False staticArrExpr  0) */
void validate_vkCreateRenderPass(
    VkResult* validateResult,
    VkDevice device,
    const VkRenderPassCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkRenderPass* pRenderPass)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateRenderPass len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateRenderPass(
    VkDevice device,
    const VkRenderPassCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkRenderPass* pRenderPass)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateRenderPass(&validateResult, device, pCreateInfo, pAllocator, pRenderPass);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateRenderPass(vkStream, device, pCreateInfo, pAllocator, pRenderPass);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyRenderPass len None optional? False staticArrExpr  0) */
void validate_vkDestroyRenderPass(
    VkResult* validateResult,
    VkDevice device,
    VkRenderPass renderPass,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyRenderPass len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyRenderPass(
    VkDevice device,
    VkRenderPass renderPass,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyRenderPass(&validateResult, device, renderPass, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyRenderPass(vkStream, device, renderPass, pAllocator);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetRenderAreaGranularity len None optional? False staticArrExpr  0) */
void validate_vkGetRenderAreaGranularity(
    VkResult* validateResult,
    VkDevice device,
    VkRenderPass renderPass,
    VkExtent2D* pGranularity)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetRenderAreaGranularity len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetRenderAreaGranularity(
    VkDevice device,
    VkRenderPass renderPass,
    VkExtent2D* pGranularity)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetRenderAreaGranularity(&validateResult, device, renderPass, pGranularity);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetRenderAreaGranularity(vkStream, device, renderPass, pGranularity);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateCommandPool len None optional? False staticArrExpr  0) */
void validate_vkCreateCommandPool(
    VkResult* validateResult,
    VkDevice device,
    const VkCommandPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkCommandPool* pCommandPool)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateCommandPool len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateCommandPool(
    VkDevice device,
    const VkCommandPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkCommandPool* pCommandPool)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateCommandPool(&validateResult, device, pCreateInfo, pAllocator, pCommandPool);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateCommandPool(vkStream, device, pCreateInfo, pAllocator, pCommandPool);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyCommandPool len None optional? False staticArrExpr  0) */
void validate_vkDestroyCommandPool(
    VkResult* validateResult,
    VkDevice device,
    VkCommandPool commandPool,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyCommandPool len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyCommandPool(
    VkDevice device,
    VkCommandPool commandPool,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyCommandPool(&validateResult, device, commandPool, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyCommandPool(vkStream, device, commandPool, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkResetCommandPool len None optional? False staticArrExpr  0) */
void validate_vkResetCommandPool(
    VkResult* validateResult,
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolResetFlags flags)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkResetCommandPool len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkResetCommandPool(
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolResetFlags flags)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkResetCommandPool(&validateResult, device, commandPool, flags);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkResetCommandPool(vkStream, device, commandPool, flags);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAllocateCommandBuffers len None optional? False staticArrExpr  0) */
void validate_vkAllocateCommandBuffers(
    VkResult* validateResult,
    VkDevice device,
    const VkCommandBufferAllocateInfo* pAllocateInfo,
    VkCommandBuffer* pCommandBuffers)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAllocateCommandBuffers len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkAllocateCommandBuffers(
    VkDevice device,
    const VkCommandBufferAllocateInfo* pAllocateInfo,
    VkCommandBuffer* pCommandBuffers)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkAllocateCommandBuffers(&validateResult, device, pAllocateInfo, pCommandBuffers);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkAllocateCommandBuffers(vkStream, device, pAllocateInfo, pCommandBuffers);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkFreeCommandBuffers len None optional? False staticArrExpr  0) */
void validate_vkFreeCommandBuffers(
    VkResult* validateResult,
    VkDevice device,
    VkCommandPool commandPool,
    uint32_t commandBufferCount,
    const VkCommandBuffer* pCommandBuffers)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkFreeCommandBuffers len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkFreeCommandBuffers(
    VkDevice device,
    VkCommandPool commandPool,
    uint32_t commandBufferCount,
    const VkCommandBuffer* pCommandBuffers)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkFreeCommandBuffers(&validateResult, device, commandPool, commandBufferCount, pCommandBuffers);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkFreeCommandBuffers(vkStream, device, commandPool, commandBufferCount, pCommandBuffers);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBeginCommandBuffer len None optional? False staticArrExpr  0) */
void validate_vkBeginCommandBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkCommandBufferBeginInfo* pBeginInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBeginCommandBuffer len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkBeginCommandBuffer(
    VkCommandBuffer commandBuffer,
    const VkCommandBufferBeginInfo* pBeginInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkBeginCommandBuffer(&validateResult, commandBuffer, pBeginInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkBeginCommandBuffer(vkStream, commandBuffer, pBeginInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEndCommandBuffer len None optional? False staticArrExpr  0) */
void validate_vkEndCommandBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEndCommandBuffer len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkEndCommandBuffer(
    VkCommandBuffer commandBuffer)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkEndCommandBuffer(&validateResult, commandBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkEndCommandBuffer(vkStream, commandBuffer);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkResetCommandBuffer len None optional? False staticArrExpr  0) */
void validate_vkResetCommandBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkCommandBufferResetFlags flags)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkResetCommandBuffer len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkResetCommandBuffer(
    VkCommandBuffer commandBuffer,
    VkCommandBufferResetFlags flags)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkResetCommandBuffer(&validateResult, commandBuffer, flags);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkResetCommandBuffer(vkStream, commandBuffer, flags);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBindPipeline len None optional? False staticArrExpr  0) */
void validate_vkCmdBindPipeline(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipeline pipeline)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBindPipeline len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdBindPipeline(
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipeline pipeline)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBindPipeline(&validateResult, commandBuffer, pipelineBindPoint, pipeline);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdBindPipeline(vkStream, commandBuffer, pipelineBindPoint, pipeline);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetViewport len None optional? False staticArrExpr  0) */
void validate_vkCmdSetViewport(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t firstViewport,
    uint32_t viewportCount,
    const VkViewport* pViewports)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetViewport len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetViewport(
    VkCommandBuffer commandBuffer,
    uint32_t firstViewport,
    uint32_t viewportCount,
    const VkViewport* pViewports)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetViewport(&validateResult, commandBuffer, firstViewport, viewportCount, pViewports);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetViewport(vkStream, commandBuffer, firstViewport, viewportCount, pViewports);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetScissor len None optional? False staticArrExpr  0) */
void validate_vkCmdSetScissor(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t firstScissor,
    uint32_t scissorCount,
    const VkRect2D* pScissors)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetScissor len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetScissor(
    VkCommandBuffer commandBuffer,
    uint32_t firstScissor,
    uint32_t scissorCount,
    const VkRect2D* pScissors)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetScissor(&validateResult, commandBuffer, firstScissor, scissorCount, pScissors);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetScissor(vkStream, commandBuffer, firstScissor, scissorCount, pScissors);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetLineWidth len None optional? False staticArrExpr  0) */
void validate_vkCmdSetLineWidth(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    float lineWidth)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetLineWidth len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetLineWidth(
    VkCommandBuffer commandBuffer,
    float lineWidth)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetLineWidth(&validateResult, commandBuffer, lineWidth);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetLineWidth(vkStream, commandBuffer, lineWidth);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetDepthBias len None optional? False staticArrExpr  0) */
void validate_vkCmdSetDepthBias(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    float depthBiasConstantFactor,
    float depthBiasClamp,
    float depthBiasSlopeFactor)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetDepthBias len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetDepthBias(
    VkCommandBuffer commandBuffer,
    float depthBiasConstantFactor,
    float depthBiasClamp,
    float depthBiasSlopeFactor)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetDepthBias(&validateResult, commandBuffer, depthBiasConstantFactor, depthBiasClamp, depthBiasSlopeFactor);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetDepthBias(vkStream, commandBuffer, depthBiasConstantFactor, depthBiasClamp, depthBiasSlopeFactor);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetBlendConstants len None optional? False staticArrExpr  0) */
void validate_vkCmdSetBlendConstants(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const float blendConstants[4])
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetBlendConstants len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetBlendConstants(
    VkCommandBuffer commandBuffer,
    const float blendConstants[4])
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetBlendConstants(&validateResult, commandBuffer, blendConstants);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetBlendConstants(vkStream, commandBuffer, blendConstants);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetDepthBounds len None optional? False staticArrExpr  0) */
void validate_vkCmdSetDepthBounds(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    float minDepthBounds,
    float maxDepthBounds)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetDepthBounds len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetDepthBounds(
    VkCommandBuffer commandBuffer,
    float minDepthBounds,
    float maxDepthBounds)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetDepthBounds(&validateResult, commandBuffer, minDepthBounds, maxDepthBounds);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetDepthBounds(vkStream, commandBuffer, minDepthBounds, maxDepthBounds);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetStencilCompareMask len None optional? False staticArrExpr  0) */
void validate_vkCmdSetStencilCompareMask(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t compareMask)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetStencilCompareMask len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetStencilCompareMask(
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t compareMask)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetStencilCompareMask(&validateResult, commandBuffer, faceMask, compareMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetStencilCompareMask(vkStream, commandBuffer, faceMask, compareMask);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetStencilWriteMask len None optional? False staticArrExpr  0) */
void validate_vkCmdSetStencilWriteMask(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t writeMask)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetStencilWriteMask len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetStencilWriteMask(
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t writeMask)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetStencilWriteMask(&validateResult, commandBuffer, faceMask, writeMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetStencilWriteMask(vkStream, commandBuffer, faceMask, writeMask);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetStencilReference len None optional? False staticArrExpr  0) */
void validate_vkCmdSetStencilReference(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t reference)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetStencilReference len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetStencilReference(
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t reference)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetStencilReference(&validateResult, commandBuffer, faceMask, reference);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetStencilReference(vkStream, commandBuffer, faceMask, reference);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBindDescriptorSets len None optional? False staticArrExpr  0) */
void validate_vkCmdBindDescriptorSets(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipelineLayout layout,
    uint32_t firstSet,
    uint32_t descriptorSetCount,
    const VkDescriptorSet* pDescriptorSets,
    uint32_t dynamicOffsetCount,
    const uint32_t* pDynamicOffsets)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBindDescriptorSets len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdBindDescriptorSets(
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipelineLayout layout,
    uint32_t firstSet,
    uint32_t descriptorSetCount,
    const VkDescriptorSet* pDescriptorSets,
    uint32_t dynamicOffsetCount,
    const uint32_t* pDynamicOffsets)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBindDescriptorSets(&validateResult, commandBuffer, pipelineBindPoint, layout, firstSet, descriptorSetCount, pDescriptorSets, dynamicOffsetCount, pDynamicOffsets);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdBindDescriptorSets(vkStream, commandBuffer, pipelineBindPoint, layout, firstSet, descriptorSetCount, pDescriptorSets, dynamicOffsetCount, pDynamicOffsets);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBindIndexBuffer len None optional? False staticArrExpr  0) */
void validate_vkCmdBindIndexBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkIndexType indexType)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBindIndexBuffer len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdBindIndexBuffer(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkIndexType indexType)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBindIndexBuffer(&validateResult, commandBuffer, buffer, offset, indexType);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdBindIndexBuffer(vkStream, commandBuffer, buffer, offset, indexType);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBindVertexBuffers len None optional? False staticArrExpr  0) */
void validate_vkCmdBindVertexBuffers(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t firstBinding,
    uint32_t bindingCount,
    const VkBuffer* pBuffers,
    const VkDeviceSize* pOffsets)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBindVertexBuffers len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdBindVertexBuffers(
    VkCommandBuffer commandBuffer,
    uint32_t firstBinding,
    uint32_t bindingCount,
    const VkBuffer* pBuffers,
    const VkDeviceSize* pOffsets)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBindVertexBuffers(&validateResult, commandBuffer, firstBinding, bindingCount, pBuffers, pOffsets);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdBindVertexBuffers(vkStream, commandBuffer, firstBinding, bindingCount, pBuffers, pOffsets);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDraw len None optional? False staticArrExpr  0) */
void validate_vkCmdDraw(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t vertexCount,
    uint32_t instanceCount,
    uint32_t firstVertex,
    uint32_t firstInstance)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDraw len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDraw(
    VkCommandBuffer commandBuffer,
    uint32_t vertexCount,
    uint32_t instanceCount,
    uint32_t firstVertex,
    uint32_t firstInstance)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDraw(&validateResult, commandBuffer, vertexCount, instanceCount, firstVertex, firstInstance);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDraw(vkStream, commandBuffer, vertexCount, instanceCount, firstVertex, firstInstance);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndexed len None optional? False staticArrExpr  0) */
void validate_vkCmdDrawIndexed(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t indexCount,
    uint32_t instanceCount,
    uint32_t firstIndex,
    int32_t vertexOffset,
    uint32_t firstInstance)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndexed len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDrawIndexed(
    VkCommandBuffer commandBuffer,
    uint32_t indexCount,
    uint32_t instanceCount,
    uint32_t firstIndex,
    int32_t vertexOffset,
    uint32_t firstInstance)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndexed(&validateResult, commandBuffer, indexCount, instanceCount, firstIndex, vertexOffset, firstInstance);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDrawIndexed(vkStream, commandBuffer, indexCount, instanceCount, firstIndex, vertexOffset, firstInstance);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndirect len None optional? False staticArrExpr  0) */
void validate_vkCmdDrawIndirect(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    uint32_t drawCount,
    uint32_t stride)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndirect len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDrawIndirect(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    uint32_t drawCount,
    uint32_t stride)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndirect(&validateResult, commandBuffer, buffer, offset, drawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDrawIndirect(vkStream, commandBuffer, buffer, offset, drawCount, stride);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndexedIndirect len None optional? False staticArrExpr  0) */
void validate_vkCmdDrawIndexedIndirect(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    uint32_t drawCount,
    uint32_t stride)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndexedIndirect len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDrawIndexedIndirect(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    uint32_t drawCount,
    uint32_t stride)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndexedIndirect(&validateResult, commandBuffer, buffer, offset, drawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDrawIndexedIndirect(vkStream, commandBuffer, buffer, offset, drawCount, stride);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDispatch len None optional? False staticArrExpr  0) */
void validate_vkCmdDispatch(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDispatch len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDispatch(
    VkCommandBuffer commandBuffer,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDispatch(&validateResult, commandBuffer, groupCountX, groupCountY, groupCountZ);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDispatch(vkStream, commandBuffer, groupCountX, groupCountY, groupCountZ);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDispatchIndirect len None optional? False staticArrExpr  0) */
void validate_vkCmdDispatchIndirect(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDispatchIndirect len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDispatchIndirect(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDispatchIndirect(&validateResult, commandBuffer, buffer, offset);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDispatchIndirect(vkStream, commandBuffer, buffer, offset);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdCopyBuffer len None optional? False staticArrExpr  0) */
void validate_vkCmdCopyBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer srcBuffer,
    VkBuffer dstBuffer,
    uint32_t regionCount,
    const VkBufferCopy* pRegions)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdCopyBuffer len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdCopyBuffer(
    VkCommandBuffer commandBuffer,
    VkBuffer srcBuffer,
    VkBuffer dstBuffer,
    uint32_t regionCount,
    const VkBufferCopy* pRegions)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdCopyBuffer(&validateResult, commandBuffer, srcBuffer, dstBuffer, regionCount, pRegions);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdCopyBuffer(vkStream, commandBuffer, srcBuffer, dstBuffer, regionCount, pRegions);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdCopyImage len None optional? False staticArrExpr  0) */
void validate_vkCmdCopyImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageCopy* pRegions)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdCopyImage len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdCopyImage(
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageCopy* pRegions)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdCopyImage(&validateResult, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdCopyImage(vkStream, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBlitImage len None optional? False staticArrExpr  0) */
void validate_vkCmdBlitImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageBlit* pRegions,
    VkFilter filter)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBlitImage len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdBlitImage(
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageBlit* pRegions,
    VkFilter filter)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBlitImage(&validateResult, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions, filter);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdBlitImage(vkStream, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions, filter);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdCopyBufferToImage len None optional? False staticArrExpr  0) */
void validate_vkCmdCopyBufferToImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer srcBuffer,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkBufferImageCopy* pRegions)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdCopyBufferToImage len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdCopyBufferToImage(
    VkCommandBuffer commandBuffer,
    VkBuffer srcBuffer,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkBufferImageCopy* pRegions)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdCopyBufferToImage(&validateResult, commandBuffer, srcBuffer, dstImage, dstImageLayout, regionCount, pRegions);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdCopyBufferToImage(vkStream, commandBuffer, srcBuffer, dstImage, dstImageLayout, regionCount, pRegions);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdCopyImageToBuffer len None optional? False staticArrExpr  0) */
void validate_vkCmdCopyImageToBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkBuffer dstBuffer,
    uint32_t regionCount,
    const VkBufferImageCopy* pRegions)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdCopyImageToBuffer len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdCopyImageToBuffer(
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkBuffer dstBuffer,
    uint32_t regionCount,
    const VkBufferImageCopy* pRegions)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdCopyImageToBuffer(&validateResult, commandBuffer, srcImage, srcImageLayout, dstBuffer, regionCount, pRegions);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdCopyImageToBuffer(vkStream, commandBuffer, srcImage, srcImageLayout, dstBuffer, regionCount, pRegions);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdUpdateBuffer len None optional? False staticArrExpr  0) */
void validate_vkCmdUpdateBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize dataSize,
    const void* pData)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdUpdateBuffer len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdUpdateBuffer(
    VkCommandBuffer commandBuffer,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize dataSize,
    const void* pData)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdUpdateBuffer(&validateResult, commandBuffer, dstBuffer, dstOffset, dataSize, pData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdUpdateBuffer(vkStream, commandBuffer, dstBuffer, dstOffset, dataSize, pData);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdFillBuffer len None optional? False staticArrExpr  0) */
void validate_vkCmdFillBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize size,
    uint32_t data)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdFillBuffer len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdFillBuffer(
    VkCommandBuffer commandBuffer,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize size,
    uint32_t data)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdFillBuffer(&validateResult, commandBuffer, dstBuffer, dstOffset, size, data);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdFillBuffer(vkStream, commandBuffer, dstBuffer, dstOffset, size, data);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdClearColorImage len None optional? False staticArrExpr  0) */
void validate_vkCmdClearColorImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage image,
    VkImageLayout imageLayout,
    const VkClearColorValue* pColor,
    uint32_t rangeCount,
    const VkImageSubresourceRange* pRanges)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdClearColorImage len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdClearColorImage(
    VkCommandBuffer commandBuffer,
    VkImage image,
    VkImageLayout imageLayout,
    const VkClearColorValue* pColor,
    uint32_t rangeCount,
    const VkImageSubresourceRange* pRanges)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdClearColorImage(&validateResult, commandBuffer, image, imageLayout, pColor, rangeCount, pRanges);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdClearColorImage(vkStream, commandBuffer, image, imageLayout, pColor, rangeCount, pRanges);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdClearDepthStencilImage len None optional? False staticArrExpr  0) */
void validate_vkCmdClearDepthStencilImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage image,
    VkImageLayout imageLayout,
    const VkClearDepthStencilValue* pDepthStencil,
    uint32_t rangeCount,
    const VkImageSubresourceRange* pRanges)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdClearDepthStencilImage len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdClearDepthStencilImage(
    VkCommandBuffer commandBuffer,
    VkImage image,
    VkImageLayout imageLayout,
    const VkClearDepthStencilValue* pDepthStencil,
    uint32_t rangeCount,
    const VkImageSubresourceRange* pRanges)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdClearDepthStencilImage(&validateResult, commandBuffer, image, imageLayout, pDepthStencil, rangeCount, pRanges);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdClearDepthStencilImage(vkStream, commandBuffer, image, imageLayout, pDepthStencil, rangeCount, pRanges);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdClearAttachments len None optional? False staticArrExpr  0) */
void validate_vkCmdClearAttachments(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t attachmentCount,
    const VkClearAttachment* pAttachments,
    uint32_t rectCount,
    const VkClearRect* pRects)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdClearAttachments len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdClearAttachments(
    VkCommandBuffer commandBuffer,
    uint32_t attachmentCount,
    const VkClearAttachment* pAttachments,
    uint32_t rectCount,
    const VkClearRect* pRects)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdClearAttachments(&validateResult, commandBuffer, attachmentCount, pAttachments, rectCount, pRects);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdClearAttachments(vkStream, commandBuffer, attachmentCount, pAttachments, rectCount, pRects);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdResolveImage len None optional? False staticArrExpr  0) */
void validate_vkCmdResolveImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageResolve* pRegions)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdResolveImage len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdResolveImage(
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageResolve* pRegions)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdResolveImage(&validateResult, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdResolveImage(vkStream, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetEvent len None optional? False staticArrExpr  0) */
void validate_vkCmdSetEvent(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkEvent event,
    VkPipelineStageFlags stageMask)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetEvent len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetEvent(
    VkCommandBuffer commandBuffer,
    VkEvent event,
    VkPipelineStageFlags stageMask)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetEvent(&validateResult, commandBuffer, event, stageMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetEvent(vkStream, commandBuffer, event, stageMask);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdResetEvent len None optional? False staticArrExpr  0) */
void validate_vkCmdResetEvent(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkEvent event,
    VkPipelineStageFlags stageMask)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdResetEvent len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdResetEvent(
    VkCommandBuffer commandBuffer,
    VkEvent event,
    VkPipelineStageFlags stageMask)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdResetEvent(&validateResult, commandBuffer, event, stageMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdResetEvent(vkStream, commandBuffer, event, stageMask);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdWaitEvents len None optional? False staticArrExpr  0) */
void validate_vkCmdWaitEvents(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t eventCount,
    const VkEvent* pEvents,
    VkPipelineStageFlags srcStageMask,
    VkPipelineStageFlags dstStageMask,
    uint32_t memoryBarrierCount,
    const VkMemoryBarrier* pMemoryBarriers,
    uint32_t bufferMemoryBarrierCount,
    const VkBufferMemoryBarrier* pBufferMemoryBarriers,
    uint32_t imageMemoryBarrierCount,
    const VkImageMemoryBarrier* pImageMemoryBarriers)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdWaitEvents len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdWaitEvents(
    VkCommandBuffer commandBuffer,
    uint32_t eventCount,
    const VkEvent* pEvents,
    VkPipelineStageFlags srcStageMask,
    VkPipelineStageFlags dstStageMask,
    uint32_t memoryBarrierCount,
    const VkMemoryBarrier* pMemoryBarriers,
    uint32_t bufferMemoryBarrierCount,
    const VkBufferMemoryBarrier* pBufferMemoryBarriers,
    uint32_t imageMemoryBarrierCount,
    const VkImageMemoryBarrier* pImageMemoryBarriers)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdWaitEvents(&validateResult, commandBuffer, eventCount, pEvents, srcStageMask, dstStageMask, memoryBarrierCount, pMemoryBarriers, bufferMemoryBarrierCount, pBufferMemoryBarriers, imageMemoryBarrierCount, pImageMemoryBarriers);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdWaitEvents(vkStream, commandBuffer, eventCount, pEvents, srcStageMask, dstStageMask, memoryBarrierCount, pMemoryBarriers, bufferMemoryBarrierCount, pBufferMemoryBarriers, imageMemoryBarrierCount, pImageMemoryBarriers);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdPipelineBarrier len None optional? False staticArrExpr  0) */
void validate_vkCmdPipelineBarrier(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlags srcStageMask,
    VkPipelineStageFlags dstStageMask,
    VkDependencyFlags dependencyFlags,
    uint32_t memoryBarrierCount,
    const VkMemoryBarrier* pMemoryBarriers,
    uint32_t bufferMemoryBarrierCount,
    const VkBufferMemoryBarrier* pBufferMemoryBarriers,
    uint32_t imageMemoryBarrierCount,
    const VkImageMemoryBarrier* pImageMemoryBarriers)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdPipelineBarrier len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdPipelineBarrier(
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlags srcStageMask,
    VkPipelineStageFlags dstStageMask,
    VkDependencyFlags dependencyFlags,
    uint32_t memoryBarrierCount,
    const VkMemoryBarrier* pMemoryBarriers,
    uint32_t bufferMemoryBarrierCount,
    const VkBufferMemoryBarrier* pBufferMemoryBarriers,
    uint32_t imageMemoryBarrierCount,
    const VkImageMemoryBarrier* pImageMemoryBarriers)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdPipelineBarrier(&validateResult, commandBuffer, srcStageMask, dstStageMask, dependencyFlags, memoryBarrierCount, pMemoryBarriers, bufferMemoryBarrierCount, pBufferMemoryBarriers, imageMemoryBarrierCount, pImageMemoryBarriers);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdPipelineBarrier(vkStream, commandBuffer, srcStageMask, dstStageMask, dependencyFlags, memoryBarrierCount, pMemoryBarriers, bufferMemoryBarrierCount, pBufferMemoryBarriers, imageMemoryBarrierCount, pImageMemoryBarriers);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBeginQuery len None optional? False staticArrExpr  0) */
void validate_vkCmdBeginQuery(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t query,
    VkQueryControlFlags flags)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBeginQuery len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdBeginQuery(
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t query,
    VkQueryControlFlags flags)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBeginQuery(&validateResult, commandBuffer, queryPool, query, flags);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdBeginQuery(vkStream, commandBuffer, queryPool, query, flags);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdEndQuery len None optional? False staticArrExpr  0) */
void validate_vkCmdEndQuery(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t query)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdEndQuery len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdEndQuery(
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t query)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdEndQuery(&validateResult, commandBuffer, queryPool, query);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdEndQuery(vkStream, commandBuffer, queryPool, query);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdResetQueryPool len None optional? False staticArrExpr  0) */
void validate_vkCmdResetQueryPool(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdResetQueryPool len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdResetQueryPool(
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdResetQueryPool(&validateResult, commandBuffer, queryPool, firstQuery, queryCount);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdResetQueryPool(vkStream, commandBuffer, queryPool, firstQuery, queryCount);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdWriteTimestamp len None optional? False staticArrExpr  0) */
void validate_vkCmdWriteTimestamp(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlagBits pipelineStage,
    VkQueryPool queryPool,
    uint32_t query)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdWriteTimestamp len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdWriteTimestamp(
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlagBits pipelineStage,
    VkQueryPool queryPool,
    uint32_t query)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdWriteTimestamp(&validateResult, commandBuffer, pipelineStage, queryPool, query);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdWriteTimestamp(vkStream, commandBuffer, pipelineStage, queryPool, query);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdCopyQueryPoolResults len None optional? False staticArrExpr  0) */
void validate_vkCmdCopyQueryPoolResults(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize stride,
    VkQueryResultFlags flags)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdCopyQueryPoolResults len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdCopyQueryPoolResults(
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize stride,
    VkQueryResultFlags flags)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdCopyQueryPoolResults(&validateResult, commandBuffer, queryPool, firstQuery, queryCount, dstBuffer, dstOffset, stride, flags);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdCopyQueryPoolResults(vkStream, commandBuffer, queryPool, firstQuery, queryCount, dstBuffer, dstOffset, stride, flags);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdPushConstants len None optional? False staticArrExpr  0) */
void validate_vkCmdPushConstants(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineLayout layout,
    VkShaderStageFlags stageFlags,
    uint32_t offset,
    uint32_t size,
    const void* pValues)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdPushConstants len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdPushConstants(
    VkCommandBuffer commandBuffer,
    VkPipelineLayout layout,
    VkShaderStageFlags stageFlags,
    uint32_t offset,
    uint32_t size,
    const void* pValues)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdPushConstants(&validateResult, commandBuffer, layout, stageFlags, offset, size, pValues);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdPushConstants(vkStream, commandBuffer, layout, stageFlags, offset, size, pValues);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBeginRenderPass len None optional? False staticArrExpr  0) */
void validate_vkCmdBeginRenderPass(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkRenderPassBeginInfo* pRenderPassBegin,
    VkSubpassContents contents)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBeginRenderPass len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdBeginRenderPass(
    VkCommandBuffer commandBuffer,
    const VkRenderPassBeginInfo* pRenderPassBegin,
    VkSubpassContents contents)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBeginRenderPass(&validateResult, commandBuffer, pRenderPassBegin, contents);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdBeginRenderPass(vkStream, commandBuffer, pRenderPassBegin, contents);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdNextSubpass len None optional? False staticArrExpr  0) */
void validate_vkCmdNextSubpass(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkSubpassContents contents)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdNextSubpass len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdNextSubpass(
    VkCommandBuffer commandBuffer,
    VkSubpassContents contents)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdNextSubpass(&validateResult, commandBuffer, contents);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdNextSubpass(vkStream, commandBuffer, contents);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdEndRenderPass len None optional? False staticArrExpr  0) */
void validate_vkCmdEndRenderPass(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdEndRenderPass len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdEndRenderPass(
    VkCommandBuffer commandBuffer)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdEndRenderPass(&validateResult, commandBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdEndRenderPass(vkStream, commandBuffer);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdExecuteCommands len None optional? False staticArrExpr  0) */
void validate_vkCmdExecuteCommands(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t commandBufferCount,
    const VkCommandBuffer* pCommandBuffers)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdExecuteCommands len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdExecuteCommands(
    VkCommandBuffer commandBuffer,
    uint32_t commandBufferCount,
    const VkCommandBuffer* pCommandBuffers)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdExecuteCommands(&validateResult, commandBuffer, commandBufferCount, pCommandBuffers);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdExecuteCommands(vkStream, commandBuffer, commandBufferCount, pCommandBuffers);
}

#endif

#ifdef VK_VERSION_1_1

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumerateInstanceVersion len None optional? False staticArrExpr  0) */
void validate_vkEnumerateInstanceVersion(
    VkResult* validateResult,
    uint32_t* pApiVersion)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumerateInstanceVersion len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkEnumerateInstanceVersion(
    uint32_t* pApiVersion)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumerateInstanceVersion(&validateResult, pApiVersion);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkEnumerateInstanceVersion(vkStream, pApiVersion);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindBufferMemory2 len None optional? False staticArrExpr  0) */
void validate_vkBindBufferMemory2(
    VkResult* validateResult,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindBufferMemoryInfo* pBindInfos)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindBufferMemory2 len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkBindBufferMemory2(
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindBufferMemoryInfo* pBindInfos)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindBufferMemory2(&validateResult, device, bindInfoCount, pBindInfos);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkBindBufferMemory2(vkStream, device, bindInfoCount, pBindInfos);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindImageMemory2 len None optional? False staticArrExpr  0) */
void validate_vkBindImageMemory2(
    VkResult* validateResult,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindImageMemoryInfo* pBindInfos)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindImageMemory2 len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkBindImageMemory2(
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindImageMemoryInfo* pBindInfos)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindImageMemory2(&validateResult, device, bindInfoCount, pBindInfos);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkBindImageMemory2(vkStream, device, bindInfoCount, pBindInfos);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetDeviceGroupPeerMemoryFeatures len None optional? False staticArrExpr  0) */
void validate_vkGetDeviceGroupPeerMemoryFeatures(
    VkResult* validateResult,
    VkDevice device,
    uint32_t heapIndex,
    uint32_t localDeviceIndex,
    uint32_t remoteDeviceIndex,
    VkPeerMemoryFeatureFlags* pPeerMemoryFeatures)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetDeviceGroupPeerMemoryFeatures len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetDeviceGroupPeerMemoryFeatures(
    VkDevice device,
    uint32_t heapIndex,
    uint32_t localDeviceIndex,
    uint32_t remoteDeviceIndex,
    VkPeerMemoryFeatureFlags* pPeerMemoryFeatures)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceGroupPeerMemoryFeatures(&validateResult, device, heapIndex, localDeviceIndex, remoteDeviceIndex, pPeerMemoryFeatures);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetDeviceGroupPeerMemoryFeatures(vkStream, device, heapIndex, localDeviceIndex, remoteDeviceIndex, pPeerMemoryFeatures);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetDeviceMask len None optional? False staticArrExpr  0) */
void validate_vkCmdSetDeviceMask(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t deviceMask)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetDeviceMask len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetDeviceMask(
    VkCommandBuffer commandBuffer,
    uint32_t deviceMask)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetDeviceMask(&validateResult, commandBuffer, deviceMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetDeviceMask(vkStream, commandBuffer, deviceMask);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDispatchBase len None optional? False staticArrExpr  0) */
void validate_vkCmdDispatchBase(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t baseGroupX,
    uint32_t baseGroupY,
    uint32_t baseGroupZ,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDispatchBase len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDispatchBase(
    VkCommandBuffer commandBuffer,
    uint32_t baseGroupX,
    uint32_t baseGroupY,
    uint32_t baseGroupZ,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDispatchBase(&validateResult, commandBuffer, baseGroupX, baseGroupY, baseGroupZ, groupCountX, groupCountY, groupCountZ);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDispatchBase(vkStream, commandBuffer, baseGroupX, baseGroupY, baseGroupZ, groupCountX, groupCountY, groupCountZ);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumeratePhysicalDeviceGroups len None optional? False staticArrExpr  0) */
void validate_vkEnumeratePhysicalDeviceGroups(
    VkResult* validateResult,
    VkInstance instance,
    uint32_t* pPhysicalDeviceGroupCount,
    VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumeratePhysicalDeviceGroups len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkEnumeratePhysicalDeviceGroups(
    VkInstance instance,
    uint32_t* pPhysicalDeviceGroupCount,
    VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumeratePhysicalDeviceGroups(&validateResult, instance, pPhysicalDeviceGroupCount, pPhysicalDeviceGroupProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkEnumeratePhysicalDeviceGroups(vkStream, instance, pPhysicalDeviceGroupCount, pPhysicalDeviceGroupProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageMemoryRequirements2 len None optional? False staticArrExpr  0) */
void validate_vkGetImageMemoryRequirements2(
    VkResult* validateResult,
    VkDevice device,
    const VkImageMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageMemoryRequirements2 len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetImageMemoryRequirements2(
    VkDevice device,
    const VkImageMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageMemoryRequirements2(&validateResult, device, pInfo, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetImageMemoryRequirements2(vkStream, device, pInfo, pMemoryRequirements);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetBufferMemoryRequirements2 len None optional? False staticArrExpr  0) */
void validate_vkGetBufferMemoryRequirements2(
    VkResult* validateResult,
    VkDevice device,
    const VkBufferMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetBufferMemoryRequirements2 len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetBufferMemoryRequirements2(
    VkDevice device,
    const VkBufferMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetBufferMemoryRequirements2(&validateResult, device, pInfo, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetBufferMemoryRequirements2(vkStream, device, pInfo, pMemoryRequirements);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageSparseMemoryRequirements2 len None optional? False staticArrExpr  0) */
void validate_vkGetImageSparseMemoryRequirements2(
    VkResult* validateResult,
    VkDevice device,
    const VkImageSparseMemoryRequirementsInfo2* pInfo,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements2* pSparseMemoryRequirements)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageSparseMemoryRequirements2 len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetImageSparseMemoryRequirements2(
    VkDevice device,
    const VkImageSparseMemoryRequirementsInfo2* pInfo,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements2* pSparseMemoryRequirements)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageSparseMemoryRequirements2(&validateResult, device, pInfo, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetImageSparseMemoryRequirements2(vkStream, device, pInfo, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFeatures2 len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceFeatures2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures2* pFeatures)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFeatures2 len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceFeatures2(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures2* pFeatures)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFeatures2(&validateResult, physicalDevice, pFeatures);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceFeatures2(vkStream, physicalDevice, pFeatures);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceProperties2 len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties2* pProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceProperties2 len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceProperties2(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties2* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceProperties2(&validateResult, physicalDevice, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceProperties2(vkStream, physicalDevice, pProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFormatProperties2 len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceFormatProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties2* pFormatProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFormatProperties2 len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceFormatProperties2(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties2* pFormatProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFormatProperties2(&validateResult, physicalDevice, format, pFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceFormatProperties2(vkStream, physicalDevice, format, pFormatProperties);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceImageFormatProperties2 len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceImageFormatProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo,
    VkImageFormatProperties2* pImageFormatProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceImageFormatProperties2 len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceImageFormatProperties2(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo,
    VkImageFormatProperties2* pImageFormatProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceImageFormatProperties2(&validateResult, physicalDevice, pImageFormatInfo, pImageFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceImageFormatProperties2(vkStream, physicalDevice, pImageFormatInfo, pImageFormatProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceQueueFamilyProperties2 len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceQueueFamilyProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties2* pQueueFamilyProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceQueueFamilyProperties2 len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceQueueFamilyProperties2(
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties2* pQueueFamilyProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceQueueFamilyProperties2(&validateResult, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceQueueFamilyProperties2(vkStream, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceMemoryProperties2 len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceMemoryProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties2* pMemoryProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceMemoryProperties2 len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceMemoryProperties2(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties2* pMemoryProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceMemoryProperties2(&validateResult, physicalDevice, pMemoryProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceMemoryProperties2(vkStream, physicalDevice, pMemoryProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceSparseImageFormatProperties2 len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceSparseImageFormatProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties2* pProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceSparseImageFormatProperties2 len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceSparseImageFormatProperties2(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties2* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSparseImageFormatProperties2(&validateResult, physicalDevice, pFormatInfo, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceSparseImageFormatProperties2(vkStream, physicalDevice, pFormatInfo, pPropertyCount, pProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkTrimCommandPool len None optional? False staticArrExpr  0) */
void validate_vkTrimCommandPool(
    VkResult* validateResult,
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolTrimFlags flags)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkTrimCommandPool len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkTrimCommandPool(
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolTrimFlags flags)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkTrimCommandPool(&validateResult, device, commandPool, flags);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkTrimCommandPool(vkStream, device, commandPool, flags);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetDeviceQueue2 len None optional? False staticArrExpr  0) */
void validate_vkGetDeviceQueue2(
    VkResult* validateResult,
    VkDevice device,
    const VkDeviceQueueInfo2* pQueueInfo,
    VkQueue* pQueue)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetDeviceQueue2 len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetDeviceQueue2(
    VkDevice device,
    const VkDeviceQueueInfo2* pQueueInfo,
    VkQueue* pQueue)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceQueue2(&validateResult, device, pQueueInfo, pQueue);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetDeviceQueue2(vkStream, device, pQueueInfo, pQueue);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSamplerYcbcrConversion len None optional? False staticArrExpr  0) */
void validate_vkCreateSamplerYcbcrConversion(
    VkResult* validateResult,
    VkDevice device,
    const VkSamplerYcbcrConversionCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSamplerYcbcrConversion* pYcbcrConversion)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSamplerYcbcrConversion len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateSamplerYcbcrConversion(
    VkDevice device,
    const VkSamplerYcbcrConversionCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSamplerYcbcrConversion* pYcbcrConversion)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSamplerYcbcrConversion(&validateResult, device, pCreateInfo, pAllocator, pYcbcrConversion);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateSamplerYcbcrConversion(vkStream, device, pCreateInfo, pAllocator, pYcbcrConversion);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySamplerYcbcrConversion len None optional? False staticArrExpr  0) */
void validate_vkDestroySamplerYcbcrConversion(
    VkResult* validateResult,
    VkDevice device,
    VkSamplerYcbcrConversion ycbcrConversion,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySamplerYcbcrConversion len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroySamplerYcbcrConversion(
    VkDevice device,
    VkSamplerYcbcrConversion ycbcrConversion,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySamplerYcbcrConversion(&validateResult, device, ycbcrConversion, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroySamplerYcbcrConversion(vkStream, device, ycbcrConversion, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDescriptorUpdateTemplate len None optional? False staticArrExpr  0) */
void validate_vkCreateDescriptorUpdateTemplate(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorUpdateTemplateCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorUpdateTemplate* pDescriptorUpdateTemplate)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDescriptorUpdateTemplate len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateDescriptorUpdateTemplate(
    VkDevice device,
    const VkDescriptorUpdateTemplateCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorUpdateTemplate* pDescriptorUpdateTemplate)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDescriptorUpdateTemplate(&validateResult, device, pCreateInfo, pAllocator, pDescriptorUpdateTemplate);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateDescriptorUpdateTemplate(vkStream, device, pCreateInfo, pAllocator, pDescriptorUpdateTemplate);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDescriptorUpdateTemplate len None optional? False staticArrExpr  0) */
void validate_vkDestroyDescriptorUpdateTemplate(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDescriptorUpdateTemplate len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyDescriptorUpdateTemplate(
    VkDevice device,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDescriptorUpdateTemplate(&validateResult, device, descriptorUpdateTemplate, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyDescriptorUpdateTemplate(vkStream, device, descriptorUpdateTemplate, pAllocator);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkUpdateDescriptorSetWithTemplate len None optional? False staticArrExpr  0) */
void validate_vkUpdateDescriptorSetWithTemplate(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorSet descriptorSet,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const void* pData)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkUpdateDescriptorSetWithTemplate len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkUpdateDescriptorSetWithTemplate(
    VkDevice device,
    VkDescriptorSet descriptorSet,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const void* pData)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkUpdateDescriptorSetWithTemplate(&validateResult, device, descriptorSet, descriptorUpdateTemplate, pData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkUpdateDescriptorSetWithTemplate(vkStream, device, descriptorSet, descriptorUpdateTemplate, pData);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalBufferProperties len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceExternalBufferProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo,
    VkExternalBufferProperties* pExternalBufferProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalBufferProperties len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceExternalBufferProperties(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo,
    VkExternalBufferProperties* pExternalBufferProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalBufferProperties(&validateResult, physicalDevice, pExternalBufferInfo, pExternalBufferProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceExternalBufferProperties(vkStream, physicalDevice, pExternalBufferInfo, pExternalBufferProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalFenceProperties len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceExternalFenceProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo,
    VkExternalFenceProperties* pExternalFenceProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalFenceProperties len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceExternalFenceProperties(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo,
    VkExternalFenceProperties* pExternalFenceProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalFenceProperties(&validateResult, physicalDevice, pExternalFenceInfo, pExternalFenceProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceExternalFenceProperties(vkStream, physicalDevice, pExternalFenceInfo, pExternalFenceProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalSemaphoreProperties len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceExternalSemaphoreProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo,
    VkExternalSemaphoreProperties* pExternalSemaphoreProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalSemaphoreProperties len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceExternalSemaphoreProperties(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo,
    VkExternalSemaphoreProperties* pExternalSemaphoreProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalSemaphoreProperties(&validateResult, physicalDevice, pExternalSemaphoreInfo, pExternalSemaphoreProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceExternalSemaphoreProperties(vkStream, physicalDevice, pExternalSemaphoreInfo, pExternalSemaphoreProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetDescriptorSetLayoutSupport len None optional? False staticArrExpr  0) */
void validate_vkGetDescriptorSetLayoutSupport(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    VkDescriptorSetLayoutSupport* pSupport)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetDescriptorSetLayoutSupport len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetDescriptorSetLayoutSupport(
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    VkDescriptorSetLayoutSupport* pSupport)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDescriptorSetLayoutSupport(&validateResult, device, pCreateInfo, pSupport);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetDescriptorSetLayoutSupport(vkStream, device, pCreateInfo, pSupport);
}

#endif

#ifdef VK_KHR_surface

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySurfaceKHR len None optional? False staticArrExpr  0) */
void validate_vkDestroySurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    VkSurfaceKHR surface,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySurfaceKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroySurfaceKHR(
    VkInstance instance,
    VkSurfaceKHR surface,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySurfaceKHR(&validateResult, instance, surface, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroySurfaceKHR(vkStream, instance, surface, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceSupportKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceSurfaceSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    VkSurfaceKHR surface,
    VkBool32* pSupported)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceSupportKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    VkSurfaceKHR surface,
    VkBool32* pSupported)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceSupportKHR(&validateResult, physicalDevice, queueFamilyIndex, surface, pSupported);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceSupportKHR(vkStream, physicalDevice, queueFamilyIndex, surface, pSupported);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceCapabilitiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    VkSurfaceCapabilitiesKHR* pSurfaceCapabilities)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceCapabilitiesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    VkSurfaceCapabilitiesKHR* pSurfaceCapabilities)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(&validateResult, physicalDevice, surface, pSurfaceCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(vkStream, physicalDevice, surface, pSurfaceCapabilities);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceFormatsKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceSurfaceFormatsKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pSurfaceFormatCount,
    VkSurfaceFormatKHR* pSurfaceFormats)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceFormatsKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceFormatsKHR(
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pSurfaceFormatCount,
    VkSurfaceFormatKHR* pSurfaceFormats)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceFormatsKHR(&validateResult, physicalDevice, surface, pSurfaceFormatCount, pSurfaceFormats);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceFormatsKHR(vkStream, physicalDevice, surface, pSurfaceFormatCount, pSurfaceFormats);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfacePresentModesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceSurfacePresentModesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pPresentModeCount,
    VkPresentModeKHR* pPresentModes)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfacePresentModesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceSurfacePresentModesKHR(
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pPresentModeCount,
    VkPresentModeKHR* pPresentModes)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfacePresentModesKHR(&validateResult, physicalDevice, surface, pPresentModeCount, pPresentModes);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfacePresentModesKHR(vkStream, physicalDevice, surface, pPresentModeCount, pPresentModes);
    return res;
}

#endif

#ifdef VK_KHR_swapchain

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSwapchainKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateSwapchainKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkSwapchainCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSwapchainKHR* pSwapchain)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSwapchainKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateSwapchainKHR(
    VkDevice device,
    const VkSwapchainCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSwapchainKHR* pSwapchain)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSwapchainKHR(&validateResult, device, pCreateInfo, pAllocator, pSwapchain);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateSwapchainKHR(vkStream, device, pCreateInfo, pAllocator, pSwapchain);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySwapchainKHR len None optional? False staticArrExpr  0) */
void validate_vkDestroySwapchainKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySwapchainKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroySwapchainKHR(
    VkDevice device,
    VkSwapchainKHR swapchain,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySwapchainKHR(&validateResult, device, swapchain, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroySwapchainKHR(vkStream, device, swapchain, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetSwapchainImagesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetSwapchainImagesKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint32_t* pSwapchainImageCount,
    VkImage* pSwapchainImages)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetSwapchainImagesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetSwapchainImagesKHR(
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint32_t* pSwapchainImageCount,
    VkImage* pSwapchainImages)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetSwapchainImagesKHR(&validateResult, device, swapchain, pSwapchainImageCount, pSwapchainImages);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetSwapchainImagesKHR(vkStream, device, swapchain, pSwapchainImageCount, pSwapchainImages);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAcquireNextImageKHR len None optional? False staticArrExpr  0) */
void validate_vkAcquireNextImageKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint64_t timeout,
    VkSemaphore semaphore,
    VkFence fence,
    uint32_t* pImageIndex)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAcquireNextImageKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkAcquireNextImageKHR(
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint64_t timeout,
    VkSemaphore semaphore,
    VkFence fence,
    uint32_t* pImageIndex)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkAcquireNextImageKHR(&validateResult, device, swapchain, timeout, semaphore, fence, pImageIndex);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkAcquireNextImageKHR(vkStream, device, swapchain, timeout, semaphore, fence, pImageIndex);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkQueuePresentKHR len None optional? False staticArrExpr  0) */
void validate_vkQueuePresentKHR(
    VkResult* validateResult,
    VkQueue queue,
    const VkPresentInfoKHR* pPresentInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkQueuePresentKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkQueuePresentKHR(
    VkQueue queue,
    const VkPresentInfoKHR* pPresentInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueuePresentKHR(&validateResult, queue, pPresentInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkQueuePresentKHR(vkStream, queue, pPresentInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDeviceGroupPresentCapabilitiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetDeviceGroupPresentCapabilitiesKHR(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceGroupPresentCapabilitiesKHR* pDeviceGroupPresentCapabilities)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDeviceGroupPresentCapabilitiesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetDeviceGroupPresentCapabilitiesKHR(
    VkDevice device,
    VkDeviceGroupPresentCapabilitiesKHR* pDeviceGroupPresentCapabilities)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceGroupPresentCapabilitiesKHR(&validateResult, device, pDeviceGroupPresentCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetDeviceGroupPresentCapabilitiesKHR(vkStream, device, pDeviceGroupPresentCapabilities);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDeviceGroupSurfacePresentModesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetDeviceGroupSurfacePresentModesKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSurfaceKHR surface,
    VkDeviceGroupPresentModeFlagsKHR* pModes)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDeviceGroupSurfacePresentModesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetDeviceGroupSurfacePresentModesKHR(
    VkDevice device,
    VkSurfaceKHR surface,
    VkDeviceGroupPresentModeFlagsKHR* pModes)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceGroupSurfacePresentModesKHR(&validateResult, device, surface, pModes);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetDeviceGroupSurfacePresentModesKHR(vkStream, device, surface, pModes);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDevicePresentRectanglesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDevicePresentRectanglesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pRectCount,
    VkRect2D* pRects)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDevicePresentRectanglesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDevicePresentRectanglesKHR(
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pRectCount,
    VkRect2D* pRects)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDevicePresentRectanglesKHR(&validateResult, physicalDevice, surface, pRectCount, pRects);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDevicePresentRectanglesKHR(vkStream, physicalDevice, surface, pRectCount, pRects);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAcquireNextImage2KHR len None optional? False staticArrExpr  0) */
void validate_vkAcquireNextImage2KHR(
    VkResult* validateResult,
    VkDevice device,
    const VkAcquireNextImageInfoKHR* pAcquireInfo,
    uint32_t* pImageIndex)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAcquireNextImage2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkAcquireNextImage2KHR(
    VkDevice device,
    const VkAcquireNextImageInfoKHR* pAcquireInfo,
    uint32_t* pImageIndex)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkAcquireNextImage2KHR(&validateResult, device, pAcquireInfo, pImageIndex);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkAcquireNextImage2KHR(vkStream, device, pAcquireInfo, pImageIndex);
    return res;
}

#endif

#ifdef VK_KHR_display

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceDisplayPropertiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceDisplayPropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPropertiesKHR* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceDisplayPropertiesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceDisplayPropertiesKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPropertiesKHR* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceDisplayPropertiesKHR(&validateResult, physicalDevice, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceDisplayPropertiesKHR(vkStream, physicalDevice, pPropertyCount, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceDisplayPlanePropertiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceDisplayPlanePropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPlanePropertiesKHR* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceDisplayPlanePropertiesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceDisplayPlanePropertiesKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPlanePropertiesKHR* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceDisplayPlanePropertiesKHR(&validateResult, physicalDevice, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceDisplayPlanePropertiesKHR(vkStream, physicalDevice, pPropertyCount, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDisplayPlaneSupportedDisplaysKHR len None optional? False staticArrExpr  0) */
void validate_vkGetDisplayPlaneSupportedDisplaysKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t planeIndex,
    uint32_t* pDisplayCount,
    VkDisplayKHR* pDisplays)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDisplayPlaneSupportedDisplaysKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetDisplayPlaneSupportedDisplaysKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t planeIndex,
    uint32_t* pDisplayCount,
    VkDisplayKHR* pDisplays)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDisplayPlaneSupportedDisplaysKHR(&validateResult, physicalDevice, planeIndex, pDisplayCount, pDisplays);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetDisplayPlaneSupportedDisplaysKHR(vkStream, physicalDevice, planeIndex, pDisplayCount, pDisplays);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDisplayModePropertiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetDisplayModePropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    uint32_t* pPropertyCount,
    VkDisplayModePropertiesKHR* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDisplayModePropertiesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetDisplayModePropertiesKHR(
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    uint32_t* pPropertyCount,
    VkDisplayModePropertiesKHR* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDisplayModePropertiesKHR(&validateResult, physicalDevice, display, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetDisplayModePropertiesKHR(vkStream, physicalDevice, display, pPropertyCount, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDisplayModeKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateDisplayModeKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    const VkDisplayModeCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDisplayModeKHR* pMode)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDisplayModeKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateDisplayModeKHR(
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    const VkDisplayModeCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDisplayModeKHR* pMode)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDisplayModeKHR(&validateResult, physicalDevice, display, pCreateInfo, pAllocator, pMode);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateDisplayModeKHR(vkStream, physicalDevice, display, pCreateInfo, pAllocator, pMode);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDisplayPlaneCapabilitiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetDisplayPlaneCapabilitiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDisplayModeKHR mode,
    uint32_t planeIndex,
    VkDisplayPlaneCapabilitiesKHR* pCapabilities)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDisplayPlaneCapabilitiesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetDisplayPlaneCapabilitiesKHR(
    VkPhysicalDevice physicalDevice,
    VkDisplayModeKHR mode,
    uint32_t planeIndex,
    VkDisplayPlaneCapabilitiesKHR* pCapabilities)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDisplayPlaneCapabilitiesKHR(&validateResult, physicalDevice, mode, planeIndex, pCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetDisplayPlaneCapabilitiesKHR(vkStream, physicalDevice, mode, planeIndex, pCapabilities);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDisplayPlaneSurfaceKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateDisplayPlaneSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkDisplaySurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDisplayPlaneSurfaceKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateDisplayPlaneSurfaceKHR(
    VkInstance instance,
    const VkDisplaySurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDisplayPlaneSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateDisplayPlaneSurfaceKHR(vkStream, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

#endif

#ifdef VK_KHR_display_swapchain

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSharedSwapchainsKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateSharedSwapchainsKHR(
    VkResult* validateResult,
    VkDevice device,
    uint32_t swapchainCount,
    const VkSwapchainCreateInfoKHR* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkSwapchainKHR* pSwapchains)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSharedSwapchainsKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateSharedSwapchainsKHR(
    VkDevice device,
    uint32_t swapchainCount,
    const VkSwapchainCreateInfoKHR* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkSwapchainKHR* pSwapchains)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSharedSwapchainsKHR(&validateResult, device, swapchainCount, pCreateInfos, pAllocator, pSwapchains);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateSharedSwapchainsKHR(vkStream, device, swapchainCount, pCreateInfos, pAllocator, pSwapchains);
    return res;
}

#endif

#ifdef VK_KHR_xlib_surface

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateXlibSurfaceKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateXlibSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkXlibSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateXlibSurfaceKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateXlibSurfaceKHR(
    VkInstance instance,
    const VkXlibSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateXlibSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateXlibSurfaceKHR(vkStream, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

static /* VulkanType for return type: (vulkantype VkBool32 nonconst paramName vkGetPhysicalDeviceXlibPresentationSupportKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceXlibPresentationSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    Display* dpy,
    VisualID visualID)
{
}

/* VulkanType for return type: (vulkantype VkBool32 nonconst paramName vkGetPhysicalDeviceXlibPresentationSupportKHR len None optional? False staticArrExpr  0) */
VkBool32 goldfish_frontend_vkGetPhysicalDeviceXlibPresentationSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    Display* dpy,
    VisualID visualID)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceXlibPresentationSupportKHR(&validateResult, physicalDevice, queueFamilyIndex, dpy, visualID);
    if (validateResult != VK_SUCCESS)
    {
        return (VkBool32)0;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkBool32 res = encode_vkGetPhysicalDeviceXlibPresentationSupportKHR(vkStream, physicalDevice, queueFamilyIndex, dpy, visualID);
    return res;
}

#endif

#ifdef VK_KHR_xcb_surface

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateXcbSurfaceKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateXcbSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkXcbSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateXcbSurfaceKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateXcbSurfaceKHR(
    VkInstance instance,
    const VkXcbSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateXcbSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateXcbSurfaceKHR(vkStream, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

static /* VulkanType for return type: (vulkantype VkBool32 nonconst paramName vkGetPhysicalDeviceXcbPresentationSupportKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceXcbPresentationSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    xcb_connection_t* connection,
    xcb_visualid_t visual_id)
{
}

/* VulkanType for return type: (vulkantype VkBool32 nonconst paramName vkGetPhysicalDeviceXcbPresentationSupportKHR len None optional? False staticArrExpr  0) */
VkBool32 goldfish_frontend_vkGetPhysicalDeviceXcbPresentationSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    xcb_connection_t* connection,
    xcb_visualid_t visual_id)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceXcbPresentationSupportKHR(&validateResult, physicalDevice, queueFamilyIndex, connection, visual_id);
    if (validateResult != VK_SUCCESS)
    {
        return (VkBool32)0;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkBool32 res = encode_vkGetPhysicalDeviceXcbPresentationSupportKHR(vkStream, physicalDevice, queueFamilyIndex, connection, visual_id);
    return res;
}

#endif

#ifdef VK_KHR_wayland_surface

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateWaylandSurfaceKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateWaylandSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkWaylandSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateWaylandSurfaceKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateWaylandSurfaceKHR(
    VkInstance instance,
    const VkWaylandSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateWaylandSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateWaylandSurfaceKHR(vkStream, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

static /* VulkanType for return type: (vulkantype VkBool32 nonconst paramName vkGetPhysicalDeviceWaylandPresentationSupportKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceWaylandPresentationSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    struct wl_display* display)
{
}

/* VulkanType for return type: (vulkantype VkBool32 nonconst paramName vkGetPhysicalDeviceWaylandPresentationSupportKHR len None optional? False staticArrExpr  0) */
VkBool32 goldfish_frontend_vkGetPhysicalDeviceWaylandPresentationSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    struct wl_display* display)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceWaylandPresentationSupportKHR(&validateResult, physicalDevice, queueFamilyIndex, display);
    if (validateResult != VK_SUCCESS)
    {
        return (VkBool32)0;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkBool32 res = encode_vkGetPhysicalDeviceWaylandPresentationSupportKHR(vkStream, physicalDevice, queueFamilyIndex, display);
    return res;
}

#endif

#ifdef VK_KHR_mir_surface

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateMirSurfaceKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateMirSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkMirSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateMirSurfaceKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateMirSurfaceKHR(
    VkInstance instance,
    const VkMirSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateMirSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateMirSurfaceKHR(vkStream, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

static /* VulkanType for return type: (vulkantype VkBool32 nonconst paramName vkGetPhysicalDeviceMirPresentationSupportKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceMirPresentationSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    MirConnection* connection)
{
}

/* VulkanType for return type: (vulkantype VkBool32 nonconst paramName vkGetPhysicalDeviceMirPresentationSupportKHR len None optional? False staticArrExpr  0) */
VkBool32 goldfish_frontend_vkGetPhysicalDeviceMirPresentationSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    MirConnection* connection)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceMirPresentationSupportKHR(&validateResult, physicalDevice, queueFamilyIndex, connection);
    if (validateResult != VK_SUCCESS)
    {
        return (VkBool32)0;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkBool32 res = encode_vkGetPhysicalDeviceMirPresentationSupportKHR(vkStream, physicalDevice, queueFamilyIndex, connection);
    return res;
}

#endif

#ifdef VK_KHR_android_surface

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateAndroidSurfaceKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateAndroidSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkAndroidSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateAndroidSurfaceKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateAndroidSurfaceKHR(
    VkInstance instance,
    const VkAndroidSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateAndroidSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateAndroidSurfaceKHR(vkStream, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

#endif

#ifdef VK_KHR_win32_surface

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateWin32SurfaceKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateWin32SurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkWin32SurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateWin32SurfaceKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateWin32SurfaceKHR(
    VkInstance instance,
    const VkWin32SurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateWin32SurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateWin32SurfaceKHR(vkStream, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

static /* VulkanType for return type: (vulkantype VkBool32 nonconst paramName vkGetPhysicalDeviceWin32PresentationSupportKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceWin32PresentationSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex)
{
}

/* VulkanType for return type: (vulkantype VkBool32 nonconst paramName vkGetPhysicalDeviceWin32PresentationSupportKHR len None optional? False staticArrExpr  0) */
VkBool32 goldfish_frontend_vkGetPhysicalDeviceWin32PresentationSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceWin32PresentationSupportKHR(&validateResult, physicalDevice, queueFamilyIndex);
    if (validateResult != VK_SUCCESS)
    {
        return (VkBool32)0;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkBool32 res = encode_vkGetPhysicalDeviceWin32PresentationSupportKHR(vkStream, physicalDevice, queueFamilyIndex);
    return res;
}

#endif

#ifdef VK_KHR_sampler_mirror_clamp_to_edge

#endif

#ifdef VK_KHR_multiview

#endif

#ifdef VK_KHR_get_physical_device_properties2

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFeatures2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceFeatures2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures2* pFeatures)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFeatures2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceFeatures2KHR(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures2* pFeatures)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFeatures2KHR(&validateResult, physicalDevice, pFeatures);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceFeatures2KHR(vkStream, physicalDevice, pFeatures);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceProperties2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties2* pProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceProperties2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceProperties2KHR(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties2* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceProperties2KHR(&validateResult, physicalDevice, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceProperties2KHR(vkStream, physicalDevice, pProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFormatProperties2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceFormatProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties2* pFormatProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceFormatProperties2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceFormatProperties2KHR(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties2* pFormatProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFormatProperties2KHR(&validateResult, physicalDevice, format, pFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceFormatProperties2KHR(vkStream, physicalDevice, format, pFormatProperties);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceImageFormatProperties2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceImageFormatProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo,
    VkImageFormatProperties2* pImageFormatProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceImageFormatProperties2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceImageFormatProperties2KHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo,
    VkImageFormatProperties2* pImageFormatProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceImageFormatProperties2KHR(&validateResult, physicalDevice, pImageFormatInfo, pImageFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceImageFormatProperties2KHR(vkStream, physicalDevice, pImageFormatInfo, pImageFormatProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceQueueFamilyProperties2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceQueueFamilyProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties2* pQueueFamilyProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceQueueFamilyProperties2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceQueueFamilyProperties2KHR(
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties2* pQueueFamilyProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceQueueFamilyProperties2KHR(&validateResult, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceQueueFamilyProperties2KHR(vkStream, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceMemoryProperties2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceMemoryProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties2* pMemoryProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceMemoryProperties2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceMemoryProperties2KHR(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties2* pMemoryProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceMemoryProperties2KHR(&validateResult, physicalDevice, pMemoryProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceMemoryProperties2KHR(vkStream, physicalDevice, pMemoryProperties);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceSparseImageFormatProperties2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceSparseImageFormatProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties2* pProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceSparseImageFormatProperties2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceSparseImageFormatProperties2KHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties2* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSparseImageFormatProperties2KHR(&validateResult, physicalDevice, pFormatInfo, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceSparseImageFormatProperties2KHR(vkStream, physicalDevice, pFormatInfo, pPropertyCount, pProperties);
}

#endif

#ifdef VK_KHR_device_group

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetDeviceGroupPeerMemoryFeaturesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetDeviceGroupPeerMemoryFeaturesKHR(
    VkResult* validateResult,
    VkDevice device,
    uint32_t heapIndex,
    uint32_t localDeviceIndex,
    uint32_t remoteDeviceIndex,
    VkPeerMemoryFeatureFlags* pPeerMemoryFeatures)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetDeviceGroupPeerMemoryFeaturesKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetDeviceGroupPeerMemoryFeaturesKHR(
    VkDevice device,
    uint32_t heapIndex,
    uint32_t localDeviceIndex,
    uint32_t remoteDeviceIndex,
    VkPeerMemoryFeatureFlags* pPeerMemoryFeatures)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceGroupPeerMemoryFeaturesKHR(&validateResult, device, heapIndex, localDeviceIndex, remoteDeviceIndex, pPeerMemoryFeatures);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetDeviceGroupPeerMemoryFeaturesKHR(vkStream, device, heapIndex, localDeviceIndex, remoteDeviceIndex, pPeerMemoryFeatures);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetDeviceMaskKHR len None optional? False staticArrExpr  0) */
void validate_vkCmdSetDeviceMaskKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t deviceMask)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetDeviceMaskKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetDeviceMaskKHR(
    VkCommandBuffer commandBuffer,
    uint32_t deviceMask)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetDeviceMaskKHR(&validateResult, commandBuffer, deviceMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetDeviceMaskKHR(vkStream, commandBuffer, deviceMask);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDispatchBaseKHR len None optional? False staticArrExpr  0) */
void validate_vkCmdDispatchBaseKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t baseGroupX,
    uint32_t baseGroupY,
    uint32_t baseGroupZ,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDispatchBaseKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDispatchBaseKHR(
    VkCommandBuffer commandBuffer,
    uint32_t baseGroupX,
    uint32_t baseGroupY,
    uint32_t baseGroupZ,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDispatchBaseKHR(&validateResult, commandBuffer, baseGroupX, baseGroupY, baseGroupZ, groupCountX, groupCountY, groupCountZ);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDispatchBaseKHR(vkStream, commandBuffer, baseGroupX, baseGroupY, baseGroupZ, groupCountX, groupCountY, groupCountZ);
}

#endif

#ifdef VK_KHR_shader_draw_parameters

#endif

#ifdef VK_KHR_maintenance1

static /* VulkanType for return type: (vulkantype void nonconst paramName vkTrimCommandPoolKHR len None optional? False staticArrExpr  0) */
void validate_vkTrimCommandPoolKHR(
    VkResult* validateResult,
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolTrimFlags flags)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkTrimCommandPoolKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkTrimCommandPoolKHR(
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolTrimFlags flags)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkTrimCommandPoolKHR(&validateResult, device, commandPool, flags);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkTrimCommandPoolKHR(vkStream, device, commandPool, flags);
}

#endif

#ifdef VK_KHR_device_group_creation

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumeratePhysicalDeviceGroupsKHR len None optional? False staticArrExpr  0) */
void validate_vkEnumeratePhysicalDeviceGroupsKHR(
    VkResult* validateResult,
    VkInstance instance,
    uint32_t* pPhysicalDeviceGroupCount,
    VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkEnumeratePhysicalDeviceGroupsKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkEnumeratePhysicalDeviceGroupsKHR(
    VkInstance instance,
    uint32_t* pPhysicalDeviceGroupCount,
    VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumeratePhysicalDeviceGroupsKHR(&validateResult, instance, pPhysicalDeviceGroupCount, pPhysicalDeviceGroupProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkEnumeratePhysicalDeviceGroupsKHR(vkStream, instance, pPhysicalDeviceGroupCount, pPhysicalDeviceGroupProperties);
    return res;
}

#endif

#ifdef VK_KHR_external_memory_capabilities

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalBufferPropertiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceExternalBufferPropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo,
    VkExternalBufferProperties* pExternalBufferProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalBufferPropertiesKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceExternalBufferPropertiesKHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo,
    VkExternalBufferProperties* pExternalBufferProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalBufferPropertiesKHR(&validateResult, physicalDevice, pExternalBufferInfo, pExternalBufferProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceExternalBufferPropertiesKHR(vkStream, physicalDevice, pExternalBufferInfo, pExternalBufferProperties);
}

#endif

#ifdef VK_KHR_external_memory

#endif

#ifdef VK_KHR_external_memory_win32

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryWin32HandleKHR len None optional? False staticArrExpr  0) */
void validate_vkGetMemoryWin32HandleKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkMemoryGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryWin32HandleKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetMemoryWin32HandleKHR(
    VkDevice device,
    const VkMemoryGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryWin32HandleKHR(&validateResult, device, pGetWin32HandleInfo, pHandle);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetMemoryWin32HandleKHR(vkStream, device, pGetWin32HandleInfo, pHandle);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryWin32HandlePropertiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetMemoryWin32HandlePropertiesKHR(
    VkResult* validateResult,
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    HANDLE handle,
    VkMemoryWin32HandlePropertiesKHR* pMemoryWin32HandleProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryWin32HandlePropertiesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetMemoryWin32HandlePropertiesKHR(
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    HANDLE handle,
    VkMemoryWin32HandlePropertiesKHR* pMemoryWin32HandleProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryWin32HandlePropertiesKHR(&validateResult, device, handleType, handle, pMemoryWin32HandleProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetMemoryWin32HandlePropertiesKHR(vkStream, device, handleType, handle, pMemoryWin32HandleProperties);
    return res;
}

#endif

#ifdef VK_KHR_external_memory_fd

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryFdKHR len None optional? False staticArrExpr  0) */
void validate_vkGetMemoryFdKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkMemoryGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryFdKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetMemoryFdKHR(
    VkDevice device,
    const VkMemoryGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryFdKHR(&validateResult, device, pGetFdInfo, pFd);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetMemoryFdKHR(vkStream, device, pGetFdInfo, pFd);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryFdPropertiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetMemoryFdPropertiesKHR(
    VkResult* validateResult,
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    int fd,
    VkMemoryFdPropertiesKHR* pMemoryFdProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryFdPropertiesKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetMemoryFdPropertiesKHR(
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    int fd,
    VkMemoryFdPropertiesKHR* pMemoryFdProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryFdPropertiesKHR(&validateResult, device, handleType, fd, pMemoryFdProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetMemoryFdPropertiesKHR(vkStream, device, handleType, fd, pMemoryFdProperties);
    return res;
}

#endif

#ifdef VK_KHR_win32_keyed_mutex

#endif

#ifdef VK_KHR_external_semaphore_capabilities

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalSemaphorePropertiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceExternalSemaphorePropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo,
    VkExternalSemaphoreProperties* pExternalSemaphoreProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalSemaphorePropertiesKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceExternalSemaphorePropertiesKHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo,
    VkExternalSemaphoreProperties* pExternalSemaphoreProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalSemaphorePropertiesKHR(&validateResult, physicalDevice, pExternalSemaphoreInfo, pExternalSemaphoreProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceExternalSemaphorePropertiesKHR(vkStream, physicalDevice, pExternalSemaphoreInfo, pExternalSemaphoreProperties);
}

#endif

#ifdef VK_KHR_external_semaphore

#endif

#ifdef VK_KHR_external_semaphore_win32

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkImportSemaphoreWin32HandleKHR len None optional? False staticArrExpr  0) */
void validate_vkImportSemaphoreWin32HandleKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImportSemaphoreWin32HandleInfoKHR* pImportSemaphoreWin32HandleInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkImportSemaphoreWin32HandleKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkImportSemaphoreWin32HandleKHR(
    VkDevice device,
    const VkImportSemaphoreWin32HandleInfoKHR* pImportSemaphoreWin32HandleInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkImportSemaphoreWin32HandleKHR(&validateResult, device, pImportSemaphoreWin32HandleInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkImportSemaphoreWin32HandleKHR(vkStream, device, pImportSemaphoreWin32HandleInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetSemaphoreWin32HandleKHR len None optional? False staticArrExpr  0) */
void validate_vkGetSemaphoreWin32HandleKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkSemaphoreGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetSemaphoreWin32HandleKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetSemaphoreWin32HandleKHR(
    VkDevice device,
    const VkSemaphoreGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetSemaphoreWin32HandleKHR(&validateResult, device, pGetWin32HandleInfo, pHandle);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetSemaphoreWin32HandleKHR(vkStream, device, pGetWin32HandleInfo, pHandle);
    return res;
}

#endif

#ifdef VK_KHR_external_semaphore_fd

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkImportSemaphoreFdKHR len None optional? False staticArrExpr  0) */
void validate_vkImportSemaphoreFdKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImportSemaphoreFdInfoKHR* pImportSemaphoreFdInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkImportSemaphoreFdKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkImportSemaphoreFdKHR(
    VkDevice device,
    const VkImportSemaphoreFdInfoKHR* pImportSemaphoreFdInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkImportSemaphoreFdKHR(&validateResult, device, pImportSemaphoreFdInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkImportSemaphoreFdKHR(vkStream, device, pImportSemaphoreFdInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetSemaphoreFdKHR len None optional? False staticArrExpr  0) */
void validate_vkGetSemaphoreFdKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkSemaphoreGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetSemaphoreFdKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetSemaphoreFdKHR(
    VkDevice device,
    const VkSemaphoreGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetSemaphoreFdKHR(&validateResult, device, pGetFdInfo, pFd);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetSemaphoreFdKHR(vkStream, device, pGetFdInfo, pFd);
    return res;
}

#endif

#ifdef VK_KHR_push_descriptor

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdPushDescriptorSetKHR len None optional? False staticArrExpr  0) */
void validate_vkCmdPushDescriptorSetKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipelineLayout layout,
    uint32_t set,
    uint32_t descriptorWriteCount,
    const VkWriteDescriptorSet* pDescriptorWrites)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdPushDescriptorSetKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdPushDescriptorSetKHR(
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipelineLayout layout,
    uint32_t set,
    uint32_t descriptorWriteCount,
    const VkWriteDescriptorSet* pDescriptorWrites)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdPushDescriptorSetKHR(&validateResult, commandBuffer, pipelineBindPoint, layout, set, descriptorWriteCount, pDescriptorWrites);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdPushDescriptorSetKHR(vkStream, commandBuffer, pipelineBindPoint, layout, set, descriptorWriteCount, pDescriptorWrites);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdPushDescriptorSetWithTemplateKHR len None optional? False staticArrExpr  0) */
void validate_vkCmdPushDescriptorSetWithTemplateKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    VkPipelineLayout layout,
    uint32_t set,
    const void* pData)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdPushDescriptorSetWithTemplateKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdPushDescriptorSetWithTemplateKHR(
    VkCommandBuffer commandBuffer,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    VkPipelineLayout layout,
    uint32_t set,
    const void* pData)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdPushDescriptorSetWithTemplateKHR(&validateResult, commandBuffer, descriptorUpdateTemplate, layout, set, pData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdPushDescriptorSetWithTemplateKHR(vkStream, commandBuffer, descriptorUpdateTemplate, layout, set, pData);
}

#endif

#ifdef VK_KHR_16bit_storage

#endif

#ifdef VK_KHR_incremental_present

#endif

#ifdef VK_KHR_descriptor_update_template

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDescriptorUpdateTemplateKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateDescriptorUpdateTemplateKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorUpdateTemplateCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorUpdateTemplate* pDescriptorUpdateTemplate)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDescriptorUpdateTemplateKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateDescriptorUpdateTemplateKHR(
    VkDevice device,
    const VkDescriptorUpdateTemplateCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorUpdateTemplate* pDescriptorUpdateTemplate)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDescriptorUpdateTemplateKHR(&validateResult, device, pCreateInfo, pAllocator, pDescriptorUpdateTemplate);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateDescriptorUpdateTemplateKHR(vkStream, device, pCreateInfo, pAllocator, pDescriptorUpdateTemplate);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDescriptorUpdateTemplateKHR len None optional? False staticArrExpr  0) */
void validate_vkDestroyDescriptorUpdateTemplateKHR(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDescriptorUpdateTemplateKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyDescriptorUpdateTemplateKHR(
    VkDevice device,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDescriptorUpdateTemplateKHR(&validateResult, device, descriptorUpdateTemplate, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyDescriptorUpdateTemplateKHR(vkStream, device, descriptorUpdateTemplate, pAllocator);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkUpdateDescriptorSetWithTemplateKHR len None optional? False staticArrExpr  0) */
void validate_vkUpdateDescriptorSetWithTemplateKHR(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorSet descriptorSet,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const void* pData)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkUpdateDescriptorSetWithTemplateKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkUpdateDescriptorSetWithTemplateKHR(
    VkDevice device,
    VkDescriptorSet descriptorSet,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const void* pData)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkUpdateDescriptorSetWithTemplateKHR(&validateResult, device, descriptorSet, descriptorUpdateTemplate, pData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkUpdateDescriptorSetWithTemplateKHR(vkStream, device, descriptorSet, descriptorUpdateTemplate, pData);
}

#endif

#ifdef VK_KHR_create_renderpass2

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateRenderPass2KHR len None optional? False staticArrExpr  0) */
void validate_vkCreateRenderPass2KHR(
    VkResult* validateResult,
    VkDevice device,
    const VkRenderPassCreateInfo2KHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkRenderPass* pRenderPass)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateRenderPass2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateRenderPass2KHR(
    VkDevice device,
    const VkRenderPassCreateInfo2KHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkRenderPass* pRenderPass)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateRenderPass2KHR(&validateResult, device, pCreateInfo, pAllocator, pRenderPass);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateRenderPass2KHR(vkStream, device, pCreateInfo, pAllocator, pRenderPass);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBeginRenderPass2KHR len None optional? False staticArrExpr  0) */
void validate_vkCmdBeginRenderPass2KHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkRenderPassBeginInfo*      pRenderPassBegin,
    const VkSubpassBeginInfoKHR*      pSubpassBeginInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBeginRenderPass2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdBeginRenderPass2KHR(
    VkCommandBuffer commandBuffer,
    const VkRenderPassBeginInfo*      pRenderPassBegin,
    const VkSubpassBeginInfoKHR*      pSubpassBeginInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBeginRenderPass2KHR(&validateResult, commandBuffer, pRenderPassBegin, pSubpassBeginInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdBeginRenderPass2KHR(vkStream, commandBuffer, pRenderPassBegin, pSubpassBeginInfo);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdNextSubpass2KHR len None optional? False staticArrExpr  0) */
void validate_vkCmdNextSubpass2KHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkSubpassBeginInfoKHR*      pSubpassBeginInfo,
    const VkSubpassEndInfoKHR*        pSubpassEndInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdNextSubpass2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdNextSubpass2KHR(
    VkCommandBuffer commandBuffer,
    const VkSubpassBeginInfoKHR*      pSubpassBeginInfo,
    const VkSubpassEndInfoKHR*        pSubpassEndInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdNextSubpass2KHR(&validateResult, commandBuffer, pSubpassBeginInfo, pSubpassEndInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdNextSubpass2KHR(vkStream, commandBuffer, pSubpassBeginInfo, pSubpassEndInfo);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdEndRenderPass2KHR len None optional? False staticArrExpr  0) */
void validate_vkCmdEndRenderPass2KHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkSubpassEndInfoKHR*        pSubpassEndInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdEndRenderPass2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdEndRenderPass2KHR(
    VkCommandBuffer commandBuffer,
    const VkSubpassEndInfoKHR*        pSubpassEndInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdEndRenderPass2KHR(&validateResult, commandBuffer, pSubpassEndInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdEndRenderPass2KHR(vkStream, commandBuffer, pSubpassEndInfo);
}

#endif

#ifdef VK_KHR_shared_presentable_image

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetSwapchainStatusKHR len None optional? False staticArrExpr  0) */
void validate_vkGetSwapchainStatusKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetSwapchainStatusKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetSwapchainStatusKHR(
    VkDevice device,
    VkSwapchainKHR swapchain)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetSwapchainStatusKHR(&validateResult, device, swapchain);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetSwapchainStatusKHR(vkStream, device, swapchain);
    return res;
}

#endif

#ifdef VK_KHR_external_fence_capabilities

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalFencePropertiesKHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceExternalFencePropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo,
    VkExternalFenceProperties* pExternalFenceProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceExternalFencePropertiesKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceExternalFencePropertiesKHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo,
    VkExternalFenceProperties* pExternalFenceProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalFencePropertiesKHR(&validateResult, physicalDevice, pExternalFenceInfo, pExternalFenceProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceExternalFencePropertiesKHR(vkStream, physicalDevice, pExternalFenceInfo, pExternalFenceProperties);
}

#endif

#ifdef VK_KHR_external_fence

#endif

#ifdef VK_KHR_external_fence_win32

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkImportFenceWin32HandleKHR len None optional? False staticArrExpr  0) */
void validate_vkImportFenceWin32HandleKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImportFenceWin32HandleInfoKHR* pImportFenceWin32HandleInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkImportFenceWin32HandleKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkImportFenceWin32HandleKHR(
    VkDevice device,
    const VkImportFenceWin32HandleInfoKHR* pImportFenceWin32HandleInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkImportFenceWin32HandleKHR(&validateResult, device, pImportFenceWin32HandleInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkImportFenceWin32HandleKHR(vkStream, device, pImportFenceWin32HandleInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetFenceWin32HandleKHR len None optional? False staticArrExpr  0) */
void validate_vkGetFenceWin32HandleKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkFenceGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetFenceWin32HandleKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetFenceWin32HandleKHR(
    VkDevice device,
    const VkFenceGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetFenceWin32HandleKHR(&validateResult, device, pGetWin32HandleInfo, pHandle);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetFenceWin32HandleKHR(vkStream, device, pGetWin32HandleInfo, pHandle);
    return res;
}

#endif

#ifdef VK_KHR_external_fence_fd

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkImportFenceFdKHR len None optional? False staticArrExpr  0) */
void validate_vkImportFenceFdKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImportFenceFdInfoKHR* pImportFenceFdInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkImportFenceFdKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkImportFenceFdKHR(
    VkDevice device,
    const VkImportFenceFdInfoKHR* pImportFenceFdInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkImportFenceFdKHR(&validateResult, device, pImportFenceFdInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkImportFenceFdKHR(vkStream, device, pImportFenceFdInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetFenceFdKHR len None optional? False staticArrExpr  0) */
void validate_vkGetFenceFdKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkFenceGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetFenceFdKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetFenceFdKHR(
    VkDevice device,
    const VkFenceGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetFenceFdKHR(&validateResult, device, pGetFdInfo, pFd);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetFenceFdKHR(vkStream, device, pGetFdInfo, pFd);
    return res;
}

#endif

#ifdef VK_KHR_maintenance2

#endif

#ifdef VK_KHR_get_surface_capabilities2

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceCapabilities2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceSurfaceCapabilities2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSurfaceInfo2KHR* pSurfaceInfo,
    VkSurfaceCapabilities2KHR* pSurfaceCapabilities)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceCapabilities2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceCapabilities2KHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSurfaceInfo2KHR* pSurfaceInfo,
    VkSurfaceCapabilities2KHR* pSurfaceCapabilities)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceCapabilities2KHR(&validateResult, physicalDevice, pSurfaceInfo, pSurfaceCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceCapabilities2KHR(vkStream, physicalDevice, pSurfaceInfo, pSurfaceCapabilities);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceFormats2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceSurfaceFormats2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSurfaceInfo2KHR* pSurfaceInfo,
    uint32_t* pSurfaceFormatCount,
    VkSurfaceFormat2KHR* pSurfaceFormats)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceFormats2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceFormats2KHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSurfaceInfo2KHR* pSurfaceInfo,
    uint32_t* pSurfaceFormatCount,
    VkSurfaceFormat2KHR* pSurfaceFormats)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceFormats2KHR(&validateResult, physicalDevice, pSurfaceInfo, pSurfaceFormatCount, pSurfaceFormats);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceFormats2KHR(vkStream, physicalDevice, pSurfaceInfo, pSurfaceFormatCount, pSurfaceFormats);
    return res;
}

#endif

#ifdef VK_KHR_variable_pointers

#endif

#ifdef VK_KHR_get_display_properties2

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceDisplayProperties2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceDisplayProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayProperties2KHR* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceDisplayProperties2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceDisplayProperties2KHR(
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayProperties2KHR* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceDisplayProperties2KHR(&validateResult, physicalDevice, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceDisplayProperties2KHR(vkStream, physicalDevice, pPropertyCount, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceDisplayPlaneProperties2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceDisplayPlaneProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPlaneProperties2KHR* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceDisplayPlaneProperties2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceDisplayPlaneProperties2KHR(
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPlaneProperties2KHR* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceDisplayPlaneProperties2KHR(&validateResult, physicalDevice, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceDisplayPlaneProperties2KHR(vkStream, physicalDevice, pPropertyCount, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDisplayModeProperties2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetDisplayModeProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    uint32_t* pPropertyCount,
    VkDisplayModeProperties2KHR* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDisplayModeProperties2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetDisplayModeProperties2KHR(
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    uint32_t* pPropertyCount,
    VkDisplayModeProperties2KHR* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDisplayModeProperties2KHR(&validateResult, physicalDevice, display, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetDisplayModeProperties2KHR(vkStream, physicalDevice, display, pPropertyCount, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDisplayPlaneCapabilities2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetDisplayPlaneCapabilities2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkDisplayPlaneInfo2KHR* pDisplayPlaneInfo,
    VkDisplayPlaneCapabilities2KHR* pCapabilities)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetDisplayPlaneCapabilities2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetDisplayPlaneCapabilities2KHR(
    VkPhysicalDevice physicalDevice,
    const VkDisplayPlaneInfo2KHR* pDisplayPlaneInfo,
    VkDisplayPlaneCapabilities2KHR* pCapabilities)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDisplayPlaneCapabilities2KHR(&validateResult, physicalDevice, pDisplayPlaneInfo, pCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetDisplayPlaneCapabilities2KHR(vkStream, physicalDevice, pDisplayPlaneInfo, pCapabilities);
    return res;
}

#endif

#ifdef VK_KHR_dedicated_allocation

#endif

#ifdef VK_KHR_storage_buffer_storage_class

#endif

#ifdef VK_KHR_relaxed_block_layout

#endif

#ifdef VK_KHR_get_memory_requirements2

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageMemoryRequirements2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetImageMemoryRequirements2KHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImageMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageMemoryRequirements2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetImageMemoryRequirements2KHR(
    VkDevice device,
    const VkImageMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageMemoryRequirements2KHR(&validateResult, device, pInfo, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetImageMemoryRequirements2KHR(vkStream, device, pInfo, pMemoryRequirements);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetBufferMemoryRequirements2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetBufferMemoryRequirements2KHR(
    VkResult* validateResult,
    VkDevice device,
    const VkBufferMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetBufferMemoryRequirements2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetBufferMemoryRequirements2KHR(
    VkDevice device,
    const VkBufferMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetBufferMemoryRequirements2KHR(&validateResult, device, pInfo, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetBufferMemoryRequirements2KHR(vkStream, device, pInfo, pMemoryRequirements);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageSparseMemoryRequirements2KHR len None optional? False staticArrExpr  0) */
void validate_vkGetImageSparseMemoryRequirements2KHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImageSparseMemoryRequirementsInfo2* pInfo,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements2* pSparseMemoryRequirements)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetImageSparseMemoryRequirements2KHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetImageSparseMemoryRequirements2KHR(
    VkDevice device,
    const VkImageSparseMemoryRequirementsInfo2* pInfo,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements2* pSparseMemoryRequirements)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageSparseMemoryRequirements2KHR(&validateResult, device, pInfo, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetImageSparseMemoryRequirements2KHR(vkStream, device, pInfo, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
}

#endif

#ifdef VK_KHR_image_format_list

#endif

#ifdef VK_KHR_sampler_ycbcr_conversion

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSamplerYcbcrConversionKHR len None optional? False staticArrExpr  0) */
void validate_vkCreateSamplerYcbcrConversionKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkSamplerYcbcrConversionCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSamplerYcbcrConversion* pYcbcrConversion)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateSamplerYcbcrConversionKHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateSamplerYcbcrConversionKHR(
    VkDevice device,
    const VkSamplerYcbcrConversionCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSamplerYcbcrConversion* pYcbcrConversion)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSamplerYcbcrConversionKHR(&validateResult, device, pCreateInfo, pAllocator, pYcbcrConversion);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateSamplerYcbcrConversionKHR(vkStream, device, pCreateInfo, pAllocator, pYcbcrConversion);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySamplerYcbcrConversionKHR len None optional? False staticArrExpr  0) */
void validate_vkDestroySamplerYcbcrConversionKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSamplerYcbcrConversion ycbcrConversion,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroySamplerYcbcrConversionKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroySamplerYcbcrConversionKHR(
    VkDevice device,
    VkSamplerYcbcrConversion ycbcrConversion,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySamplerYcbcrConversionKHR(&validateResult, device, ycbcrConversion, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroySamplerYcbcrConversionKHR(vkStream, device, ycbcrConversion, pAllocator);
}

#endif

#ifdef VK_KHR_bind_memory2

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindBufferMemory2KHR len None optional? False staticArrExpr  0) */
void validate_vkBindBufferMemory2KHR(
    VkResult* validateResult,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindBufferMemoryInfo* pBindInfos)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindBufferMemory2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkBindBufferMemory2KHR(
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindBufferMemoryInfo* pBindInfos)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindBufferMemory2KHR(&validateResult, device, bindInfoCount, pBindInfos);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkBindBufferMemory2KHR(vkStream, device, bindInfoCount, pBindInfos);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindImageMemory2KHR len None optional? False staticArrExpr  0) */
void validate_vkBindImageMemory2KHR(
    VkResult* validateResult,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindImageMemoryInfo* pBindInfos)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkBindImageMemory2KHR len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkBindImageMemory2KHR(
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindImageMemoryInfo* pBindInfos)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindImageMemory2KHR(&validateResult, device, bindInfoCount, pBindInfos);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkBindImageMemory2KHR(vkStream, device, bindInfoCount, pBindInfos);
    return res;
}

#endif

#ifdef VK_KHR_maintenance3

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetDescriptorSetLayoutSupportKHR len None optional? False staticArrExpr  0) */
void validate_vkGetDescriptorSetLayoutSupportKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    VkDescriptorSetLayoutSupport* pSupport)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetDescriptorSetLayoutSupportKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetDescriptorSetLayoutSupportKHR(
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    VkDescriptorSetLayoutSupport* pSupport)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDescriptorSetLayoutSupportKHR(&validateResult, device, pCreateInfo, pSupport);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetDescriptorSetLayoutSupportKHR(vkStream, device, pCreateInfo, pSupport);
}

#endif

#ifdef VK_KHR_draw_indirect_count

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndirectCountKHR len None optional? False staticArrExpr  0) */
void validate_vkCmdDrawIndirectCountKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndirectCountKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDrawIndirectCountKHR(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndirectCountKHR(&validateResult, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDrawIndirectCountKHR(vkStream, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndexedIndirectCountKHR len None optional? False staticArrExpr  0) */
void validate_vkCmdDrawIndexedIndirectCountKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndexedIndirectCountKHR len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDrawIndexedIndirectCountKHR(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndexedIndirectCountKHR(&validateResult, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDrawIndexedIndirectCountKHR(vkStream, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
}

#endif

#ifdef VK_KHR_8bit_storage

#endif

#ifdef VK_EXT_debug_report

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDebugReportCallbackEXT len None optional? False staticArrExpr  0) */
void validate_vkCreateDebugReportCallbackEXT(
    VkResult* validateResult,
    VkInstance instance,
    const VkDebugReportCallbackCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDebugReportCallbackEXT* pCallback)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDebugReportCallbackEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateDebugReportCallbackEXT(
    VkInstance instance,
    const VkDebugReportCallbackCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDebugReportCallbackEXT* pCallback)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDebugReportCallbackEXT(&validateResult, instance, pCreateInfo, pAllocator, pCallback);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateDebugReportCallbackEXT(vkStream, instance, pCreateInfo, pAllocator, pCallback);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDebugReportCallbackEXT len None optional? False staticArrExpr  0) */
void validate_vkDestroyDebugReportCallbackEXT(
    VkResult* validateResult,
    VkInstance instance,
    VkDebugReportCallbackEXT callback,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDebugReportCallbackEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyDebugReportCallbackEXT(
    VkInstance instance,
    VkDebugReportCallbackEXT callback,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDebugReportCallbackEXT(&validateResult, instance, callback, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyDebugReportCallbackEXT(vkStream, instance, callback, pAllocator);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDebugReportMessageEXT len None optional? False staticArrExpr  0) */
void validate_vkDebugReportMessageEXT(
    VkResult* validateResult,
    VkInstance instance,
    VkDebugReportFlagsEXT flags,
    VkDebugReportObjectTypeEXT objectType,
    uint64_t object,
    size_t location,
    int32_t messageCode,
    const char* pLayerPrefix,
    const char* pMessage)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDebugReportMessageEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDebugReportMessageEXT(
    VkInstance instance,
    VkDebugReportFlagsEXT flags,
    VkDebugReportObjectTypeEXT objectType,
    uint64_t object,
    size_t location,
    int32_t messageCode,
    const char* pLayerPrefix,
    const char* pMessage)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDebugReportMessageEXT(&validateResult, instance, flags, objectType, object, location, messageCode, pLayerPrefix, pMessage);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDebugReportMessageEXT(vkStream, instance, flags, objectType, object, location, messageCode, pLayerPrefix, pMessage);
}

#endif

#ifdef VK_NV_glsl_shader

#endif

#ifdef VK_EXT_depth_range_unrestricted

#endif

#ifdef VK_IMG_filter_cubic

#endif

#ifdef VK_AMD_rasterization_order

#endif

#ifdef VK_AMD_shader_trinary_minmax

#endif

#ifdef VK_AMD_shader_explicit_vertex_parameter

#endif

#ifdef VK_EXT_debug_marker

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkDebugMarkerSetObjectTagEXT len None optional? False staticArrExpr  0) */
void validate_vkDebugMarkerSetObjectTagEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkDebugMarkerObjectTagInfoEXT* pTagInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkDebugMarkerSetObjectTagEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkDebugMarkerSetObjectTagEXT(
    VkDevice device,
    const VkDebugMarkerObjectTagInfoEXT* pTagInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDebugMarkerSetObjectTagEXT(&validateResult, device, pTagInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkDebugMarkerSetObjectTagEXT(vkStream, device, pTagInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkDebugMarkerSetObjectNameEXT len None optional? False staticArrExpr  0) */
void validate_vkDebugMarkerSetObjectNameEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkDebugMarkerObjectNameInfoEXT* pNameInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkDebugMarkerSetObjectNameEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkDebugMarkerSetObjectNameEXT(
    VkDevice device,
    const VkDebugMarkerObjectNameInfoEXT* pNameInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDebugMarkerSetObjectNameEXT(&validateResult, device, pNameInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkDebugMarkerSetObjectNameEXT(vkStream, device, pNameInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDebugMarkerBeginEXT len None optional? False staticArrExpr  0) */
void validate_vkCmdDebugMarkerBeginEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkDebugMarkerMarkerInfoEXT* pMarkerInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDebugMarkerBeginEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDebugMarkerBeginEXT(
    VkCommandBuffer commandBuffer,
    const VkDebugMarkerMarkerInfoEXT* pMarkerInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDebugMarkerBeginEXT(&validateResult, commandBuffer, pMarkerInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDebugMarkerBeginEXT(vkStream, commandBuffer, pMarkerInfo);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDebugMarkerEndEXT len None optional? False staticArrExpr  0) */
void validate_vkCmdDebugMarkerEndEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDebugMarkerEndEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDebugMarkerEndEXT(
    VkCommandBuffer commandBuffer)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDebugMarkerEndEXT(&validateResult, commandBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDebugMarkerEndEXT(vkStream, commandBuffer);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDebugMarkerInsertEXT len None optional? False staticArrExpr  0) */
void validate_vkCmdDebugMarkerInsertEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkDebugMarkerMarkerInfoEXT* pMarkerInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDebugMarkerInsertEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDebugMarkerInsertEXT(
    VkCommandBuffer commandBuffer,
    const VkDebugMarkerMarkerInfoEXT* pMarkerInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDebugMarkerInsertEXT(&validateResult, commandBuffer, pMarkerInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDebugMarkerInsertEXT(vkStream, commandBuffer, pMarkerInfo);
}

#endif

#ifdef VK_AMD_gcn_shader

#endif

#ifdef VK_NV_dedicated_allocation

#endif

#ifdef VK_AMD_draw_indirect_count

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndirectCountAMD len None optional? False staticArrExpr  0) */
void validate_vkCmdDrawIndirectCountAMD(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndirectCountAMD len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDrawIndirectCountAMD(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndirectCountAMD(&validateResult, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDrawIndirectCountAMD(vkStream, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndexedIndirectCountAMD len None optional? False staticArrExpr  0) */
void validate_vkCmdDrawIndexedIndirectCountAMD(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdDrawIndexedIndirectCountAMD len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdDrawIndexedIndirectCountAMD(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndexedIndirectCountAMD(&validateResult, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdDrawIndexedIndirectCountAMD(vkStream, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
}

#endif

#ifdef VK_AMD_negative_viewport_height

#endif

#ifdef VK_AMD_gpu_shader_half_float

#endif

#ifdef VK_AMD_shader_ballot

#endif

#ifdef VK_AMD_texture_gather_bias_lod

#endif

#ifdef VK_AMD_shader_info

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetShaderInfoAMD len None optional? False staticArrExpr  0) */
void validate_vkGetShaderInfoAMD(
    VkResult* validateResult,
    VkDevice device,
    VkPipeline pipeline,
    VkShaderStageFlagBits shaderStage,
    VkShaderInfoTypeAMD infoType,
    size_t* pInfoSize,
    void* pInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetShaderInfoAMD len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetShaderInfoAMD(
    VkDevice device,
    VkPipeline pipeline,
    VkShaderStageFlagBits shaderStage,
    VkShaderInfoTypeAMD infoType,
    size_t* pInfoSize,
    void* pInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetShaderInfoAMD(&validateResult, device, pipeline, shaderStage, infoType, pInfoSize, pInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetShaderInfoAMD(vkStream, device, pipeline, shaderStage, infoType, pInfoSize, pInfo);
    return res;
}

#endif

#ifdef VK_AMD_shader_image_load_store_lod

#endif

#ifdef VK_IMG_format_pvrtc

#endif

#ifdef VK_NV_external_memory_capabilities

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceExternalImageFormatPropertiesNV len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceExternalImageFormatPropertiesNV(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkImageTiling tiling,
    VkImageUsageFlags usage,
    VkImageCreateFlags flags,
    VkExternalMemoryHandleTypeFlagsNV externalHandleType,
    VkExternalImageFormatPropertiesNV* pExternalImageFormatProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceExternalImageFormatPropertiesNV len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceExternalImageFormatPropertiesNV(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkImageTiling tiling,
    VkImageUsageFlags usage,
    VkImageCreateFlags flags,
    VkExternalMemoryHandleTypeFlagsNV externalHandleType,
    VkExternalImageFormatPropertiesNV* pExternalImageFormatProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalImageFormatPropertiesNV(&validateResult, physicalDevice, format, type, tiling, usage, flags, externalHandleType, pExternalImageFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceExternalImageFormatPropertiesNV(vkStream, physicalDevice, format, type, tiling, usage, flags, externalHandleType, pExternalImageFormatProperties);
    return res;
}

#endif

#ifdef VK_NV_external_memory

#endif

#ifdef VK_NV_external_memory_win32

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryWin32HandleNV len None optional? False staticArrExpr  0) */
void validate_vkGetMemoryWin32HandleNV(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceMemory memory,
    VkExternalMemoryHandleTypeFlagsNV handleType,
    HANDLE* pHandle)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryWin32HandleNV len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetMemoryWin32HandleNV(
    VkDevice device,
    VkDeviceMemory memory,
    VkExternalMemoryHandleTypeFlagsNV handleType,
    HANDLE* pHandle)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryWin32HandleNV(&validateResult, device, memory, handleType, pHandle);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetMemoryWin32HandleNV(vkStream, device, memory, handleType, pHandle);
    return res;
}

#endif

#ifdef VK_NV_win32_keyed_mutex

#endif

#ifdef VK_EXT_validation_flags

#endif

#ifdef VK_NN_vi_surface

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateViSurfaceNN len None optional? False staticArrExpr  0) */
void validate_vkCreateViSurfaceNN(
    VkResult* validateResult,
    VkInstance instance,
    const VkViSurfaceCreateInfoNN* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateViSurfaceNN len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateViSurfaceNN(
    VkInstance instance,
    const VkViSurfaceCreateInfoNN* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateViSurfaceNN(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateViSurfaceNN(vkStream, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

#endif

#ifdef VK_EXT_shader_subgroup_ballot

#endif

#ifdef VK_EXT_shader_subgroup_vote

#endif

#ifdef VK_EXT_conditional_rendering

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBeginConditionalRenderingEXT len None optional? False staticArrExpr  0) */
void validate_vkCmdBeginConditionalRenderingEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkConditionalRenderingBeginInfoEXT* pConditionalRenderingBegin)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBeginConditionalRenderingEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdBeginConditionalRenderingEXT(
    VkCommandBuffer commandBuffer,
    const VkConditionalRenderingBeginInfoEXT* pConditionalRenderingBegin)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBeginConditionalRenderingEXT(&validateResult, commandBuffer, pConditionalRenderingBegin);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdBeginConditionalRenderingEXT(vkStream, commandBuffer, pConditionalRenderingBegin);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdEndConditionalRenderingEXT len None optional? False staticArrExpr  0) */
void validate_vkCmdEndConditionalRenderingEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdEndConditionalRenderingEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdEndConditionalRenderingEXT(
    VkCommandBuffer commandBuffer)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdEndConditionalRenderingEXT(&validateResult, commandBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdEndConditionalRenderingEXT(vkStream, commandBuffer);
}

#endif

#ifdef VK_NVX_device_generated_commands

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdProcessCommandsNVX len None optional? False staticArrExpr  0) */
void validate_vkCmdProcessCommandsNVX(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkCmdProcessCommandsInfoNVX* pProcessCommandsInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdProcessCommandsNVX len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdProcessCommandsNVX(
    VkCommandBuffer commandBuffer,
    const VkCmdProcessCommandsInfoNVX* pProcessCommandsInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdProcessCommandsNVX(&validateResult, commandBuffer, pProcessCommandsInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdProcessCommandsNVX(vkStream, commandBuffer, pProcessCommandsInfo);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdReserveSpaceForCommandsNVX len None optional? False staticArrExpr  0) */
void validate_vkCmdReserveSpaceForCommandsNVX(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkCmdReserveSpaceForCommandsInfoNVX* pReserveSpaceInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdReserveSpaceForCommandsNVX len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdReserveSpaceForCommandsNVX(
    VkCommandBuffer commandBuffer,
    const VkCmdReserveSpaceForCommandsInfoNVX* pReserveSpaceInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdReserveSpaceForCommandsNVX(&validateResult, commandBuffer, pReserveSpaceInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdReserveSpaceForCommandsNVX(vkStream, commandBuffer, pReserveSpaceInfo);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateIndirectCommandsLayoutNVX len None optional? False staticArrExpr  0) */
void validate_vkCreateIndirectCommandsLayoutNVX(
    VkResult* validateResult,
    VkDevice device,
    const VkIndirectCommandsLayoutCreateInfoNVX* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkIndirectCommandsLayoutNVX* pIndirectCommandsLayout)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateIndirectCommandsLayoutNVX len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateIndirectCommandsLayoutNVX(
    VkDevice device,
    const VkIndirectCommandsLayoutCreateInfoNVX* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkIndirectCommandsLayoutNVX* pIndirectCommandsLayout)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateIndirectCommandsLayoutNVX(&validateResult, device, pCreateInfo, pAllocator, pIndirectCommandsLayout);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateIndirectCommandsLayoutNVX(vkStream, device, pCreateInfo, pAllocator, pIndirectCommandsLayout);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyIndirectCommandsLayoutNVX len None optional? False staticArrExpr  0) */
void validate_vkDestroyIndirectCommandsLayoutNVX(
    VkResult* validateResult,
    VkDevice device,
    VkIndirectCommandsLayoutNVX indirectCommandsLayout,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyIndirectCommandsLayoutNVX len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyIndirectCommandsLayoutNVX(
    VkDevice device,
    VkIndirectCommandsLayoutNVX indirectCommandsLayout,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyIndirectCommandsLayoutNVX(&validateResult, device, indirectCommandsLayout, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyIndirectCommandsLayoutNVX(vkStream, device, indirectCommandsLayout, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateObjectTableNVX len None optional? False staticArrExpr  0) */
void validate_vkCreateObjectTableNVX(
    VkResult* validateResult,
    VkDevice device,
    const VkObjectTableCreateInfoNVX* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkObjectTableNVX* pObjectTable)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateObjectTableNVX len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateObjectTableNVX(
    VkDevice device,
    const VkObjectTableCreateInfoNVX* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkObjectTableNVX* pObjectTable)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateObjectTableNVX(&validateResult, device, pCreateInfo, pAllocator, pObjectTable);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateObjectTableNVX(vkStream, device, pCreateInfo, pAllocator, pObjectTable);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyObjectTableNVX len None optional? False staticArrExpr  0) */
void validate_vkDestroyObjectTableNVX(
    VkResult* validateResult,
    VkDevice device,
    VkObjectTableNVX objectTable,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyObjectTableNVX len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyObjectTableNVX(
    VkDevice device,
    VkObjectTableNVX objectTable,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyObjectTableNVX(&validateResult, device, objectTable, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyObjectTableNVX(vkStream, device, objectTable, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkRegisterObjectsNVX len None optional? False staticArrExpr  0) */
void validate_vkRegisterObjectsNVX(
    VkResult* validateResult,
    VkDevice device,
    VkObjectTableNVX objectTable,
    uint32_t objectCount,
    const VkObjectTableEntryNVX* const*    ppObjectTableEntries,
    const uint32_t* pObjectIndices)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkRegisterObjectsNVX len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkRegisterObjectsNVX(
    VkDevice device,
    VkObjectTableNVX objectTable,
    uint32_t objectCount,
    const VkObjectTableEntryNVX* const*    ppObjectTableEntries,
    const uint32_t* pObjectIndices)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkRegisterObjectsNVX(&validateResult, device, objectTable, objectCount, ppObjectTableEntries, pObjectIndices);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkRegisterObjectsNVX(vkStream, device, objectTable, objectCount, ppObjectTableEntries, pObjectIndices);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkUnregisterObjectsNVX len None optional? False staticArrExpr  0) */
void validate_vkUnregisterObjectsNVX(
    VkResult* validateResult,
    VkDevice device,
    VkObjectTableNVX objectTable,
    uint32_t objectCount,
    const VkObjectEntryTypeNVX* pObjectEntryTypes,
    const uint32_t* pObjectIndices)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkUnregisterObjectsNVX len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkUnregisterObjectsNVX(
    VkDevice device,
    VkObjectTableNVX objectTable,
    uint32_t objectCount,
    const VkObjectEntryTypeNVX* pObjectEntryTypes,
    const uint32_t* pObjectIndices)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkUnregisterObjectsNVX(&validateResult, device, objectTable, objectCount, pObjectEntryTypes, pObjectIndices);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkUnregisterObjectsNVX(vkStream, device, objectTable, objectCount, pObjectEntryTypes, pObjectIndices);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDeviceGeneratedCommandsFeaturesNVX* pFeatures,
    VkDeviceGeneratedCommandsLimitsNVX* pLimits)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX(
    VkPhysicalDevice physicalDevice,
    VkDeviceGeneratedCommandsFeaturesNVX* pFeatures,
    VkDeviceGeneratedCommandsLimitsNVX* pLimits)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX(&validateResult, physicalDevice, pFeatures, pLimits);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX(vkStream, physicalDevice, pFeatures, pLimits);
}

#endif

#ifdef VK_NV_clip_space_w_scaling

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetViewportWScalingNV len None optional? False staticArrExpr  0) */
void validate_vkCmdSetViewportWScalingNV(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t firstViewport,
    uint32_t viewportCount,
    const VkViewportWScalingNV* pViewportWScalings)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetViewportWScalingNV len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetViewportWScalingNV(
    VkCommandBuffer commandBuffer,
    uint32_t firstViewport,
    uint32_t viewportCount,
    const VkViewportWScalingNV* pViewportWScalings)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetViewportWScalingNV(&validateResult, commandBuffer, firstViewport, viewportCount, pViewportWScalings);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetViewportWScalingNV(vkStream, commandBuffer, firstViewport, viewportCount, pViewportWScalings);
}

#endif

#ifdef VK_EXT_direct_mode_display

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkReleaseDisplayEXT len None optional? False staticArrExpr  0) */
void validate_vkReleaseDisplayEXT(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkReleaseDisplayEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkReleaseDisplayEXT(
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkReleaseDisplayEXT(&validateResult, physicalDevice, display);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkReleaseDisplayEXT(vkStream, physicalDevice, display);
    return res;
}

#endif

#ifdef VK_EXT_acquire_xlib_display

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAcquireXlibDisplayEXT len None optional? False staticArrExpr  0) */
void validate_vkAcquireXlibDisplayEXT(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    Display* dpy,
    VkDisplayKHR display)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkAcquireXlibDisplayEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkAcquireXlibDisplayEXT(
    VkPhysicalDevice physicalDevice,
    Display* dpy,
    VkDisplayKHR display)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkAcquireXlibDisplayEXT(&validateResult, physicalDevice, dpy, display);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkAcquireXlibDisplayEXT(vkStream, physicalDevice, dpy, display);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetRandROutputDisplayEXT len None optional? False staticArrExpr  0) */
void validate_vkGetRandROutputDisplayEXT(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    Display* dpy,
    RROutput rrOutput,
    VkDisplayKHR* pDisplay)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetRandROutputDisplayEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetRandROutputDisplayEXT(
    VkPhysicalDevice physicalDevice,
    Display* dpy,
    RROutput rrOutput,
    VkDisplayKHR* pDisplay)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetRandROutputDisplayEXT(&validateResult, physicalDevice, dpy, rrOutput, pDisplay);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetRandROutputDisplayEXT(vkStream, physicalDevice, dpy, rrOutput, pDisplay);
    return res;
}

#endif

#ifdef VK_EXT_display_surface_counter

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceCapabilities2EXT len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceSurfaceCapabilities2EXT(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    VkSurfaceCapabilities2EXT* pSurfaceCapabilities)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPhysicalDeviceSurfaceCapabilities2EXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceCapabilities2EXT(
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    VkSurfaceCapabilities2EXT* pSurfaceCapabilities)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceCapabilities2EXT(&validateResult, physicalDevice, surface, pSurfaceCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceCapabilities2EXT(vkStream, physicalDevice, surface, pSurfaceCapabilities);
    return res;
}

#endif

#ifdef VK_EXT_display_control

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkDisplayPowerControlEXT len None optional? False staticArrExpr  0) */
void validate_vkDisplayPowerControlEXT(
    VkResult* validateResult,
    VkDevice device,
    VkDisplayKHR display,
    const VkDisplayPowerInfoEXT* pDisplayPowerInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkDisplayPowerControlEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkDisplayPowerControlEXT(
    VkDevice device,
    VkDisplayKHR display,
    const VkDisplayPowerInfoEXT* pDisplayPowerInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDisplayPowerControlEXT(&validateResult, device, display, pDisplayPowerInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkDisplayPowerControlEXT(vkStream, device, display, pDisplayPowerInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkRegisterDeviceEventEXT len None optional? False staticArrExpr  0) */
void validate_vkRegisterDeviceEventEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkDeviceEventInfoEXT* pDeviceEventInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkRegisterDeviceEventEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkRegisterDeviceEventEXT(
    VkDevice device,
    const VkDeviceEventInfoEXT* pDeviceEventInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkRegisterDeviceEventEXT(&validateResult, device, pDeviceEventInfo, pAllocator, pFence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkRegisterDeviceEventEXT(vkStream, device, pDeviceEventInfo, pAllocator, pFence);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkRegisterDisplayEventEXT len None optional? False staticArrExpr  0) */
void validate_vkRegisterDisplayEventEXT(
    VkResult* validateResult,
    VkDevice device,
    VkDisplayKHR display,
    const VkDisplayEventInfoEXT* pDisplayEventInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkRegisterDisplayEventEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkRegisterDisplayEventEXT(
    VkDevice device,
    VkDisplayKHR display,
    const VkDisplayEventInfoEXT* pDisplayEventInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkRegisterDisplayEventEXT(&validateResult, device, display, pDisplayEventInfo, pAllocator, pFence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkRegisterDisplayEventEXT(vkStream, device, display, pDisplayEventInfo, pAllocator, pFence);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetSwapchainCounterEXT len None optional? False staticArrExpr  0) */
void validate_vkGetSwapchainCounterEXT(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    VkSurfaceCounterFlagBitsEXT counter,
    uint64_t* pCounterValue)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetSwapchainCounterEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetSwapchainCounterEXT(
    VkDevice device,
    VkSwapchainKHR swapchain,
    VkSurfaceCounterFlagBitsEXT counter,
    uint64_t* pCounterValue)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetSwapchainCounterEXT(&validateResult, device, swapchain, counter, pCounterValue);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetSwapchainCounterEXT(vkStream, device, swapchain, counter, pCounterValue);
    return res;
}

#endif

#ifdef VK_GOOGLE_display_timing

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetRefreshCycleDurationGOOGLE len None optional? False staticArrExpr  0) */
void validate_vkGetRefreshCycleDurationGOOGLE(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    VkRefreshCycleDurationGOOGLE* pDisplayTimingProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetRefreshCycleDurationGOOGLE len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetRefreshCycleDurationGOOGLE(
    VkDevice device,
    VkSwapchainKHR swapchain,
    VkRefreshCycleDurationGOOGLE* pDisplayTimingProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetRefreshCycleDurationGOOGLE(&validateResult, device, swapchain, pDisplayTimingProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetRefreshCycleDurationGOOGLE(vkStream, device, swapchain, pDisplayTimingProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPastPresentationTimingGOOGLE len None optional? False staticArrExpr  0) */
void validate_vkGetPastPresentationTimingGOOGLE(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint32_t* pPresentationTimingCount,
    VkPastPresentationTimingGOOGLE* pPresentationTimings)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetPastPresentationTimingGOOGLE len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetPastPresentationTimingGOOGLE(
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint32_t* pPresentationTimingCount,
    VkPastPresentationTimingGOOGLE* pPresentationTimings)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPastPresentationTimingGOOGLE(&validateResult, device, swapchain, pPresentationTimingCount, pPresentationTimings);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetPastPresentationTimingGOOGLE(vkStream, device, swapchain, pPresentationTimingCount, pPresentationTimings);
    return res;
}

#endif

#ifdef VK_NV_sample_mask_override_coverage

#endif

#ifdef VK_NV_geometry_shader_passthrough

#endif

#ifdef VK_NV_viewport_array2

#endif

#ifdef VK_NVX_multiview_per_view_attributes

#endif

#ifdef VK_NV_viewport_swizzle

#endif

#ifdef VK_EXT_discard_rectangles

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetDiscardRectangleEXT len None optional? False staticArrExpr  0) */
void validate_vkCmdSetDiscardRectangleEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t firstDiscardRectangle,
    uint32_t discardRectangleCount,
    const VkRect2D* pDiscardRectangles)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetDiscardRectangleEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetDiscardRectangleEXT(
    VkCommandBuffer commandBuffer,
    uint32_t firstDiscardRectangle,
    uint32_t discardRectangleCount,
    const VkRect2D* pDiscardRectangles)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetDiscardRectangleEXT(&validateResult, commandBuffer, firstDiscardRectangle, discardRectangleCount, pDiscardRectangles);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetDiscardRectangleEXT(vkStream, commandBuffer, firstDiscardRectangle, discardRectangleCount, pDiscardRectangles);
}

#endif

#ifdef VK_EXT_conservative_rasterization

#endif

#ifdef VK_EXT_swapchain_colorspace

#endif

#ifdef VK_EXT_hdr_metadata

static /* VulkanType for return type: (vulkantype void nonconst paramName vkSetHdrMetadataEXT len None optional? False staticArrExpr  0) */
void validate_vkSetHdrMetadataEXT(
    VkResult* validateResult,
    VkDevice device,
    uint32_t swapchainCount,
    const VkSwapchainKHR* pSwapchains,
    const VkHdrMetadataEXT* pMetadata)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkSetHdrMetadataEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkSetHdrMetadataEXT(
    VkDevice device,
    uint32_t swapchainCount,
    const VkSwapchainKHR* pSwapchains,
    const VkHdrMetadataEXT* pMetadata)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkSetHdrMetadataEXT(&validateResult, device, swapchainCount, pSwapchains, pMetadata);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkSetHdrMetadataEXT(vkStream, device, swapchainCount, pSwapchains, pMetadata);
}

#endif

#ifdef VK_MVK_ios_surface

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateIOSSurfaceMVK len None optional? False staticArrExpr  0) */
void validate_vkCreateIOSSurfaceMVK(
    VkResult* validateResult,
    VkInstance instance,
    const VkIOSSurfaceCreateInfoMVK* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateIOSSurfaceMVK len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateIOSSurfaceMVK(
    VkInstance instance,
    const VkIOSSurfaceCreateInfoMVK* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateIOSSurfaceMVK(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateIOSSurfaceMVK(vkStream, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

#endif

#ifdef VK_MVK_macos_surface

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateMacOSSurfaceMVK len None optional? False staticArrExpr  0) */
void validate_vkCreateMacOSSurfaceMVK(
    VkResult* validateResult,
    VkInstance instance,
    const VkMacOSSurfaceCreateInfoMVK* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateMacOSSurfaceMVK len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateMacOSSurfaceMVK(
    VkInstance instance,
    const VkMacOSSurfaceCreateInfoMVK* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateMacOSSurfaceMVK(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateMacOSSurfaceMVK(vkStream, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

#endif

#ifdef VK_EXT_external_memory_dma_buf

#endif

#ifdef VK_EXT_queue_family_foreign

#endif

#ifdef VK_EXT_debug_utils

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkSetDebugUtilsObjectNameEXT len None optional? False staticArrExpr  0) */
void validate_vkSetDebugUtilsObjectNameEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkDebugUtilsObjectNameInfoEXT* pNameInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkSetDebugUtilsObjectNameEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkSetDebugUtilsObjectNameEXT(
    VkDevice device,
    const VkDebugUtilsObjectNameInfoEXT* pNameInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkSetDebugUtilsObjectNameEXT(&validateResult, device, pNameInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkSetDebugUtilsObjectNameEXT(vkStream, device, pNameInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkSetDebugUtilsObjectTagEXT len None optional? False staticArrExpr  0) */
void validate_vkSetDebugUtilsObjectTagEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkDebugUtilsObjectTagInfoEXT* pTagInfo)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkSetDebugUtilsObjectTagEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkSetDebugUtilsObjectTagEXT(
    VkDevice device,
    const VkDebugUtilsObjectTagInfoEXT* pTagInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkSetDebugUtilsObjectTagEXT(&validateResult, device, pTagInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkSetDebugUtilsObjectTagEXT(vkStream, device, pTagInfo);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkQueueBeginDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void validate_vkQueueBeginDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkQueue queue,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkQueueBeginDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkQueueBeginDebugUtilsLabelEXT(
    VkQueue queue,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueBeginDebugUtilsLabelEXT(&validateResult, queue, pLabelInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkQueueBeginDebugUtilsLabelEXT(vkStream, queue, pLabelInfo);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkQueueEndDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void validate_vkQueueEndDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkQueue queue)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkQueueEndDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkQueueEndDebugUtilsLabelEXT(
    VkQueue queue)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueEndDebugUtilsLabelEXT(&validateResult, queue);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkQueueEndDebugUtilsLabelEXT(vkStream, queue);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkQueueInsertDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void validate_vkQueueInsertDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkQueue queue,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkQueueInsertDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkQueueInsertDebugUtilsLabelEXT(
    VkQueue queue,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueInsertDebugUtilsLabelEXT(&validateResult, queue, pLabelInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkQueueInsertDebugUtilsLabelEXT(vkStream, queue, pLabelInfo);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBeginDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void validate_vkCmdBeginDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdBeginDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdBeginDebugUtilsLabelEXT(
    VkCommandBuffer commandBuffer,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBeginDebugUtilsLabelEXT(&validateResult, commandBuffer, pLabelInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdBeginDebugUtilsLabelEXT(vkStream, commandBuffer, pLabelInfo);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdEndDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void validate_vkCmdEndDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdEndDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdEndDebugUtilsLabelEXT(
    VkCommandBuffer commandBuffer)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdEndDebugUtilsLabelEXT(&validateResult, commandBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdEndDebugUtilsLabelEXT(vkStream, commandBuffer);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdInsertDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void validate_vkCmdInsertDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdInsertDebugUtilsLabelEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdInsertDebugUtilsLabelEXT(
    VkCommandBuffer commandBuffer,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdInsertDebugUtilsLabelEXT(&validateResult, commandBuffer, pLabelInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdInsertDebugUtilsLabelEXT(vkStream, commandBuffer, pLabelInfo);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDebugUtilsMessengerEXT len None optional? False staticArrExpr  0) */
void validate_vkCreateDebugUtilsMessengerEXT(
    VkResult* validateResult,
    VkInstance instance,
    const VkDebugUtilsMessengerCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDebugUtilsMessengerEXT* pMessenger)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateDebugUtilsMessengerEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateDebugUtilsMessengerEXT(
    VkInstance instance,
    const VkDebugUtilsMessengerCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDebugUtilsMessengerEXT* pMessenger)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDebugUtilsMessengerEXT(&validateResult, instance, pCreateInfo, pAllocator, pMessenger);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateDebugUtilsMessengerEXT(vkStream, instance, pCreateInfo, pAllocator, pMessenger);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDebugUtilsMessengerEXT len None optional? False staticArrExpr  0) */
void validate_vkDestroyDebugUtilsMessengerEXT(
    VkResult* validateResult,
    VkInstance instance,
    VkDebugUtilsMessengerEXT messenger,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyDebugUtilsMessengerEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyDebugUtilsMessengerEXT(
    VkInstance instance,
    VkDebugUtilsMessengerEXT messenger,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDebugUtilsMessengerEXT(&validateResult, instance, messenger, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyDebugUtilsMessengerEXT(vkStream, instance, messenger, pAllocator);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkSubmitDebugUtilsMessageEXT len None optional? False staticArrExpr  0) */
void validate_vkSubmitDebugUtilsMessageEXT(
    VkResult* validateResult,
    VkInstance instance,
    VkDebugUtilsMessageSeverityFlagBitsEXT messageSeverity,
    VkDebugUtilsMessageTypeFlagsEXT messageTypes,
    const VkDebugUtilsMessengerCallbackDataEXT* pCallbackData)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkSubmitDebugUtilsMessageEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkSubmitDebugUtilsMessageEXT(
    VkInstance instance,
    VkDebugUtilsMessageSeverityFlagBitsEXT messageSeverity,
    VkDebugUtilsMessageTypeFlagsEXT messageTypes,
    const VkDebugUtilsMessengerCallbackDataEXT* pCallbackData)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkSubmitDebugUtilsMessageEXT(&validateResult, instance, messageSeverity, messageTypes, pCallbackData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkSubmitDebugUtilsMessageEXT(vkStream, instance, messageSeverity, messageTypes, pCallbackData);
}

#endif

#ifdef VK_ANDROID_external_memory_android_hardware_buffer

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetAndroidHardwareBufferPropertiesANDROID len None optional? False staticArrExpr  0) */
void validate_vkGetAndroidHardwareBufferPropertiesANDROID(
    VkResult* validateResult,
    VkDevice device,
    const struct AHardwareBuffer* buffer,
    VkAndroidHardwareBufferPropertiesANDROID* pProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetAndroidHardwareBufferPropertiesANDROID len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetAndroidHardwareBufferPropertiesANDROID(
    VkDevice device,
    const struct AHardwareBuffer* buffer,
    VkAndroidHardwareBufferPropertiesANDROID* pProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetAndroidHardwareBufferPropertiesANDROID(&validateResult, device, buffer, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetAndroidHardwareBufferPropertiesANDROID(vkStream, device, buffer, pProperties);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryAndroidHardwareBufferANDROID len None optional? False staticArrExpr  0) */
void validate_vkGetMemoryAndroidHardwareBufferANDROID(
    VkResult* validateResult,
    VkDevice device,
    const VkMemoryGetAndroidHardwareBufferInfoANDROID* pInfo,
    struct AHardwareBuffer** pBuffer)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryAndroidHardwareBufferANDROID len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetMemoryAndroidHardwareBufferANDROID(
    VkDevice device,
    const VkMemoryGetAndroidHardwareBufferInfoANDROID* pInfo,
    struct AHardwareBuffer** pBuffer)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryAndroidHardwareBufferANDROID(&validateResult, device, pInfo, pBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetMemoryAndroidHardwareBufferANDROID(vkStream, device, pInfo, pBuffer);
    return res;
}

#endif

#ifdef VK_EXT_sampler_filter_minmax

#endif

#ifdef VK_AMD_gpu_shader_int16

#endif

#ifdef VK_AMD_mixed_attachment_samples

#endif

#ifdef VK_AMD_shader_fragment_mask

#endif

#ifdef VK_EXT_shader_stencil_export

#endif

#ifdef VK_EXT_sample_locations

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetSampleLocationsEXT len None optional? False staticArrExpr  0) */
void validate_vkCmdSetSampleLocationsEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkSampleLocationsInfoEXT* pSampleLocationsInfo)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetSampleLocationsEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetSampleLocationsEXT(
    VkCommandBuffer commandBuffer,
    const VkSampleLocationsInfoEXT* pSampleLocationsInfo)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetSampleLocationsEXT(&validateResult, commandBuffer, pSampleLocationsInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetSampleLocationsEXT(vkStream, commandBuffer, pSampleLocationsInfo);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceMultisamplePropertiesEXT len None optional? False staticArrExpr  0) */
void validate_vkGetPhysicalDeviceMultisamplePropertiesEXT(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSampleCountFlagBits samples,
    VkMultisamplePropertiesEXT* pMultisampleProperties)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetPhysicalDeviceMultisamplePropertiesEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetPhysicalDeviceMultisamplePropertiesEXT(
    VkPhysicalDevice physicalDevice,
    VkSampleCountFlagBits samples,
    VkMultisamplePropertiesEXT* pMultisampleProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceMultisamplePropertiesEXT(&validateResult, physicalDevice, samples, pMultisampleProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetPhysicalDeviceMultisamplePropertiesEXT(vkStream, physicalDevice, samples, pMultisampleProperties);
}

#endif

#ifdef VK_EXT_blend_operation_advanced

#endif

#ifdef VK_NV_fragment_coverage_to_color

#endif

#ifdef VK_NV_framebuffer_mixed_samples

#endif

#ifdef VK_NV_fill_rectangle

#endif

#ifdef VK_EXT_post_depth_coverage

#endif

#ifdef VK_EXT_validation_cache

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateValidationCacheEXT len None optional? False staticArrExpr  0) */
void validate_vkCreateValidationCacheEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkValidationCacheCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkValidationCacheEXT* pValidationCache)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkCreateValidationCacheEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkCreateValidationCacheEXT(
    VkDevice device,
    const VkValidationCacheCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkValidationCacheEXT* pValidationCache)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateValidationCacheEXT(&validateResult, device, pCreateInfo, pAllocator, pValidationCache);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkCreateValidationCacheEXT(vkStream, device, pCreateInfo, pAllocator, pValidationCache);
    return res;
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyValidationCacheEXT len None optional? False staticArrExpr  0) */
void validate_vkDestroyValidationCacheEXT(
    VkResult* validateResult,
    VkDevice device,
    VkValidationCacheEXT validationCache,
    const VkAllocationCallbacks* pAllocator)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkDestroyValidationCacheEXT len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkDestroyValidationCacheEXT(
    VkDevice device,
    VkValidationCacheEXT validationCache,
    const VkAllocationCallbacks* pAllocator)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyValidationCacheEXT(&validateResult, device, validationCache, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkDestroyValidationCacheEXT(vkStream, device, validationCache, pAllocator);
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkMergeValidationCachesEXT len None optional? False staticArrExpr  0) */
void validate_vkMergeValidationCachesEXT(
    VkResult* validateResult,
    VkDevice device,
    VkValidationCacheEXT dstCache,
    uint32_t srcCacheCount,
    const VkValidationCacheEXT* pSrcCaches)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkMergeValidationCachesEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkMergeValidationCachesEXT(
    VkDevice device,
    VkValidationCacheEXT dstCache,
    uint32_t srcCacheCount,
    const VkValidationCacheEXT* pSrcCaches)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkMergeValidationCachesEXT(&validateResult, device, dstCache, srcCacheCount, pSrcCaches);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkMergeValidationCachesEXT(vkStream, device, dstCache, srcCacheCount, pSrcCaches);
    return res;
}

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetValidationCacheDataEXT len None optional? False staticArrExpr  0) */
void validate_vkGetValidationCacheDataEXT(
    VkResult* validateResult,
    VkDevice device,
    VkValidationCacheEXT validationCache,
    size_t* pDataSize,
    void* pData)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetValidationCacheDataEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetValidationCacheDataEXT(
    VkDevice device,
    VkValidationCacheEXT validationCache,
    size_t* pDataSize,
    void* pData)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetValidationCacheDataEXT(&validateResult, device, validationCache, pDataSize, pData);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetValidationCacheDataEXT(vkStream, device, validationCache, pDataSize, pData);
    return res;
}

#endif

#ifdef VK_EXT_descriptor_indexing

#endif

#ifdef VK_EXT_shader_viewport_index_layer

#endif

#ifdef VK_EXT_global_priority

#endif

#ifdef VK_EXT_external_memory_host

static /* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryHostPointerPropertiesEXT len None optional? False staticArrExpr  0) */
void validate_vkGetMemoryHostPointerPropertiesEXT(
    VkResult* validateResult,
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    const void* pHostPointer,
    VkMemoryHostPointerPropertiesEXT* pMemoryHostPointerProperties)
{
}

/* VulkanType for return type: (vulkantype VkResult nonconst paramName vkGetMemoryHostPointerPropertiesEXT len None optional? False staticArrExpr  0) */
VkResult goldfish_frontend_vkGetMemoryHostPointerPropertiesEXT(
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    const void* pHostPointer,
    VkMemoryHostPointerPropertiesEXT* pMemoryHostPointerProperties)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryHostPointerPropertiesEXT(&validateResult, device, handleType, pHostPointer, pMemoryHostPointerProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    VkResult res = encode_vkGetMemoryHostPointerPropertiesEXT(vkStream, device, handleType, pHostPointer, pMemoryHostPointerProperties);
    return res;
}

#endif

#ifdef VK_AMD_buffer_marker

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdWriteBufferMarkerAMD len None optional? False staticArrExpr  0) */
void validate_vkCmdWriteBufferMarkerAMD(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlagBits pipelineStage,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    uint32_t marker)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdWriteBufferMarkerAMD len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdWriteBufferMarkerAMD(
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlagBits pipelineStage,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    uint32_t marker)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdWriteBufferMarkerAMD(&validateResult, commandBuffer, pipelineStage, dstBuffer, dstOffset, marker);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdWriteBufferMarkerAMD(vkStream, commandBuffer, pipelineStage, dstBuffer, dstOffset, marker);
}

#endif

#ifdef VK_AMD_shader_core_properties

#endif

#ifdef VK_EXT_vertex_attribute_divisor

#endif

#ifdef VK_NV_shader_subgroup_partitioned

#endif

#ifdef VK_NV_device_diagnostic_checkpoints

static /* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetCheckpointNV len None optional? False staticArrExpr  0) */
void validate_vkCmdSetCheckpointNV(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const void* pCheckpointMarker)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkCmdSetCheckpointNV len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkCmdSetCheckpointNV(
    VkCommandBuffer commandBuffer,
    const void* pCheckpointMarker)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetCheckpointNV(&validateResult, commandBuffer, pCheckpointMarker);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkCmdSetCheckpointNV(vkStream, commandBuffer, pCheckpointMarker);
}

static /* VulkanType for return type: (vulkantype void nonconst paramName vkGetQueueCheckpointDataNV len None optional? False staticArrExpr  0) */
void validate_vkGetQueueCheckpointDataNV(
    VkResult* validateResult,
    VkQueue queue,
    uint32_t* pCheckpointDataCount,
    VkCheckpointDataNV* pCheckpointData)
{
}

/* VulkanType for return type: (vulkantype void nonconst paramName vkGetQueueCheckpointDataNV len None optional? False staticArrExpr  0) */
void goldfish_frontend_vkGetQueueCheckpointDataNV(
    VkQueue queue,
    uint32_t* pCheckpointDataCount,
    VkCheckpointDataNV* pCheckpointData)
{
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetQueueCheckpointDataNV(&validateResult, queue, pCheckpointDataCount, pCheckpointData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_STREAM_GET();
    void* vkStream = 0;
    encode_vkGetQueueCheckpointDataNV(vkStream, queue, pCheckpointDataCount, pCheckpointData);
}

#endif


#include "guest/goldfish_vk_frontend.h"

#include "guest/goldfish_vk_encoder.h"

