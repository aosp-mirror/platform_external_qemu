// Module: goldfish_vk_frontend (impl) Autogenerated by CerealGenerator


#include "goldfish_vk_frontend.h"

#ifdef VK_VERSION_1_0

static void validate_vkCreateInstance(
    VkResult* validateResult,
    const VkInstanceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkInstance* pInstance)
{
}

static VkResult encode_vkCreateInstance(
    void* vkChannel,
    const VkInstanceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkInstance* pInstance)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateInstance(
    const VkInstanceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkInstance* pInstance)

{
    // This is the body for vkCreateInstance
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateInstance(&validateResult, pCreateInfo, pAllocator, pInstance);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateInstance(vkChannel, pCreateInfo, pAllocator, pInstance);
    return res;
}

static void validate_vkDestroyInstance(
    VkResult* validateResult,
    VkInstance instance,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyInstance(
    void* vkChannel,
    VkInstance instance,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyInstance(
    VkInstance instance,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyInstance
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyInstance(&validateResult, instance, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyInstance(vkChannel, instance, pAllocator);
}

static void validate_vkEnumeratePhysicalDevices(
    VkResult* validateResult,
    VkInstance instance,
    uint32_t* pPhysicalDeviceCount,
    VkPhysicalDevice* pPhysicalDevices)
{
}

static VkResult encode_vkEnumeratePhysicalDevices(
    void* vkChannel,
    VkInstance instance,
    uint32_t* pPhysicalDeviceCount,
    VkPhysicalDevice* pPhysicalDevices)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkEnumeratePhysicalDevices(
    VkInstance instance,
    uint32_t* pPhysicalDeviceCount,
    VkPhysicalDevice* pPhysicalDevices)

{
    // This is the body for vkEnumeratePhysicalDevices
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumeratePhysicalDevices(&validateResult, instance, pPhysicalDeviceCount, pPhysicalDevices);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkEnumeratePhysicalDevices(vkChannel, instance, pPhysicalDeviceCount, pPhysicalDevices);
    return res;
}

static void validate_vkGetPhysicalDeviceFeatures(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures* pFeatures)
{
}

static void encode_vkGetPhysicalDeviceFeatures(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures* pFeatures)
{
}

void goldfish_frontend_vkGetPhysicalDeviceFeatures(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures* pFeatures)

{
    // This is the body for vkGetPhysicalDeviceFeatures
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFeatures(&validateResult, physicalDevice, pFeatures);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceFeatures(vkChannel, physicalDevice, pFeatures);
}

static void validate_vkGetPhysicalDeviceFormatProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties* pFormatProperties)
{
}

static void encode_vkGetPhysicalDeviceFormatProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties* pFormatProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceFormatProperties(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties* pFormatProperties)

{
    // This is the body for vkGetPhysicalDeviceFormatProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFormatProperties(&validateResult, physicalDevice, format, pFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceFormatProperties(vkChannel, physicalDevice, format, pFormatProperties);
}

static void validate_vkGetPhysicalDeviceImageFormatProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkImageTiling tiling,
    VkImageUsageFlags usage,
    VkImageCreateFlags flags,
    VkImageFormatProperties* pImageFormatProperties)
{
}

static VkResult encode_vkGetPhysicalDeviceImageFormatProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkImageTiling tiling,
    VkImageUsageFlags usage,
    VkImageCreateFlags flags,
    VkImageFormatProperties* pImageFormatProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceImageFormatProperties(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkImageTiling tiling,
    VkImageUsageFlags usage,
    VkImageCreateFlags flags,
    VkImageFormatProperties* pImageFormatProperties)

{
    // This is the body for vkGetPhysicalDeviceImageFormatProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceImageFormatProperties(&validateResult, physicalDevice, format, type, tiling, usage, flags, pImageFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceImageFormatProperties(vkChannel, physicalDevice, format, type, tiling, usage, flags, pImageFormatProperties);
    return res;
}

static void validate_vkGetPhysicalDeviceProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties* pProperties)
{
}

static void encode_vkGetPhysicalDeviceProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties* pProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceProperties(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties* pProperties)

{
    // This is the body for vkGetPhysicalDeviceProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceProperties(&validateResult, physicalDevice, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceProperties(vkChannel, physicalDevice, pProperties);
}

static void validate_vkGetPhysicalDeviceQueueFamilyProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties* pQueueFamilyProperties)
{
}

static void encode_vkGetPhysicalDeviceQueueFamilyProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties* pQueueFamilyProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceQueueFamilyProperties(
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties* pQueueFamilyProperties)

{
    // This is the body for vkGetPhysicalDeviceQueueFamilyProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceQueueFamilyProperties(&validateResult, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceQueueFamilyProperties(vkChannel, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
}

static void validate_vkGetPhysicalDeviceMemoryProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties* pMemoryProperties)
{
}

static void encode_vkGetPhysicalDeviceMemoryProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties* pMemoryProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceMemoryProperties(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties* pMemoryProperties)

{
    // This is the body for vkGetPhysicalDeviceMemoryProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceMemoryProperties(&validateResult, physicalDevice, pMemoryProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceMemoryProperties(vkChannel, physicalDevice, pMemoryProperties);
}

static void validate_vkGetInstanceProcAddr(
    VkResult* validateResult,
    VkInstance instance,
    const char* pName)
{
}

static PFN_vkVoidFunction encode_vkGetInstanceProcAddr(
    void* vkChannel,
    VkInstance instance,
    const char* pName)
{
    return (PFN_vkVoidFunction)0;
}

PFN_vkVoidFunction goldfish_frontend_vkGetInstanceProcAddr(
    VkInstance instance,
    const char* pName)

{
    // This is the body for vkGetInstanceProcAddr
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetInstanceProcAddr(&validateResult, instance, pName);
    if (validateResult != VK_SUCCESS)
    {
        return (PFN_vkVoidFunction)0;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    PFN_vkVoidFunction res = encode_vkGetInstanceProcAddr(vkChannel, instance, pName);
    return res;
}

static void validate_vkGetDeviceProcAddr(
    VkResult* validateResult,
    VkDevice device,
    const char* pName)
{
}

static PFN_vkVoidFunction encode_vkGetDeviceProcAddr(
    void* vkChannel,
    VkDevice device,
    const char* pName)
{
    return (PFN_vkVoidFunction)0;
}

PFN_vkVoidFunction goldfish_frontend_vkGetDeviceProcAddr(
    VkDevice device,
    const char* pName)

{
    // This is the body for vkGetDeviceProcAddr
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceProcAddr(&validateResult, device, pName);
    if (validateResult != VK_SUCCESS)
    {
        return (PFN_vkVoidFunction)0;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    PFN_vkVoidFunction res = encode_vkGetDeviceProcAddr(vkChannel, device, pName);
    return res;
}

static void validate_vkCreateDevice(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkDeviceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDevice* pDevice)
{
}

static VkResult encode_vkCreateDevice(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkDeviceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDevice* pDevice)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateDevice(
    VkPhysicalDevice physicalDevice,
    const VkDeviceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDevice* pDevice)

{
    // This is the body for vkCreateDevice
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDevice(&validateResult, physicalDevice, pCreateInfo, pAllocator, pDevice);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateDevice(vkChannel, physicalDevice, pCreateInfo, pAllocator, pDevice);
    return res;
}

static void validate_vkDestroyDevice(
    VkResult* validateResult,
    VkDevice device,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyDevice(
    void* vkChannel,
    VkDevice device,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyDevice(
    VkDevice device,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyDevice
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDevice(&validateResult, device, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyDevice(vkChannel, device, pAllocator);
}

static void validate_vkEnumerateInstanceExtensionProperties(
    VkResult* validateResult,
    const char* pLayerName,
    uint32_t* pPropertyCount,
    VkExtensionProperties* pProperties)
{
}

static VkResult encode_vkEnumerateInstanceExtensionProperties(
    void* vkChannel,
    const char* pLayerName,
    uint32_t* pPropertyCount,
    VkExtensionProperties* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkEnumerateInstanceExtensionProperties(
    const char* pLayerName,
    uint32_t* pPropertyCount,
    VkExtensionProperties* pProperties)

{
    // This is the body for vkEnumerateInstanceExtensionProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumerateInstanceExtensionProperties(&validateResult, pLayerName, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkEnumerateInstanceExtensionProperties(vkChannel, pLayerName, pPropertyCount, pProperties);
    return res;
}

static void validate_vkEnumerateDeviceExtensionProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const char* pLayerName,
    uint32_t* pPropertyCount,
    VkExtensionProperties* pProperties)
{
}

static VkResult encode_vkEnumerateDeviceExtensionProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const char* pLayerName,
    uint32_t* pPropertyCount,
    VkExtensionProperties* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkEnumerateDeviceExtensionProperties(
    VkPhysicalDevice physicalDevice,
    const char* pLayerName,
    uint32_t* pPropertyCount,
    VkExtensionProperties* pProperties)

{
    // This is the body for vkEnumerateDeviceExtensionProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumerateDeviceExtensionProperties(&validateResult, physicalDevice, pLayerName, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkEnumerateDeviceExtensionProperties(vkChannel, physicalDevice, pLayerName, pPropertyCount, pProperties);
    return res;
}

static void validate_vkEnumerateInstanceLayerProperties(
    VkResult* validateResult,
    uint32_t* pPropertyCount,
    VkLayerProperties* pProperties)
{
}

static VkResult encode_vkEnumerateInstanceLayerProperties(
    void* vkChannel,
    uint32_t* pPropertyCount,
    VkLayerProperties* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkEnumerateInstanceLayerProperties(
    uint32_t* pPropertyCount,
    VkLayerProperties* pProperties)

{
    // This is the body for vkEnumerateInstanceLayerProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumerateInstanceLayerProperties(&validateResult, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkEnumerateInstanceLayerProperties(vkChannel, pPropertyCount, pProperties);
    return res;
}

static void validate_vkEnumerateDeviceLayerProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkLayerProperties* pProperties)
{
}

static VkResult encode_vkEnumerateDeviceLayerProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkLayerProperties* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkEnumerateDeviceLayerProperties(
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkLayerProperties* pProperties)

{
    // This is the body for vkEnumerateDeviceLayerProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumerateDeviceLayerProperties(&validateResult, physicalDevice, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkEnumerateDeviceLayerProperties(vkChannel, physicalDevice, pPropertyCount, pProperties);
    return res;
}

static void validate_vkGetDeviceQueue(
    VkResult* validateResult,
    VkDevice device,
    uint32_t queueFamilyIndex,
    uint32_t queueIndex,
    VkQueue* pQueue)
{
}

static void encode_vkGetDeviceQueue(
    void* vkChannel,
    VkDevice device,
    uint32_t queueFamilyIndex,
    uint32_t queueIndex,
    VkQueue* pQueue)
{
}

void goldfish_frontend_vkGetDeviceQueue(
    VkDevice device,
    uint32_t queueFamilyIndex,
    uint32_t queueIndex,
    VkQueue* pQueue)

{
    // This is the body for vkGetDeviceQueue
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceQueue(&validateResult, device, queueFamilyIndex, queueIndex, pQueue);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetDeviceQueue(vkChannel, device, queueFamilyIndex, queueIndex, pQueue);
}

static void validate_vkQueueSubmit(
    VkResult* validateResult,
    VkQueue queue,
    uint32_t submitCount,
    const VkSubmitInfo* pSubmits,
    VkFence fence)
{
}

static VkResult encode_vkQueueSubmit(
    void* vkChannel,
    VkQueue queue,
    uint32_t submitCount,
    const VkSubmitInfo* pSubmits,
    VkFence fence)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkQueueSubmit(
    VkQueue queue,
    uint32_t submitCount,
    const VkSubmitInfo* pSubmits,
    VkFence fence)

{
    // This is the body for vkQueueSubmit
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueSubmit(&validateResult, queue, submitCount, pSubmits, fence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkQueueSubmit(vkChannel, queue, submitCount, pSubmits, fence);
    return res;
}

static void validate_vkQueueWaitIdle(
    VkResult* validateResult,
    VkQueue queue)
{
}

static VkResult encode_vkQueueWaitIdle(
    void* vkChannel,
    VkQueue queue)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkQueueWaitIdle(
    VkQueue queue)

{
    // This is the body for vkQueueWaitIdle
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueWaitIdle(&validateResult, queue);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkQueueWaitIdle(vkChannel, queue);
    return res;
}

static void validate_vkDeviceWaitIdle(
    VkResult* validateResult,
    VkDevice device)
{
}

static VkResult encode_vkDeviceWaitIdle(
    void* vkChannel,
    VkDevice device)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkDeviceWaitIdle(
    VkDevice device)

{
    // This is the body for vkDeviceWaitIdle
    VkResult validateResult = VK_SUCCESS;
    validate_vkDeviceWaitIdle(&validateResult, device);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkDeviceWaitIdle(vkChannel, device);
    return res;
}

static void validate_vkAllocateMemory(
    VkResult* validateResult,
    VkDevice device,
    const VkMemoryAllocateInfo* pAllocateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDeviceMemory* pMemory)
{
}

static VkResult encode_vkAllocateMemory(
    void* vkChannel,
    VkDevice device,
    const VkMemoryAllocateInfo* pAllocateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDeviceMemory* pMemory)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkAllocateMemory(
    VkDevice device,
    const VkMemoryAllocateInfo* pAllocateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDeviceMemory* pMemory)

{
    // This is the body for vkAllocateMemory
    VkResult validateResult = VK_SUCCESS;
    validate_vkAllocateMemory(&validateResult, device, pAllocateInfo, pAllocator, pMemory);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkAllocateMemory(vkChannel, device, pAllocateInfo, pAllocator, pMemory);
    return res;
}

static void validate_vkFreeMemory(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceMemory memory,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkFreeMemory(
    void* vkChannel,
    VkDevice device,
    VkDeviceMemory memory,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkFreeMemory(
    VkDevice device,
    VkDeviceMemory memory,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkFreeMemory
    VkResult validateResult = VK_SUCCESS;
    validate_vkFreeMemory(&validateResult, device, memory, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkFreeMemory(vkChannel, device, memory, pAllocator);
}

static void validate_vkMapMemory(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceMemory memory,
    VkDeviceSize offset,
    VkDeviceSize size,
    VkMemoryMapFlags flags,
    void** ppData)
{
}

static VkResult encode_vkMapMemory(
    void* vkChannel,
    VkDevice device,
    VkDeviceMemory memory,
    VkDeviceSize offset,
    VkDeviceSize size,
    VkMemoryMapFlags flags,
    void** ppData)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkMapMemory(
    VkDevice device,
    VkDeviceMemory memory,
    VkDeviceSize offset,
    VkDeviceSize size,
    VkMemoryMapFlags flags,
    void** ppData)

{
    // This is the body for vkMapMemory
    VkResult validateResult = VK_SUCCESS;
    validate_vkMapMemory(&validateResult, device, memory, offset, size, flags, ppData);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkMapMemory(vkChannel, device, memory, offset, size, flags, ppData);
    return res;
}

static void validate_vkUnmapMemory(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceMemory memory)
{
}

static void encode_vkUnmapMemory(
    void* vkChannel,
    VkDevice device,
    VkDeviceMemory memory)
{
}

void goldfish_frontend_vkUnmapMemory(
    VkDevice device,
    VkDeviceMemory memory)

{
    // This is the body for vkUnmapMemory
    VkResult validateResult = VK_SUCCESS;
    validate_vkUnmapMemory(&validateResult, device, memory);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkUnmapMemory(vkChannel, device, memory);
}

static void validate_vkFlushMappedMemoryRanges(
    VkResult* validateResult,
    VkDevice device,
    uint32_t memoryRangeCount,
    const VkMappedMemoryRange* pMemoryRanges)
{
}

static VkResult encode_vkFlushMappedMemoryRanges(
    void* vkChannel,
    VkDevice device,
    uint32_t memoryRangeCount,
    const VkMappedMemoryRange* pMemoryRanges)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkFlushMappedMemoryRanges(
    VkDevice device,
    uint32_t memoryRangeCount,
    const VkMappedMemoryRange* pMemoryRanges)

{
    // This is the body for vkFlushMappedMemoryRanges
    VkResult validateResult = VK_SUCCESS;
    validate_vkFlushMappedMemoryRanges(&validateResult, device, memoryRangeCount, pMemoryRanges);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkFlushMappedMemoryRanges(vkChannel, device, memoryRangeCount, pMemoryRanges);
    return res;
}

static void validate_vkInvalidateMappedMemoryRanges(
    VkResult* validateResult,
    VkDevice device,
    uint32_t memoryRangeCount,
    const VkMappedMemoryRange* pMemoryRanges)
{
}

static VkResult encode_vkInvalidateMappedMemoryRanges(
    void* vkChannel,
    VkDevice device,
    uint32_t memoryRangeCount,
    const VkMappedMemoryRange* pMemoryRanges)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkInvalidateMappedMemoryRanges(
    VkDevice device,
    uint32_t memoryRangeCount,
    const VkMappedMemoryRange* pMemoryRanges)

{
    // This is the body for vkInvalidateMappedMemoryRanges
    VkResult validateResult = VK_SUCCESS;
    validate_vkInvalidateMappedMemoryRanges(&validateResult, device, memoryRangeCount, pMemoryRanges);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkInvalidateMappedMemoryRanges(vkChannel, device, memoryRangeCount, pMemoryRanges);
    return res;
}

static void validate_vkGetDeviceMemoryCommitment(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceMemory memory,
    VkDeviceSize* pCommittedMemoryInBytes)
{
}

static void encode_vkGetDeviceMemoryCommitment(
    void* vkChannel,
    VkDevice device,
    VkDeviceMemory memory,
    VkDeviceSize* pCommittedMemoryInBytes)
{
}

void goldfish_frontend_vkGetDeviceMemoryCommitment(
    VkDevice device,
    VkDeviceMemory memory,
    VkDeviceSize* pCommittedMemoryInBytes)

{
    // This is the body for vkGetDeviceMemoryCommitment
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceMemoryCommitment(&validateResult, device, memory, pCommittedMemoryInBytes);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetDeviceMemoryCommitment(vkChannel, device, memory, pCommittedMemoryInBytes);
}

static void validate_vkBindBufferMemory(
    VkResult* validateResult,
    VkDevice device,
    VkBuffer buffer,
    VkDeviceMemory memory,
    VkDeviceSize memoryOffset)
{
}

static VkResult encode_vkBindBufferMemory(
    void* vkChannel,
    VkDevice device,
    VkBuffer buffer,
    VkDeviceMemory memory,
    VkDeviceSize memoryOffset)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkBindBufferMemory(
    VkDevice device,
    VkBuffer buffer,
    VkDeviceMemory memory,
    VkDeviceSize memoryOffset)

{
    // This is the body for vkBindBufferMemory
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindBufferMemory(&validateResult, device, buffer, memory, memoryOffset);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkBindBufferMemory(vkChannel, device, buffer, memory, memoryOffset);
    return res;
}

static void validate_vkBindImageMemory(
    VkResult* validateResult,
    VkDevice device,
    VkImage image,
    VkDeviceMemory memory,
    VkDeviceSize memoryOffset)
{
}

static VkResult encode_vkBindImageMemory(
    void* vkChannel,
    VkDevice device,
    VkImage image,
    VkDeviceMemory memory,
    VkDeviceSize memoryOffset)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkBindImageMemory(
    VkDevice device,
    VkImage image,
    VkDeviceMemory memory,
    VkDeviceSize memoryOffset)

{
    // This is the body for vkBindImageMemory
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindImageMemory(&validateResult, device, image, memory, memoryOffset);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkBindImageMemory(vkChannel, device, image, memory, memoryOffset);
    return res;
}

static void validate_vkGetBufferMemoryRequirements(
    VkResult* validateResult,
    VkDevice device,
    VkBuffer buffer,
    VkMemoryRequirements* pMemoryRequirements)
{
}

static void encode_vkGetBufferMemoryRequirements(
    void* vkChannel,
    VkDevice device,
    VkBuffer buffer,
    VkMemoryRequirements* pMemoryRequirements)
{
}

void goldfish_frontend_vkGetBufferMemoryRequirements(
    VkDevice device,
    VkBuffer buffer,
    VkMemoryRequirements* pMemoryRequirements)

{
    // This is the body for vkGetBufferMemoryRequirements
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetBufferMemoryRequirements(&validateResult, device, buffer, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetBufferMemoryRequirements(vkChannel, device, buffer, pMemoryRequirements);
}

static void validate_vkGetImageMemoryRequirements(
    VkResult* validateResult,
    VkDevice device,
    VkImage image,
    VkMemoryRequirements* pMemoryRequirements)
{
}

static void encode_vkGetImageMemoryRequirements(
    void* vkChannel,
    VkDevice device,
    VkImage image,
    VkMemoryRequirements* pMemoryRequirements)
{
}

void goldfish_frontend_vkGetImageMemoryRequirements(
    VkDevice device,
    VkImage image,
    VkMemoryRequirements* pMemoryRequirements)

{
    // This is the body for vkGetImageMemoryRequirements
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageMemoryRequirements(&validateResult, device, image, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetImageMemoryRequirements(vkChannel, device, image, pMemoryRequirements);
}

static void validate_vkGetImageSparseMemoryRequirements(
    VkResult* validateResult,
    VkDevice device,
    VkImage image,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements* pSparseMemoryRequirements)
{
}

static void encode_vkGetImageSparseMemoryRequirements(
    void* vkChannel,
    VkDevice device,
    VkImage image,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements* pSparseMemoryRequirements)
{
}

void goldfish_frontend_vkGetImageSparseMemoryRequirements(
    VkDevice device,
    VkImage image,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements* pSparseMemoryRequirements)

{
    // This is the body for vkGetImageSparseMemoryRequirements
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageSparseMemoryRequirements(&validateResult, device, image, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetImageSparseMemoryRequirements(vkChannel, device, image, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
}

static void validate_vkGetPhysicalDeviceSparseImageFormatProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkSampleCountFlagBits samples,
    VkImageUsageFlags usage,
    VkImageTiling tiling,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties* pProperties)
{
}

static void encode_vkGetPhysicalDeviceSparseImageFormatProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkSampleCountFlagBits samples,
    VkImageUsageFlags usage,
    VkImageTiling tiling,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties* pProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceSparseImageFormatProperties(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkSampleCountFlagBits samples,
    VkImageUsageFlags usage,
    VkImageTiling tiling,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties* pProperties)

{
    // This is the body for vkGetPhysicalDeviceSparseImageFormatProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSparseImageFormatProperties(&validateResult, physicalDevice, format, type, samples, usage, tiling, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceSparseImageFormatProperties(vkChannel, physicalDevice, format, type, samples, usage, tiling, pPropertyCount, pProperties);
}

static void validate_vkQueueBindSparse(
    VkResult* validateResult,
    VkQueue queue,
    uint32_t bindInfoCount,
    const VkBindSparseInfo* pBindInfo,
    VkFence fence)
{
}

static VkResult encode_vkQueueBindSparse(
    void* vkChannel,
    VkQueue queue,
    uint32_t bindInfoCount,
    const VkBindSparseInfo* pBindInfo,
    VkFence fence)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkQueueBindSparse(
    VkQueue queue,
    uint32_t bindInfoCount,
    const VkBindSparseInfo* pBindInfo,
    VkFence fence)

{
    // This is the body for vkQueueBindSparse
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueBindSparse(&validateResult, queue, bindInfoCount, pBindInfo, fence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkQueueBindSparse(vkChannel, queue, bindInfoCount, pBindInfo, fence);
    return res;
}

static void validate_vkCreateFence(
    VkResult* validateResult,
    VkDevice device,
    const VkFenceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
}

static VkResult encode_vkCreateFence(
    void* vkChannel,
    VkDevice device,
    const VkFenceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateFence(
    VkDevice device,
    const VkFenceCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)

{
    // This is the body for vkCreateFence
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateFence(&validateResult, device, pCreateInfo, pAllocator, pFence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateFence(vkChannel, device, pCreateInfo, pAllocator, pFence);
    return res;
}

static void validate_vkDestroyFence(
    VkResult* validateResult,
    VkDevice device,
    VkFence fence,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyFence(
    void* vkChannel,
    VkDevice device,
    VkFence fence,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyFence(
    VkDevice device,
    VkFence fence,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyFence
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyFence(&validateResult, device, fence, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyFence(vkChannel, device, fence, pAllocator);
}

static void validate_vkResetFences(
    VkResult* validateResult,
    VkDevice device,
    uint32_t fenceCount,
    const VkFence* pFences)
{
}

static VkResult encode_vkResetFences(
    void* vkChannel,
    VkDevice device,
    uint32_t fenceCount,
    const VkFence* pFences)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkResetFences(
    VkDevice device,
    uint32_t fenceCount,
    const VkFence* pFences)

{
    // This is the body for vkResetFences
    VkResult validateResult = VK_SUCCESS;
    validate_vkResetFences(&validateResult, device, fenceCount, pFences);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkResetFences(vkChannel, device, fenceCount, pFences);
    return res;
}

static void validate_vkGetFenceStatus(
    VkResult* validateResult,
    VkDevice device,
    VkFence fence)
{
}

static VkResult encode_vkGetFenceStatus(
    void* vkChannel,
    VkDevice device,
    VkFence fence)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetFenceStatus(
    VkDevice device,
    VkFence fence)

{
    // This is the body for vkGetFenceStatus
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetFenceStatus(&validateResult, device, fence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetFenceStatus(vkChannel, device, fence);
    return res;
}

static void validate_vkWaitForFences(
    VkResult* validateResult,
    VkDevice device,
    uint32_t fenceCount,
    const VkFence* pFences,
    VkBool32 waitAll,
    uint64_t timeout)
{
}

static VkResult encode_vkWaitForFences(
    void* vkChannel,
    VkDevice device,
    uint32_t fenceCount,
    const VkFence* pFences,
    VkBool32 waitAll,
    uint64_t timeout)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkWaitForFences(
    VkDevice device,
    uint32_t fenceCount,
    const VkFence* pFences,
    VkBool32 waitAll,
    uint64_t timeout)

{
    // This is the body for vkWaitForFences
    VkResult validateResult = VK_SUCCESS;
    validate_vkWaitForFences(&validateResult, device, fenceCount, pFences, waitAll, timeout);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkWaitForFences(vkChannel, device, fenceCount, pFences, waitAll, timeout);
    return res;
}

static void validate_vkCreateSemaphore(
    VkResult* validateResult,
    VkDevice device,
    const VkSemaphoreCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSemaphore* pSemaphore)
{
}

static VkResult encode_vkCreateSemaphore(
    void* vkChannel,
    VkDevice device,
    const VkSemaphoreCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSemaphore* pSemaphore)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateSemaphore(
    VkDevice device,
    const VkSemaphoreCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSemaphore* pSemaphore)

{
    // This is the body for vkCreateSemaphore
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSemaphore(&validateResult, device, pCreateInfo, pAllocator, pSemaphore);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateSemaphore(vkChannel, device, pCreateInfo, pAllocator, pSemaphore);
    return res;
}

static void validate_vkDestroySemaphore(
    VkResult* validateResult,
    VkDevice device,
    VkSemaphore semaphore,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroySemaphore(
    void* vkChannel,
    VkDevice device,
    VkSemaphore semaphore,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroySemaphore(
    VkDevice device,
    VkSemaphore semaphore,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroySemaphore
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySemaphore(&validateResult, device, semaphore, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroySemaphore(vkChannel, device, semaphore, pAllocator);
}

static void validate_vkCreateEvent(
    VkResult* validateResult,
    VkDevice device,
    const VkEventCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkEvent* pEvent)
{
}

static VkResult encode_vkCreateEvent(
    void* vkChannel,
    VkDevice device,
    const VkEventCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkEvent* pEvent)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateEvent(
    VkDevice device,
    const VkEventCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkEvent* pEvent)

{
    // This is the body for vkCreateEvent
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateEvent(&validateResult, device, pCreateInfo, pAllocator, pEvent);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateEvent(vkChannel, device, pCreateInfo, pAllocator, pEvent);
    return res;
}

static void validate_vkDestroyEvent(
    VkResult* validateResult,
    VkDevice device,
    VkEvent event,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyEvent(
    void* vkChannel,
    VkDevice device,
    VkEvent event,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyEvent(
    VkDevice device,
    VkEvent event,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyEvent
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyEvent(&validateResult, device, event, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyEvent(vkChannel, device, event, pAllocator);
}

static void validate_vkGetEventStatus(
    VkResult* validateResult,
    VkDevice device,
    VkEvent event)
{
}

static VkResult encode_vkGetEventStatus(
    void* vkChannel,
    VkDevice device,
    VkEvent event)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetEventStatus(
    VkDevice device,
    VkEvent event)

{
    // This is the body for vkGetEventStatus
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetEventStatus(&validateResult, device, event);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetEventStatus(vkChannel, device, event);
    return res;
}

static void validate_vkSetEvent(
    VkResult* validateResult,
    VkDevice device,
    VkEvent event)
{
}

static VkResult encode_vkSetEvent(
    void* vkChannel,
    VkDevice device,
    VkEvent event)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkSetEvent(
    VkDevice device,
    VkEvent event)

{
    // This is the body for vkSetEvent
    VkResult validateResult = VK_SUCCESS;
    validate_vkSetEvent(&validateResult, device, event);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkSetEvent(vkChannel, device, event);
    return res;
}

static void validate_vkResetEvent(
    VkResult* validateResult,
    VkDevice device,
    VkEvent event)
{
}

static VkResult encode_vkResetEvent(
    void* vkChannel,
    VkDevice device,
    VkEvent event)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkResetEvent(
    VkDevice device,
    VkEvent event)

{
    // This is the body for vkResetEvent
    VkResult validateResult = VK_SUCCESS;
    validate_vkResetEvent(&validateResult, device, event);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkResetEvent(vkChannel, device, event);
    return res;
}

static void validate_vkCreateQueryPool(
    VkResult* validateResult,
    VkDevice device,
    const VkQueryPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkQueryPool* pQueryPool)
{
}

static VkResult encode_vkCreateQueryPool(
    void* vkChannel,
    VkDevice device,
    const VkQueryPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkQueryPool* pQueryPool)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateQueryPool(
    VkDevice device,
    const VkQueryPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkQueryPool* pQueryPool)

{
    // This is the body for vkCreateQueryPool
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateQueryPool(&validateResult, device, pCreateInfo, pAllocator, pQueryPool);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateQueryPool(vkChannel, device, pCreateInfo, pAllocator, pQueryPool);
    return res;
}

static void validate_vkDestroyQueryPool(
    VkResult* validateResult,
    VkDevice device,
    VkQueryPool queryPool,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyQueryPool(
    void* vkChannel,
    VkDevice device,
    VkQueryPool queryPool,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyQueryPool(
    VkDevice device,
    VkQueryPool queryPool,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyQueryPool
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyQueryPool(&validateResult, device, queryPool, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyQueryPool(vkChannel, device, queryPool, pAllocator);
}

static void validate_vkGetQueryPoolResults(
    VkResult* validateResult,
    VkDevice device,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount,
    size_t dataSize,
    void* pData,
    VkDeviceSize stride,
    VkQueryResultFlags flags)
{
}

static VkResult encode_vkGetQueryPoolResults(
    void* vkChannel,
    VkDevice device,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount,
    size_t dataSize,
    void* pData,
    VkDeviceSize stride,
    VkQueryResultFlags flags)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetQueryPoolResults(
    VkDevice device,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount,
    size_t dataSize,
    void* pData,
    VkDeviceSize stride,
    VkQueryResultFlags flags)

{
    // This is the body for vkGetQueryPoolResults
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetQueryPoolResults(&validateResult, device, queryPool, firstQuery, queryCount, dataSize, pData, stride, flags);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetQueryPoolResults(vkChannel, device, queryPool, firstQuery, queryCount, dataSize, pData, stride, flags);
    return res;
}

static void validate_vkCreateBuffer(
    VkResult* validateResult,
    VkDevice device,
    const VkBufferCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkBuffer* pBuffer)
{
}

static VkResult encode_vkCreateBuffer(
    void* vkChannel,
    VkDevice device,
    const VkBufferCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkBuffer* pBuffer)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateBuffer(
    VkDevice device,
    const VkBufferCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkBuffer* pBuffer)

{
    // This is the body for vkCreateBuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateBuffer(&validateResult, device, pCreateInfo, pAllocator, pBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateBuffer(vkChannel, device, pCreateInfo, pAllocator, pBuffer);
    return res;
}

static void validate_vkDestroyBuffer(
    VkResult* validateResult,
    VkDevice device,
    VkBuffer buffer,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyBuffer(
    void* vkChannel,
    VkDevice device,
    VkBuffer buffer,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyBuffer(
    VkDevice device,
    VkBuffer buffer,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyBuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyBuffer(&validateResult, device, buffer, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyBuffer(vkChannel, device, buffer, pAllocator);
}

static void validate_vkCreateBufferView(
    VkResult* validateResult,
    VkDevice device,
    const VkBufferViewCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkBufferView* pView)
{
}

static VkResult encode_vkCreateBufferView(
    void* vkChannel,
    VkDevice device,
    const VkBufferViewCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkBufferView* pView)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateBufferView(
    VkDevice device,
    const VkBufferViewCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkBufferView* pView)

{
    // This is the body for vkCreateBufferView
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateBufferView(&validateResult, device, pCreateInfo, pAllocator, pView);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateBufferView(vkChannel, device, pCreateInfo, pAllocator, pView);
    return res;
}

static void validate_vkDestroyBufferView(
    VkResult* validateResult,
    VkDevice device,
    VkBufferView bufferView,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyBufferView(
    void* vkChannel,
    VkDevice device,
    VkBufferView bufferView,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyBufferView(
    VkDevice device,
    VkBufferView bufferView,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyBufferView
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyBufferView(&validateResult, device, bufferView, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyBufferView(vkChannel, device, bufferView, pAllocator);
}

static void validate_vkCreateImage(
    VkResult* validateResult,
    VkDevice device,
    const VkImageCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkImage* pImage)
{
}

static VkResult encode_vkCreateImage(
    void* vkChannel,
    VkDevice device,
    const VkImageCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkImage* pImage)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateImage(
    VkDevice device,
    const VkImageCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkImage* pImage)

{
    // This is the body for vkCreateImage
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateImage(&validateResult, device, pCreateInfo, pAllocator, pImage);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateImage(vkChannel, device, pCreateInfo, pAllocator, pImage);
    return res;
}

static void validate_vkDestroyImage(
    VkResult* validateResult,
    VkDevice device,
    VkImage image,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyImage(
    void* vkChannel,
    VkDevice device,
    VkImage image,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyImage(
    VkDevice device,
    VkImage image,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyImage
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyImage(&validateResult, device, image, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyImage(vkChannel, device, image, pAllocator);
}

static void validate_vkGetImageSubresourceLayout(
    VkResult* validateResult,
    VkDevice device,
    VkImage image,
    const VkImageSubresource* pSubresource,
    VkSubresourceLayout* pLayout)
{
}

static void encode_vkGetImageSubresourceLayout(
    void* vkChannel,
    VkDevice device,
    VkImage image,
    const VkImageSubresource* pSubresource,
    VkSubresourceLayout* pLayout)
{
}

void goldfish_frontend_vkGetImageSubresourceLayout(
    VkDevice device,
    VkImage image,
    const VkImageSubresource* pSubresource,
    VkSubresourceLayout* pLayout)

{
    // This is the body for vkGetImageSubresourceLayout
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageSubresourceLayout(&validateResult, device, image, pSubresource, pLayout);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetImageSubresourceLayout(vkChannel, device, image, pSubresource, pLayout);
}

static void validate_vkCreateImageView(
    VkResult* validateResult,
    VkDevice device,
    const VkImageViewCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkImageView* pView)
{
}

static VkResult encode_vkCreateImageView(
    void* vkChannel,
    VkDevice device,
    const VkImageViewCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkImageView* pView)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateImageView(
    VkDevice device,
    const VkImageViewCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkImageView* pView)

{
    // This is the body for vkCreateImageView
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateImageView(&validateResult, device, pCreateInfo, pAllocator, pView);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateImageView(vkChannel, device, pCreateInfo, pAllocator, pView);
    return res;
}

static void validate_vkDestroyImageView(
    VkResult* validateResult,
    VkDevice device,
    VkImageView imageView,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyImageView(
    void* vkChannel,
    VkDevice device,
    VkImageView imageView,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyImageView(
    VkDevice device,
    VkImageView imageView,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyImageView
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyImageView(&validateResult, device, imageView, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyImageView(vkChannel, device, imageView, pAllocator);
}

static void validate_vkCreateShaderModule(
    VkResult* validateResult,
    VkDevice device,
    const VkShaderModuleCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkShaderModule* pShaderModule)
{
}

static VkResult encode_vkCreateShaderModule(
    void* vkChannel,
    VkDevice device,
    const VkShaderModuleCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkShaderModule* pShaderModule)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateShaderModule(
    VkDevice device,
    const VkShaderModuleCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkShaderModule* pShaderModule)

{
    // This is the body for vkCreateShaderModule
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateShaderModule(&validateResult, device, pCreateInfo, pAllocator, pShaderModule);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateShaderModule(vkChannel, device, pCreateInfo, pAllocator, pShaderModule);
    return res;
}

static void validate_vkDestroyShaderModule(
    VkResult* validateResult,
    VkDevice device,
    VkShaderModule shaderModule,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyShaderModule(
    void* vkChannel,
    VkDevice device,
    VkShaderModule shaderModule,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyShaderModule(
    VkDevice device,
    VkShaderModule shaderModule,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyShaderModule
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyShaderModule(&validateResult, device, shaderModule, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyShaderModule(vkChannel, device, shaderModule, pAllocator);
}

static void validate_vkCreatePipelineCache(
    VkResult* validateResult,
    VkDevice device,
    const VkPipelineCacheCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkPipelineCache* pPipelineCache)
{
}

static VkResult encode_vkCreatePipelineCache(
    void* vkChannel,
    VkDevice device,
    const VkPipelineCacheCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkPipelineCache* pPipelineCache)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreatePipelineCache(
    VkDevice device,
    const VkPipelineCacheCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkPipelineCache* pPipelineCache)

{
    // This is the body for vkCreatePipelineCache
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreatePipelineCache(&validateResult, device, pCreateInfo, pAllocator, pPipelineCache);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreatePipelineCache(vkChannel, device, pCreateInfo, pAllocator, pPipelineCache);
    return res;
}

static void validate_vkDestroyPipelineCache(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineCache pipelineCache,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyPipelineCache(
    void* vkChannel,
    VkDevice device,
    VkPipelineCache pipelineCache,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyPipelineCache(
    VkDevice device,
    VkPipelineCache pipelineCache,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyPipelineCache
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyPipelineCache(&validateResult, device, pipelineCache, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyPipelineCache(vkChannel, device, pipelineCache, pAllocator);
}

static void validate_vkGetPipelineCacheData(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineCache pipelineCache,
    size_t* pDataSize,
    void* pData)
{
}

static VkResult encode_vkGetPipelineCacheData(
    void* vkChannel,
    VkDevice device,
    VkPipelineCache pipelineCache,
    size_t* pDataSize,
    void* pData)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPipelineCacheData(
    VkDevice device,
    VkPipelineCache pipelineCache,
    size_t* pDataSize,
    void* pData)

{
    // This is the body for vkGetPipelineCacheData
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPipelineCacheData(&validateResult, device, pipelineCache, pDataSize, pData);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPipelineCacheData(vkChannel, device, pipelineCache, pDataSize, pData);
    return res;
}

static void validate_vkMergePipelineCaches(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineCache dstCache,
    uint32_t srcCacheCount,
    const VkPipelineCache* pSrcCaches)
{
}

static VkResult encode_vkMergePipelineCaches(
    void* vkChannel,
    VkDevice device,
    VkPipelineCache dstCache,
    uint32_t srcCacheCount,
    const VkPipelineCache* pSrcCaches)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkMergePipelineCaches(
    VkDevice device,
    VkPipelineCache dstCache,
    uint32_t srcCacheCount,
    const VkPipelineCache* pSrcCaches)

{
    // This is the body for vkMergePipelineCaches
    VkResult validateResult = VK_SUCCESS;
    validate_vkMergePipelineCaches(&validateResult, device, dstCache, srcCacheCount, pSrcCaches);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkMergePipelineCaches(vkChannel, device, dstCache, srcCacheCount, pSrcCaches);
    return res;
}

static void validate_vkCreateGraphicsPipelines(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineCache pipelineCache,
    uint32_t createInfoCount,
    const VkGraphicsPipelineCreateInfo* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkPipeline* pPipelines)
{
}

static VkResult encode_vkCreateGraphicsPipelines(
    void* vkChannel,
    VkDevice device,
    VkPipelineCache pipelineCache,
    uint32_t createInfoCount,
    const VkGraphicsPipelineCreateInfo* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkPipeline* pPipelines)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateGraphicsPipelines(
    VkDevice device,
    VkPipelineCache pipelineCache,
    uint32_t createInfoCount,
    const VkGraphicsPipelineCreateInfo* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkPipeline* pPipelines)

{
    // This is the body for vkCreateGraphicsPipelines
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateGraphicsPipelines(&validateResult, device, pipelineCache, createInfoCount, pCreateInfos, pAllocator, pPipelines);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateGraphicsPipelines(vkChannel, device, pipelineCache, createInfoCount, pCreateInfos, pAllocator, pPipelines);
    return res;
}

static void validate_vkCreateComputePipelines(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineCache pipelineCache,
    uint32_t createInfoCount,
    const VkComputePipelineCreateInfo* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkPipeline* pPipelines)
{
}

static VkResult encode_vkCreateComputePipelines(
    void* vkChannel,
    VkDevice device,
    VkPipelineCache pipelineCache,
    uint32_t createInfoCount,
    const VkComputePipelineCreateInfo* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkPipeline* pPipelines)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateComputePipelines(
    VkDevice device,
    VkPipelineCache pipelineCache,
    uint32_t createInfoCount,
    const VkComputePipelineCreateInfo* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkPipeline* pPipelines)

{
    // This is the body for vkCreateComputePipelines
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateComputePipelines(&validateResult, device, pipelineCache, createInfoCount, pCreateInfos, pAllocator, pPipelines);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateComputePipelines(vkChannel, device, pipelineCache, createInfoCount, pCreateInfos, pAllocator, pPipelines);
    return res;
}

static void validate_vkDestroyPipeline(
    VkResult* validateResult,
    VkDevice device,
    VkPipeline pipeline,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyPipeline(
    void* vkChannel,
    VkDevice device,
    VkPipeline pipeline,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyPipeline(
    VkDevice device,
    VkPipeline pipeline,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyPipeline
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyPipeline(&validateResult, device, pipeline, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyPipeline(vkChannel, device, pipeline, pAllocator);
}

static void validate_vkCreatePipelineLayout(
    VkResult* validateResult,
    VkDevice device,
    const VkPipelineLayoutCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkPipelineLayout* pPipelineLayout)
{
}

static VkResult encode_vkCreatePipelineLayout(
    void* vkChannel,
    VkDevice device,
    const VkPipelineLayoutCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkPipelineLayout* pPipelineLayout)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreatePipelineLayout(
    VkDevice device,
    const VkPipelineLayoutCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkPipelineLayout* pPipelineLayout)

{
    // This is the body for vkCreatePipelineLayout
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreatePipelineLayout(&validateResult, device, pCreateInfo, pAllocator, pPipelineLayout);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreatePipelineLayout(vkChannel, device, pCreateInfo, pAllocator, pPipelineLayout);
    return res;
}

static void validate_vkDestroyPipelineLayout(
    VkResult* validateResult,
    VkDevice device,
    VkPipelineLayout pipelineLayout,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyPipelineLayout(
    void* vkChannel,
    VkDevice device,
    VkPipelineLayout pipelineLayout,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyPipelineLayout(
    VkDevice device,
    VkPipelineLayout pipelineLayout,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyPipelineLayout
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyPipelineLayout(&validateResult, device, pipelineLayout, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyPipelineLayout(vkChannel, device, pipelineLayout, pAllocator);
}

static void validate_vkCreateSampler(
    VkResult* validateResult,
    VkDevice device,
    const VkSamplerCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSampler* pSampler)
{
}

static VkResult encode_vkCreateSampler(
    void* vkChannel,
    VkDevice device,
    const VkSamplerCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSampler* pSampler)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateSampler(
    VkDevice device,
    const VkSamplerCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSampler* pSampler)

{
    // This is the body for vkCreateSampler
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSampler(&validateResult, device, pCreateInfo, pAllocator, pSampler);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateSampler(vkChannel, device, pCreateInfo, pAllocator, pSampler);
    return res;
}

static void validate_vkDestroySampler(
    VkResult* validateResult,
    VkDevice device,
    VkSampler sampler,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroySampler(
    void* vkChannel,
    VkDevice device,
    VkSampler sampler,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroySampler(
    VkDevice device,
    VkSampler sampler,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroySampler
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySampler(&validateResult, device, sampler, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroySampler(vkChannel, device, sampler, pAllocator);
}

static void validate_vkCreateDescriptorSetLayout(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorSetLayout* pSetLayout)
{
}

static VkResult encode_vkCreateDescriptorSetLayout(
    void* vkChannel,
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorSetLayout* pSetLayout)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateDescriptorSetLayout(
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorSetLayout* pSetLayout)

{
    // This is the body for vkCreateDescriptorSetLayout
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDescriptorSetLayout(&validateResult, device, pCreateInfo, pAllocator, pSetLayout);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateDescriptorSetLayout(vkChannel, device, pCreateInfo, pAllocator, pSetLayout);
    return res;
}

static void validate_vkDestroyDescriptorSetLayout(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorSetLayout descriptorSetLayout,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyDescriptorSetLayout(
    void* vkChannel,
    VkDevice device,
    VkDescriptorSetLayout descriptorSetLayout,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyDescriptorSetLayout(
    VkDevice device,
    VkDescriptorSetLayout descriptorSetLayout,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyDescriptorSetLayout
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDescriptorSetLayout(&validateResult, device, descriptorSetLayout, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyDescriptorSetLayout(vkChannel, device, descriptorSetLayout, pAllocator);
}

static void validate_vkCreateDescriptorPool(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorPool* pDescriptorPool)
{
}

static VkResult encode_vkCreateDescriptorPool(
    void* vkChannel,
    VkDevice device,
    const VkDescriptorPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorPool* pDescriptorPool)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateDescriptorPool(
    VkDevice device,
    const VkDescriptorPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorPool* pDescriptorPool)

{
    // This is the body for vkCreateDescriptorPool
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDescriptorPool(&validateResult, device, pCreateInfo, pAllocator, pDescriptorPool);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateDescriptorPool(vkChannel, device, pCreateInfo, pAllocator, pDescriptorPool);
    return res;
}

static void validate_vkDestroyDescriptorPool(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorPool descriptorPool,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyDescriptorPool(
    void* vkChannel,
    VkDevice device,
    VkDescriptorPool descriptorPool,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyDescriptorPool(
    VkDevice device,
    VkDescriptorPool descriptorPool,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyDescriptorPool
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDescriptorPool(&validateResult, device, descriptorPool, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyDescriptorPool(vkChannel, device, descriptorPool, pAllocator);
}

static void validate_vkResetDescriptorPool(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorPool descriptorPool,
    VkDescriptorPoolResetFlags flags)
{
}

static VkResult encode_vkResetDescriptorPool(
    void* vkChannel,
    VkDevice device,
    VkDescriptorPool descriptorPool,
    VkDescriptorPoolResetFlags flags)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkResetDescriptorPool(
    VkDevice device,
    VkDescriptorPool descriptorPool,
    VkDescriptorPoolResetFlags flags)

{
    // This is the body for vkResetDescriptorPool
    VkResult validateResult = VK_SUCCESS;
    validate_vkResetDescriptorPool(&validateResult, device, descriptorPool, flags);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkResetDescriptorPool(vkChannel, device, descriptorPool, flags);
    return res;
}

static void validate_vkAllocateDescriptorSets(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorSetAllocateInfo* pAllocateInfo,
    VkDescriptorSet* pDescriptorSets)
{
}

static VkResult encode_vkAllocateDescriptorSets(
    void* vkChannel,
    VkDevice device,
    const VkDescriptorSetAllocateInfo* pAllocateInfo,
    VkDescriptorSet* pDescriptorSets)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkAllocateDescriptorSets(
    VkDevice device,
    const VkDescriptorSetAllocateInfo* pAllocateInfo,
    VkDescriptorSet* pDescriptorSets)

{
    // This is the body for vkAllocateDescriptorSets
    VkResult validateResult = VK_SUCCESS;
    validate_vkAllocateDescriptorSets(&validateResult, device, pAllocateInfo, pDescriptorSets);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkAllocateDescriptorSets(vkChannel, device, pAllocateInfo, pDescriptorSets);
    return res;
}

static void validate_vkFreeDescriptorSets(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorPool descriptorPool,
    uint32_t descriptorSetCount,
    const VkDescriptorSet* pDescriptorSets)
{
}

static VkResult encode_vkFreeDescriptorSets(
    void* vkChannel,
    VkDevice device,
    VkDescriptorPool descriptorPool,
    uint32_t descriptorSetCount,
    const VkDescriptorSet* pDescriptorSets)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkFreeDescriptorSets(
    VkDevice device,
    VkDescriptorPool descriptorPool,
    uint32_t descriptorSetCount,
    const VkDescriptorSet* pDescriptorSets)

{
    // This is the body for vkFreeDescriptorSets
    VkResult validateResult = VK_SUCCESS;
    validate_vkFreeDescriptorSets(&validateResult, device, descriptorPool, descriptorSetCount, pDescriptorSets);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkFreeDescriptorSets(vkChannel, device, descriptorPool, descriptorSetCount, pDescriptorSets);
    return res;
}

static void validate_vkUpdateDescriptorSets(
    VkResult* validateResult,
    VkDevice device,
    uint32_t descriptorWriteCount,
    const VkWriteDescriptorSet* pDescriptorWrites,
    uint32_t descriptorCopyCount,
    const VkCopyDescriptorSet* pDescriptorCopies)
{
}

static void encode_vkUpdateDescriptorSets(
    void* vkChannel,
    VkDevice device,
    uint32_t descriptorWriteCount,
    const VkWriteDescriptorSet* pDescriptorWrites,
    uint32_t descriptorCopyCount,
    const VkCopyDescriptorSet* pDescriptorCopies)
{
}

void goldfish_frontend_vkUpdateDescriptorSets(
    VkDevice device,
    uint32_t descriptorWriteCount,
    const VkWriteDescriptorSet* pDescriptorWrites,
    uint32_t descriptorCopyCount,
    const VkCopyDescriptorSet* pDescriptorCopies)

{
    // This is the body for vkUpdateDescriptorSets
    VkResult validateResult = VK_SUCCESS;
    validate_vkUpdateDescriptorSets(&validateResult, device, descriptorWriteCount, pDescriptorWrites, descriptorCopyCount, pDescriptorCopies);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkUpdateDescriptorSets(vkChannel, device, descriptorWriteCount, pDescriptorWrites, descriptorCopyCount, pDescriptorCopies);
}

static void validate_vkCreateFramebuffer(
    VkResult* validateResult,
    VkDevice device,
    const VkFramebufferCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFramebuffer* pFramebuffer)
{
}

static VkResult encode_vkCreateFramebuffer(
    void* vkChannel,
    VkDevice device,
    const VkFramebufferCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFramebuffer* pFramebuffer)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateFramebuffer(
    VkDevice device,
    const VkFramebufferCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFramebuffer* pFramebuffer)

{
    // This is the body for vkCreateFramebuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateFramebuffer(&validateResult, device, pCreateInfo, pAllocator, pFramebuffer);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateFramebuffer(vkChannel, device, pCreateInfo, pAllocator, pFramebuffer);
    return res;
}

static void validate_vkDestroyFramebuffer(
    VkResult* validateResult,
    VkDevice device,
    VkFramebuffer framebuffer,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyFramebuffer(
    void* vkChannel,
    VkDevice device,
    VkFramebuffer framebuffer,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyFramebuffer(
    VkDevice device,
    VkFramebuffer framebuffer,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyFramebuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyFramebuffer(&validateResult, device, framebuffer, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyFramebuffer(vkChannel, device, framebuffer, pAllocator);
}

static void validate_vkCreateRenderPass(
    VkResult* validateResult,
    VkDevice device,
    const VkRenderPassCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkRenderPass* pRenderPass)
{
}

static VkResult encode_vkCreateRenderPass(
    void* vkChannel,
    VkDevice device,
    const VkRenderPassCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkRenderPass* pRenderPass)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateRenderPass(
    VkDevice device,
    const VkRenderPassCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkRenderPass* pRenderPass)

{
    // This is the body for vkCreateRenderPass
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateRenderPass(&validateResult, device, pCreateInfo, pAllocator, pRenderPass);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateRenderPass(vkChannel, device, pCreateInfo, pAllocator, pRenderPass);
    return res;
}

static void validate_vkDestroyRenderPass(
    VkResult* validateResult,
    VkDevice device,
    VkRenderPass renderPass,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyRenderPass(
    void* vkChannel,
    VkDevice device,
    VkRenderPass renderPass,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyRenderPass(
    VkDevice device,
    VkRenderPass renderPass,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyRenderPass
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyRenderPass(&validateResult, device, renderPass, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyRenderPass(vkChannel, device, renderPass, pAllocator);
}

static void validate_vkGetRenderAreaGranularity(
    VkResult* validateResult,
    VkDevice device,
    VkRenderPass renderPass,
    VkExtent2D* pGranularity)
{
}

static void encode_vkGetRenderAreaGranularity(
    void* vkChannel,
    VkDevice device,
    VkRenderPass renderPass,
    VkExtent2D* pGranularity)
{
}

void goldfish_frontend_vkGetRenderAreaGranularity(
    VkDevice device,
    VkRenderPass renderPass,
    VkExtent2D* pGranularity)

{
    // This is the body for vkGetRenderAreaGranularity
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetRenderAreaGranularity(&validateResult, device, renderPass, pGranularity);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetRenderAreaGranularity(vkChannel, device, renderPass, pGranularity);
}

static void validate_vkCreateCommandPool(
    VkResult* validateResult,
    VkDevice device,
    const VkCommandPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkCommandPool* pCommandPool)
{
}

static VkResult encode_vkCreateCommandPool(
    void* vkChannel,
    VkDevice device,
    const VkCommandPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkCommandPool* pCommandPool)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateCommandPool(
    VkDevice device,
    const VkCommandPoolCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkCommandPool* pCommandPool)

{
    // This is the body for vkCreateCommandPool
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateCommandPool(&validateResult, device, pCreateInfo, pAllocator, pCommandPool);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateCommandPool(vkChannel, device, pCreateInfo, pAllocator, pCommandPool);
    return res;
}

static void validate_vkDestroyCommandPool(
    VkResult* validateResult,
    VkDevice device,
    VkCommandPool commandPool,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyCommandPool(
    void* vkChannel,
    VkDevice device,
    VkCommandPool commandPool,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyCommandPool(
    VkDevice device,
    VkCommandPool commandPool,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyCommandPool
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyCommandPool(&validateResult, device, commandPool, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyCommandPool(vkChannel, device, commandPool, pAllocator);
}

static void validate_vkResetCommandPool(
    VkResult* validateResult,
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolResetFlags flags)
{
}

static VkResult encode_vkResetCommandPool(
    void* vkChannel,
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolResetFlags flags)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkResetCommandPool(
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolResetFlags flags)

{
    // This is the body for vkResetCommandPool
    VkResult validateResult = VK_SUCCESS;
    validate_vkResetCommandPool(&validateResult, device, commandPool, flags);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkResetCommandPool(vkChannel, device, commandPool, flags);
    return res;
}

static void validate_vkAllocateCommandBuffers(
    VkResult* validateResult,
    VkDevice device,
    const VkCommandBufferAllocateInfo* pAllocateInfo,
    VkCommandBuffer* pCommandBuffers)
{
}

static VkResult encode_vkAllocateCommandBuffers(
    void* vkChannel,
    VkDevice device,
    const VkCommandBufferAllocateInfo* pAllocateInfo,
    VkCommandBuffer* pCommandBuffers)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkAllocateCommandBuffers(
    VkDevice device,
    const VkCommandBufferAllocateInfo* pAllocateInfo,
    VkCommandBuffer* pCommandBuffers)

{
    // This is the body for vkAllocateCommandBuffers
    VkResult validateResult = VK_SUCCESS;
    validate_vkAllocateCommandBuffers(&validateResult, device, pAllocateInfo, pCommandBuffers);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkAllocateCommandBuffers(vkChannel, device, pAllocateInfo, pCommandBuffers);
    return res;
}

static void validate_vkFreeCommandBuffers(
    VkResult* validateResult,
    VkDevice device,
    VkCommandPool commandPool,
    uint32_t commandBufferCount,
    const VkCommandBuffer* pCommandBuffers)
{
}

static void encode_vkFreeCommandBuffers(
    void* vkChannel,
    VkDevice device,
    VkCommandPool commandPool,
    uint32_t commandBufferCount,
    const VkCommandBuffer* pCommandBuffers)
{
}

void goldfish_frontend_vkFreeCommandBuffers(
    VkDevice device,
    VkCommandPool commandPool,
    uint32_t commandBufferCount,
    const VkCommandBuffer* pCommandBuffers)

{
    // This is the body for vkFreeCommandBuffers
    VkResult validateResult = VK_SUCCESS;
    validate_vkFreeCommandBuffers(&validateResult, device, commandPool, commandBufferCount, pCommandBuffers);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkFreeCommandBuffers(vkChannel, device, commandPool, commandBufferCount, pCommandBuffers);
}

static void validate_vkBeginCommandBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkCommandBufferBeginInfo* pBeginInfo)
{
}

static VkResult encode_vkBeginCommandBuffer(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkCommandBufferBeginInfo* pBeginInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkBeginCommandBuffer(
    VkCommandBuffer commandBuffer,
    const VkCommandBufferBeginInfo* pBeginInfo)

{
    // This is the body for vkBeginCommandBuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkBeginCommandBuffer(&validateResult, commandBuffer, pBeginInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkBeginCommandBuffer(vkChannel, commandBuffer, pBeginInfo);
    return res;
}

static void validate_vkEndCommandBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer)
{
}

static VkResult encode_vkEndCommandBuffer(
    void* vkChannel,
    VkCommandBuffer commandBuffer)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkEndCommandBuffer(
    VkCommandBuffer commandBuffer)

{
    // This is the body for vkEndCommandBuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkEndCommandBuffer(&validateResult, commandBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkEndCommandBuffer(vkChannel, commandBuffer);
    return res;
}

static void validate_vkResetCommandBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkCommandBufferResetFlags flags)
{
}

static VkResult encode_vkResetCommandBuffer(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkCommandBufferResetFlags flags)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkResetCommandBuffer(
    VkCommandBuffer commandBuffer,
    VkCommandBufferResetFlags flags)

{
    // This is the body for vkResetCommandBuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkResetCommandBuffer(&validateResult, commandBuffer, flags);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkResetCommandBuffer(vkChannel, commandBuffer, flags);
    return res;
}

static void validate_vkCmdBindPipeline(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipeline pipeline)
{
}

static void encode_vkCmdBindPipeline(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipeline pipeline)
{
}

void goldfish_frontend_vkCmdBindPipeline(
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipeline pipeline)

{
    // This is the body for vkCmdBindPipeline
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBindPipeline(&validateResult, commandBuffer, pipelineBindPoint, pipeline);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdBindPipeline(vkChannel, commandBuffer, pipelineBindPoint, pipeline);
}

static void validate_vkCmdSetViewport(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t firstViewport,
    uint32_t viewportCount,
    const VkViewport* pViewports)
{
}

static void encode_vkCmdSetViewport(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t firstViewport,
    uint32_t viewportCount,
    const VkViewport* pViewports)
{
}

void goldfish_frontend_vkCmdSetViewport(
    VkCommandBuffer commandBuffer,
    uint32_t firstViewport,
    uint32_t viewportCount,
    const VkViewport* pViewports)

{
    // This is the body for vkCmdSetViewport
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetViewport(&validateResult, commandBuffer, firstViewport, viewportCount, pViewports);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetViewport(vkChannel, commandBuffer, firstViewport, viewportCount, pViewports);
}

static void validate_vkCmdSetScissor(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t firstScissor,
    uint32_t scissorCount,
    const VkRect2D* pScissors)
{
}

static void encode_vkCmdSetScissor(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t firstScissor,
    uint32_t scissorCount,
    const VkRect2D* pScissors)
{
}

void goldfish_frontend_vkCmdSetScissor(
    VkCommandBuffer commandBuffer,
    uint32_t firstScissor,
    uint32_t scissorCount,
    const VkRect2D* pScissors)

{
    // This is the body for vkCmdSetScissor
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetScissor(&validateResult, commandBuffer, firstScissor, scissorCount, pScissors);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetScissor(vkChannel, commandBuffer, firstScissor, scissorCount, pScissors);
}

static void validate_vkCmdSetLineWidth(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    float lineWidth)
{
}

static void encode_vkCmdSetLineWidth(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    float lineWidth)
{
}

void goldfish_frontend_vkCmdSetLineWidth(
    VkCommandBuffer commandBuffer,
    float lineWidth)

{
    // This is the body for vkCmdSetLineWidth
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetLineWidth(&validateResult, commandBuffer, lineWidth);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetLineWidth(vkChannel, commandBuffer, lineWidth);
}

static void validate_vkCmdSetDepthBias(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    float depthBiasConstantFactor,
    float depthBiasClamp,
    float depthBiasSlopeFactor)
{
}

static void encode_vkCmdSetDepthBias(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    float depthBiasConstantFactor,
    float depthBiasClamp,
    float depthBiasSlopeFactor)
{
}

void goldfish_frontend_vkCmdSetDepthBias(
    VkCommandBuffer commandBuffer,
    float depthBiasConstantFactor,
    float depthBiasClamp,
    float depthBiasSlopeFactor)

{
    // This is the body for vkCmdSetDepthBias
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetDepthBias(&validateResult, commandBuffer, depthBiasConstantFactor, depthBiasClamp, depthBiasSlopeFactor);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetDepthBias(vkChannel, commandBuffer, depthBiasConstantFactor, depthBiasClamp, depthBiasSlopeFactor);
}

static void validate_vkCmdSetBlendConstants(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const float blendConstants[4])
{
}

static void encode_vkCmdSetBlendConstants(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const float blendConstants[4])
{
}

void goldfish_frontend_vkCmdSetBlendConstants(
    VkCommandBuffer commandBuffer,
    const float blendConstants[4])

{
    // This is the body for vkCmdSetBlendConstants
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetBlendConstants(&validateResult, commandBuffer, blendConstants);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetBlendConstants(vkChannel, commandBuffer, blendConstants);
}

static void validate_vkCmdSetDepthBounds(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    float minDepthBounds,
    float maxDepthBounds)
{
}

static void encode_vkCmdSetDepthBounds(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    float minDepthBounds,
    float maxDepthBounds)
{
}

void goldfish_frontend_vkCmdSetDepthBounds(
    VkCommandBuffer commandBuffer,
    float minDepthBounds,
    float maxDepthBounds)

{
    // This is the body for vkCmdSetDepthBounds
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetDepthBounds(&validateResult, commandBuffer, minDepthBounds, maxDepthBounds);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetDepthBounds(vkChannel, commandBuffer, minDepthBounds, maxDepthBounds);
}

static void validate_vkCmdSetStencilCompareMask(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t compareMask)
{
}

static void encode_vkCmdSetStencilCompareMask(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t compareMask)
{
}

void goldfish_frontend_vkCmdSetStencilCompareMask(
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t compareMask)

{
    // This is the body for vkCmdSetStencilCompareMask
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetStencilCompareMask(&validateResult, commandBuffer, faceMask, compareMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetStencilCompareMask(vkChannel, commandBuffer, faceMask, compareMask);
}

static void validate_vkCmdSetStencilWriteMask(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t writeMask)
{
}

static void encode_vkCmdSetStencilWriteMask(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t writeMask)
{
}

void goldfish_frontend_vkCmdSetStencilWriteMask(
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t writeMask)

{
    // This is the body for vkCmdSetStencilWriteMask
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetStencilWriteMask(&validateResult, commandBuffer, faceMask, writeMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetStencilWriteMask(vkChannel, commandBuffer, faceMask, writeMask);
}

static void validate_vkCmdSetStencilReference(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t reference)
{
}

static void encode_vkCmdSetStencilReference(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t reference)
{
}

void goldfish_frontend_vkCmdSetStencilReference(
    VkCommandBuffer commandBuffer,
    VkStencilFaceFlags faceMask,
    uint32_t reference)

{
    // This is the body for vkCmdSetStencilReference
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetStencilReference(&validateResult, commandBuffer, faceMask, reference);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetStencilReference(vkChannel, commandBuffer, faceMask, reference);
}

static void validate_vkCmdBindDescriptorSets(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipelineLayout layout,
    uint32_t firstSet,
    uint32_t descriptorSetCount,
    const VkDescriptorSet* pDescriptorSets,
    uint32_t dynamicOffsetCount,
    const uint32_t* pDynamicOffsets)
{
}

static void encode_vkCmdBindDescriptorSets(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipelineLayout layout,
    uint32_t firstSet,
    uint32_t descriptorSetCount,
    const VkDescriptorSet* pDescriptorSets,
    uint32_t dynamicOffsetCount,
    const uint32_t* pDynamicOffsets)
{
}

void goldfish_frontend_vkCmdBindDescriptorSets(
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipelineLayout layout,
    uint32_t firstSet,
    uint32_t descriptorSetCount,
    const VkDescriptorSet* pDescriptorSets,
    uint32_t dynamicOffsetCount,
    const uint32_t* pDynamicOffsets)

{
    // This is the body for vkCmdBindDescriptorSets
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBindDescriptorSets(&validateResult, commandBuffer, pipelineBindPoint, layout, firstSet, descriptorSetCount, pDescriptorSets, dynamicOffsetCount, pDynamicOffsets);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdBindDescriptorSets(vkChannel, commandBuffer, pipelineBindPoint, layout, firstSet, descriptorSetCount, pDescriptorSets, dynamicOffsetCount, pDynamicOffsets);
}

static void validate_vkCmdBindIndexBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkIndexType indexType)
{
}

static void encode_vkCmdBindIndexBuffer(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkIndexType indexType)
{
}

void goldfish_frontend_vkCmdBindIndexBuffer(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkIndexType indexType)

{
    // This is the body for vkCmdBindIndexBuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBindIndexBuffer(&validateResult, commandBuffer, buffer, offset, indexType);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdBindIndexBuffer(vkChannel, commandBuffer, buffer, offset, indexType);
}

static void validate_vkCmdBindVertexBuffers(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t firstBinding,
    uint32_t bindingCount,
    const VkBuffer* pBuffers,
    const VkDeviceSize* pOffsets)
{
}

static void encode_vkCmdBindVertexBuffers(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t firstBinding,
    uint32_t bindingCount,
    const VkBuffer* pBuffers,
    const VkDeviceSize* pOffsets)
{
}

void goldfish_frontend_vkCmdBindVertexBuffers(
    VkCommandBuffer commandBuffer,
    uint32_t firstBinding,
    uint32_t bindingCount,
    const VkBuffer* pBuffers,
    const VkDeviceSize* pOffsets)

{
    // This is the body for vkCmdBindVertexBuffers
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBindVertexBuffers(&validateResult, commandBuffer, firstBinding, bindingCount, pBuffers, pOffsets);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdBindVertexBuffers(vkChannel, commandBuffer, firstBinding, bindingCount, pBuffers, pOffsets);
}

static void validate_vkCmdDraw(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t vertexCount,
    uint32_t instanceCount,
    uint32_t firstVertex,
    uint32_t firstInstance)
{
}

static void encode_vkCmdDraw(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t vertexCount,
    uint32_t instanceCount,
    uint32_t firstVertex,
    uint32_t firstInstance)
{
}

void goldfish_frontend_vkCmdDraw(
    VkCommandBuffer commandBuffer,
    uint32_t vertexCount,
    uint32_t instanceCount,
    uint32_t firstVertex,
    uint32_t firstInstance)

{
    // This is the body for vkCmdDraw
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDraw(&validateResult, commandBuffer, vertexCount, instanceCount, firstVertex, firstInstance);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDraw(vkChannel, commandBuffer, vertexCount, instanceCount, firstVertex, firstInstance);
}

static void validate_vkCmdDrawIndexed(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t indexCount,
    uint32_t instanceCount,
    uint32_t firstIndex,
    int32_t vertexOffset,
    uint32_t firstInstance)
{
}

static void encode_vkCmdDrawIndexed(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t indexCount,
    uint32_t instanceCount,
    uint32_t firstIndex,
    int32_t vertexOffset,
    uint32_t firstInstance)
{
}

void goldfish_frontend_vkCmdDrawIndexed(
    VkCommandBuffer commandBuffer,
    uint32_t indexCount,
    uint32_t instanceCount,
    uint32_t firstIndex,
    int32_t vertexOffset,
    uint32_t firstInstance)

{
    // This is the body for vkCmdDrawIndexed
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndexed(&validateResult, commandBuffer, indexCount, instanceCount, firstIndex, vertexOffset, firstInstance);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDrawIndexed(vkChannel, commandBuffer, indexCount, instanceCount, firstIndex, vertexOffset, firstInstance);
}

static void validate_vkCmdDrawIndirect(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    uint32_t drawCount,
    uint32_t stride)
{
}

static void encode_vkCmdDrawIndirect(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    uint32_t drawCount,
    uint32_t stride)
{
}

void goldfish_frontend_vkCmdDrawIndirect(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    uint32_t drawCount,
    uint32_t stride)

{
    // This is the body for vkCmdDrawIndirect
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndirect(&validateResult, commandBuffer, buffer, offset, drawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDrawIndirect(vkChannel, commandBuffer, buffer, offset, drawCount, stride);
}

static void validate_vkCmdDrawIndexedIndirect(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    uint32_t drawCount,
    uint32_t stride)
{
}

static void encode_vkCmdDrawIndexedIndirect(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    uint32_t drawCount,
    uint32_t stride)
{
}

void goldfish_frontend_vkCmdDrawIndexedIndirect(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    uint32_t drawCount,
    uint32_t stride)

{
    // This is the body for vkCmdDrawIndexedIndirect
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndexedIndirect(&validateResult, commandBuffer, buffer, offset, drawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDrawIndexedIndirect(vkChannel, commandBuffer, buffer, offset, drawCount, stride);
}

static void validate_vkCmdDispatch(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
}

static void encode_vkCmdDispatch(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
}

void goldfish_frontend_vkCmdDispatch(
    VkCommandBuffer commandBuffer,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)

{
    // This is the body for vkCmdDispatch
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDispatch(&validateResult, commandBuffer, groupCountX, groupCountY, groupCountZ);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDispatch(vkChannel, commandBuffer, groupCountX, groupCountY, groupCountZ);
}

static void validate_vkCmdDispatchIndirect(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset)
{
}

static void encode_vkCmdDispatchIndirect(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset)
{
}

void goldfish_frontend_vkCmdDispatchIndirect(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset)

{
    // This is the body for vkCmdDispatchIndirect
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDispatchIndirect(&validateResult, commandBuffer, buffer, offset);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDispatchIndirect(vkChannel, commandBuffer, buffer, offset);
}

static void validate_vkCmdCopyBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer srcBuffer,
    VkBuffer dstBuffer,
    uint32_t regionCount,
    const VkBufferCopy* pRegions)
{
}

static void encode_vkCmdCopyBuffer(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer srcBuffer,
    VkBuffer dstBuffer,
    uint32_t regionCount,
    const VkBufferCopy* pRegions)
{
}

void goldfish_frontend_vkCmdCopyBuffer(
    VkCommandBuffer commandBuffer,
    VkBuffer srcBuffer,
    VkBuffer dstBuffer,
    uint32_t regionCount,
    const VkBufferCopy* pRegions)

{
    // This is the body for vkCmdCopyBuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdCopyBuffer(&validateResult, commandBuffer, srcBuffer, dstBuffer, regionCount, pRegions);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdCopyBuffer(vkChannel, commandBuffer, srcBuffer, dstBuffer, regionCount, pRegions);
}

static void validate_vkCmdCopyImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageCopy* pRegions)
{
}

static void encode_vkCmdCopyImage(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageCopy* pRegions)
{
}

void goldfish_frontend_vkCmdCopyImage(
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageCopy* pRegions)

{
    // This is the body for vkCmdCopyImage
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdCopyImage(&validateResult, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdCopyImage(vkChannel, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions);
}

static void validate_vkCmdBlitImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageBlit* pRegions,
    VkFilter filter)
{
}

static void encode_vkCmdBlitImage(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageBlit* pRegions,
    VkFilter filter)
{
}

void goldfish_frontend_vkCmdBlitImage(
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageBlit* pRegions,
    VkFilter filter)

{
    // This is the body for vkCmdBlitImage
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBlitImage(&validateResult, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions, filter);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdBlitImage(vkChannel, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions, filter);
}

static void validate_vkCmdCopyBufferToImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer srcBuffer,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkBufferImageCopy* pRegions)
{
}

static void encode_vkCmdCopyBufferToImage(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer srcBuffer,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkBufferImageCopy* pRegions)
{
}

void goldfish_frontend_vkCmdCopyBufferToImage(
    VkCommandBuffer commandBuffer,
    VkBuffer srcBuffer,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkBufferImageCopy* pRegions)

{
    // This is the body for vkCmdCopyBufferToImage
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdCopyBufferToImage(&validateResult, commandBuffer, srcBuffer, dstImage, dstImageLayout, regionCount, pRegions);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdCopyBufferToImage(vkChannel, commandBuffer, srcBuffer, dstImage, dstImageLayout, regionCount, pRegions);
}

static void validate_vkCmdCopyImageToBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkBuffer dstBuffer,
    uint32_t regionCount,
    const VkBufferImageCopy* pRegions)
{
}

static void encode_vkCmdCopyImageToBuffer(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkBuffer dstBuffer,
    uint32_t regionCount,
    const VkBufferImageCopy* pRegions)
{
}

void goldfish_frontend_vkCmdCopyImageToBuffer(
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkBuffer dstBuffer,
    uint32_t regionCount,
    const VkBufferImageCopy* pRegions)

{
    // This is the body for vkCmdCopyImageToBuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdCopyImageToBuffer(&validateResult, commandBuffer, srcImage, srcImageLayout, dstBuffer, regionCount, pRegions);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdCopyImageToBuffer(vkChannel, commandBuffer, srcImage, srcImageLayout, dstBuffer, regionCount, pRegions);
}

static void validate_vkCmdUpdateBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize dataSize,
    const void* pData)
{
}

static void encode_vkCmdUpdateBuffer(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize dataSize,
    const void* pData)
{
}

void goldfish_frontend_vkCmdUpdateBuffer(
    VkCommandBuffer commandBuffer,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize dataSize,
    const void* pData)

{
    // This is the body for vkCmdUpdateBuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdUpdateBuffer(&validateResult, commandBuffer, dstBuffer, dstOffset, dataSize, pData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdUpdateBuffer(vkChannel, commandBuffer, dstBuffer, dstOffset, dataSize, pData);
}

static void validate_vkCmdFillBuffer(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize size,
    uint32_t data)
{
}

static void encode_vkCmdFillBuffer(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize size,
    uint32_t data)
{
}

void goldfish_frontend_vkCmdFillBuffer(
    VkCommandBuffer commandBuffer,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize size,
    uint32_t data)

{
    // This is the body for vkCmdFillBuffer
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdFillBuffer(&validateResult, commandBuffer, dstBuffer, dstOffset, size, data);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdFillBuffer(vkChannel, commandBuffer, dstBuffer, dstOffset, size, data);
}

static void validate_vkCmdClearColorImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage image,
    VkImageLayout imageLayout,
    const VkClearColorValue* pColor,
    uint32_t rangeCount,
    const VkImageSubresourceRange* pRanges)
{
}

static void encode_vkCmdClearColorImage(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkImage image,
    VkImageLayout imageLayout,
    const VkClearColorValue* pColor,
    uint32_t rangeCount,
    const VkImageSubresourceRange* pRanges)
{
}

void goldfish_frontend_vkCmdClearColorImage(
    VkCommandBuffer commandBuffer,
    VkImage image,
    VkImageLayout imageLayout,
    const VkClearColorValue* pColor,
    uint32_t rangeCount,
    const VkImageSubresourceRange* pRanges)

{
    // This is the body for vkCmdClearColorImage
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdClearColorImage(&validateResult, commandBuffer, image, imageLayout, pColor, rangeCount, pRanges);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdClearColorImage(vkChannel, commandBuffer, image, imageLayout, pColor, rangeCount, pRanges);
}

static void validate_vkCmdClearDepthStencilImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage image,
    VkImageLayout imageLayout,
    const VkClearDepthStencilValue* pDepthStencil,
    uint32_t rangeCount,
    const VkImageSubresourceRange* pRanges)
{
}

static void encode_vkCmdClearDepthStencilImage(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkImage image,
    VkImageLayout imageLayout,
    const VkClearDepthStencilValue* pDepthStencil,
    uint32_t rangeCount,
    const VkImageSubresourceRange* pRanges)
{
}

void goldfish_frontend_vkCmdClearDepthStencilImage(
    VkCommandBuffer commandBuffer,
    VkImage image,
    VkImageLayout imageLayout,
    const VkClearDepthStencilValue* pDepthStencil,
    uint32_t rangeCount,
    const VkImageSubresourceRange* pRanges)

{
    // This is the body for vkCmdClearDepthStencilImage
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdClearDepthStencilImage(&validateResult, commandBuffer, image, imageLayout, pDepthStencil, rangeCount, pRanges);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdClearDepthStencilImage(vkChannel, commandBuffer, image, imageLayout, pDepthStencil, rangeCount, pRanges);
}

static void validate_vkCmdClearAttachments(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t attachmentCount,
    const VkClearAttachment* pAttachments,
    uint32_t rectCount,
    const VkClearRect* pRects)
{
}

static void encode_vkCmdClearAttachments(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t attachmentCount,
    const VkClearAttachment* pAttachments,
    uint32_t rectCount,
    const VkClearRect* pRects)
{
}

void goldfish_frontend_vkCmdClearAttachments(
    VkCommandBuffer commandBuffer,
    uint32_t attachmentCount,
    const VkClearAttachment* pAttachments,
    uint32_t rectCount,
    const VkClearRect* pRects)

{
    // This is the body for vkCmdClearAttachments
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdClearAttachments(&validateResult, commandBuffer, attachmentCount, pAttachments, rectCount, pRects);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdClearAttachments(vkChannel, commandBuffer, attachmentCount, pAttachments, rectCount, pRects);
}

static void validate_vkCmdResolveImage(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageResolve* pRegions)
{
}

static void encode_vkCmdResolveImage(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageResolve* pRegions)
{
}

void goldfish_frontend_vkCmdResolveImage(
    VkCommandBuffer commandBuffer,
    VkImage srcImage,
    VkImageLayout srcImageLayout,
    VkImage dstImage,
    VkImageLayout dstImageLayout,
    uint32_t regionCount,
    const VkImageResolve* pRegions)

{
    // This is the body for vkCmdResolveImage
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdResolveImage(&validateResult, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdResolveImage(vkChannel, commandBuffer, srcImage, srcImageLayout, dstImage, dstImageLayout, regionCount, pRegions);
}

static void validate_vkCmdSetEvent(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkEvent event,
    VkPipelineStageFlags stageMask)
{
}

static void encode_vkCmdSetEvent(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkEvent event,
    VkPipelineStageFlags stageMask)
{
}

void goldfish_frontend_vkCmdSetEvent(
    VkCommandBuffer commandBuffer,
    VkEvent event,
    VkPipelineStageFlags stageMask)

{
    // This is the body for vkCmdSetEvent
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetEvent(&validateResult, commandBuffer, event, stageMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetEvent(vkChannel, commandBuffer, event, stageMask);
}

static void validate_vkCmdResetEvent(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkEvent event,
    VkPipelineStageFlags stageMask)
{
}

static void encode_vkCmdResetEvent(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkEvent event,
    VkPipelineStageFlags stageMask)
{
}

void goldfish_frontend_vkCmdResetEvent(
    VkCommandBuffer commandBuffer,
    VkEvent event,
    VkPipelineStageFlags stageMask)

{
    // This is the body for vkCmdResetEvent
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdResetEvent(&validateResult, commandBuffer, event, stageMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdResetEvent(vkChannel, commandBuffer, event, stageMask);
}

static void validate_vkCmdWaitEvents(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t eventCount,
    const VkEvent* pEvents,
    VkPipelineStageFlags srcStageMask,
    VkPipelineStageFlags dstStageMask,
    uint32_t memoryBarrierCount,
    const VkMemoryBarrier* pMemoryBarriers,
    uint32_t bufferMemoryBarrierCount,
    const VkBufferMemoryBarrier* pBufferMemoryBarriers,
    uint32_t imageMemoryBarrierCount,
    const VkImageMemoryBarrier* pImageMemoryBarriers)
{
}

static void encode_vkCmdWaitEvents(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t eventCount,
    const VkEvent* pEvents,
    VkPipelineStageFlags srcStageMask,
    VkPipelineStageFlags dstStageMask,
    uint32_t memoryBarrierCount,
    const VkMemoryBarrier* pMemoryBarriers,
    uint32_t bufferMemoryBarrierCount,
    const VkBufferMemoryBarrier* pBufferMemoryBarriers,
    uint32_t imageMemoryBarrierCount,
    const VkImageMemoryBarrier* pImageMemoryBarriers)
{
}

void goldfish_frontend_vkCmdWaitEvents(
    VkCommandBuffer commandBuffer,
    uint32_t eventCount,
    const VkEvent* pEvents,
    VkPipelineStageFlags srcStageMask,
    VkPipelineStageFlags dstStageMask,
    uint32_t memoryBarrierCount,
    const VkMemoryBarrier* pMemoryBarriers,
    uint32_t bufferMemoryBarrierCount,
    const VkBufferMemoryBarrier* pBufferMemoryBarriers,
    uint32_t imageMemoryBarrierCount,
    const VkImageMemoryBarrier* pImageMemoryBarriers)

{
    // This is the body for vkCmdWaitEvents
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdWaitEvents(&validateResult, commandBuffer, eventCount, pEvents, srcStageMask, dstStageMask, memoryBarrierCount, pMemoryBarriers, bufferMemoryBarrierCount, pBufferMemoryBarriers, imageMemoryBarrierCount, pImageMemoryBarriers);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdWaitEvents(vkChannel, commandBuffer, eventCount, pEvents, srcStageMask, dstStageMask, memoryBarrierCount, pMemoryBarriers, bufferMemoryBarrierCount, pBufferMemoryBarriers, imageMemoryBarrierCount, pImageMemoryBarriers);
}

static void validate_vkCmdPipelineBarrier(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlags srcStageMask,
    VkPipelineStageFlags dstStageMask,
    VkDependencyFlags dependencyFlags,
    uint32_t memoryBarrierCount,
    const VkMemoryBarrier* pMemoryBarriers,
    uint32_t bufferMemoryBarrierCount,
    const VkBufferMemoryBarrier* pBufferMemoryBarriers,
    uint32_t imageMemoryBarrierCount,
    const VkImageMemoryBarrier* pImageMemoryBarriers)
{
}

static void encode_vkCmdPipelineBarrier(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlags srcStageMask,
    VkPipelineStageFlags dstStageMask,
    VkDependencyFlags dependencyFlags,
    uint32_t memoryBarrierCount,
    const VkMemoryBarrier* pMemoryBarriers,
    uint32_t bufferMemoryBarrierCount,
    const VkBufferMemoryBarrier* pBufferMemoryBarriers,
    uint32_t imageMemoryBarrierCount,
    const VkImageMemoryBarrier* pImageMemoryBarriers)
{
}

void goldfish_frontend_vkCmdPipelineBarrier(
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlags srcStageMask,
    VkPipelineStageFlags dstStageMask,
    VkDependencyFlags dependencyFlags,
    uint32_t memoryBarrierCount,
    const VkMemoryBarrier* pMemoryBarriers,
    uint32_t bufferMemoryBarrierCount,
    const VkBufferMemoryBarrier* pBufferMemoryBarriers,
    uint32_t imageMemoryBarrierCount,
    const VkImageMemoryBarrier* pImageMemoryBarriers)

{
    // This is the body for vkCmdPipelineBarrier
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdPipelineBarrier(&validateResult, commandBuffer, srcStageMask, dstStageMask, dependencyFlags, memoryBarrierCount, pMemoryBarriers, bufferMemoryBarrierCount, pBufferMemoryBarriers, imageMemoryBarrierCount, pImageMemoryBarriers);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdPipelineBarrier(vkChannel, commandBuffer, srcStageMask, dstStageMask, dependencyFlags, memoryBarrierCount, pMemoryBarriers, bufferMemoryBarrierCount, pBufferMemoryBarriers, imageMemoryBarrierCount, pImageMemoryBarriers);
}

static void validate_vkCmdBeginQuery(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t query,
    VkQueryControlFlags flags)
{
}

static void encode_vkCmdBeginQuery(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t query,
    VkQueryControlFlags flags)
{
}

void goldfish_frontend_vkCmdBeginQuery(
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t query,
    VkQueryControlFlags flags)

{
    // This is the body for vkCmdBeginQuery
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBeginQuery(&validateResult, commandBuffer, queryPool, query, flags);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdBeginQuery(vkChannel, commandBuffer, queryPool, query, flags);
}

static void validate_vkCmdEndQuery(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t query)
{
}

static void encode_vkCmdEndQuery(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t query)
{
}

void goldfish_frontend_vkCmdEndQuery(
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t query)

{
    // This is the body for vkCmdEndQuery
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdEndQuery(&validateResult, commandBuffer, queryPool, query);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdEndQuery(vkChannel, commandBuffer, queryPool, query);
}

static void validate_vkCmdResetQueryPool(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount)
{
}

static void encode_vkCmdResetQueryPool(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount)
{
}

void goldfish_frontend_vkCmdResetQueryPool(
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount)

{
    // This is the body for vkCmdResetQueryPool
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdResetQueryPool(&validateResult, commandBuffer, queryPool, firstQuery, queryCount);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdResetQueryPool(vkChannel, commandBuffer, queryPool, firstQuery, queryCount);
}

static void validate_vkCmdWriteTimestamp(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlagBits pipelineStage,
    VkQueryPool queryPool,
    uint32_t query)
{
}

static void encode_vkCmdWriteTimestamp(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlagBits pipelineStage,
    VkQueryPool queryPool,
    uint32_t query)
{
}

void goldfish_frontend_vkCmdWriteTimestamp(
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlagBits pipelineStage,
    VkQueryPool queryPool,
    uint32_t query)

{
    // This is the body for vkCmdWriteTimestamp
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdWriteTimestamp(&validateResult, commandBuffer, pipelineStage, queryPool, query);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdWriteTimestamp(vkChannel, commandBuffer, pipelineStage, queryPool, query);
}

static void validate_vkCmdCopyQueryPoolResults(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize stride,
    VkQueryResultFlags flags)
{
}

static void encode_vkCmdCopyQueryPoolResults(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize stride,
    VkQueryResultFlags flags)
{
}

void goldfish_frontend_vkCmdCopyQueryPoolResults(
    VkCommandBuffer commandBuffer,
    VkQueryPool queryPool,
    uint32_t firstQuery,
    uint32_t queryCount,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    VkDeviceSize stride,
    VkQueryResultFlags flags)

{
    // This is the body for vkCmdCopyQueryPoolResults
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdCopyQueryPoolResults(&validateResult, commandBuffer, queryPool, firstQuery, queryCount, dstBuffer, dstOffset, stride, flags);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdCopyQueryPoolResults(vkChannel, commandBuffer, queryPool, firstQuery, queryCount, dstBuffer, dstOffset, stride, flags);
}

static void validate_vkCmdPushConstants(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineLayout layout,
    VkShaderStageFlags stageFlags,
    uint32_t offset,
    uint32_t size,
    const void* pValues)
{
}

static void encode_vkCmdPushConstants(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkPipelineLayout layout,
    VkShaderStageFlags stageFlags,
    uint32_t offset,
    uint32_t size,
    const void* pValues)
{
}

void goldfish_frontend_vkCmdPushConstants(
    VkCommandBuffer commandBuffer,
    VkPipelineLayout layout,
    VkShaderStageFlags stageFlags,
    uint32_t offset,
    uint32_t size,
    const void* pValues)

{
    // This is the body for vkCmdPushConstants
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdPushConstants(&validateResult, commandBuffer, layout, stageFlags, offset, size, pValues);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdPushConstants(vkChannel, commandBuffer, layout, stageFlags, offset, size, pValues);
}

static void validate_vkCmdBeginRenderPass(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkRenderPassBeginInfo* pRenderPassBegin,
    VkSubpassContents contents)
{
}

static void encode_vkCmdBeginRenderPass(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkRenderPassBeginInfo* pRenderPassBegin,
    VkSubpassContents contents)
{
}

void goldfish_frontend_vkCmdBeginRenderPass(
    VkCommandBuffer commandBuffer,
    const VkRenderPassBeginInfo* pRenderPassBegin,
    VkSubpassContents contents)

{
    // This is the body for vkCmdBeginRenderPass
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBeginRenderPass(&validateResult, commandBuffer, pRenderPassBegin, contents);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdBeginRenderPass(vkChannel, commandBuffer, pRenderPassBegin, contents);
}

static void validate_vkCmdNextSubpass(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkSubpassContents contents)
{
}

static void encode_vkCmdNextSubpass(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkSubpassContents contents)
{
}

void goldfish_frontend_vkCmdNextSubpass(
    VkCommandBuffer commandBuffer,
    VkSubpassContents contents)

{
    // This is the body for vkCmdNextSubpass
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdNextSubpass(&validateResult, commandBuffer, contents);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdNextSubpass(vkChannel, commandBuffer, contents);
}

static void validate_vkCmdEndRenderPass(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer)
{
}

static void encode_vkCmdEndRenderPass(
    void* vkChannel,
    VkCommandBuffer commandBuffer)
{
}

void goldfish_frontend_vkCmdEndRenderPass(
    VkCommandBuffer commandBuffer)

{
    // This is the body for vkCmdEndRenderPass
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdEndRenderPass(&validateResult, commandBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdEndRenderPass(vkChannel, commandBuffer);
}

static void validate_vkCmdExecuteCommands(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t commandBufferCount,
    const VkCommandBuffer* pCommandBuffers)
{
}

static void encode_vkCmdExecuteCommands(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t commandBufferCount,
    const VkCommandBuffer* pCommandBuffers)
{
}

void goldfish_frontend_vkCmdExecuteCommands(
    VkCommandBuffer commandBuffer,
    uint32_t commandBufferCount,
    const VkCommandBuffer* pCommandBuffers)

{
    // This is the body for vkCmdExecuteCommands
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdExecuteCommands(&validateResult, commandBuffer, commandBufferCount, pCommandBuffers);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdExecuteCommands(vkChannel, commandBuffer, commandBufferCount, pCommandBuffers);
}

#endif

#ifdef VK_VERSION_1_1

static void validate_vkEnumerateInstanceVersion(
    VkResult* validateResult,
    uint32_t* pApiVersion)
{
}

static VkResult encode_vkEnumerateInstanceVersion(
    void* vkChannel,
    uint32_t* pApiVersion)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkEnumerateInstanceVersion(
    uint32_t* pApiVersion)

{
    // This is the body for vkEnumerateInstanceVersion
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumerateInstanceVersion(&validateResult, pApiVersion);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkEnumerateInstanceVersion(vkChannel, pApiVersion);
    return res;
}

static void validate_vkBindBufferMemory2(
    VkResult* validateResult,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindBufferMemoryInfo* pBindInfos)
{
}

static VkResult encode_vkBindBufferMemory2(
    void* vkChannel,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindBufferMemoryInfo* pBindInfos)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkBindBufferMemory2(
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindBufferMemoryInfo* pBindInfos)

{
    // This is the body for vkBindBufferMemory2
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindBufferMemory2(&validateResult, device, bindInfoCount, pBindInfos);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkBindBufferMemory2(vkChannel, device, bindInfoCount, pBindInfos);
    return res;
}

static void validate_vkBindImageMemory2(
    VkResult* validateResult,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindImageMemoryInfo* pBindInfos)
{
}

static VkResult encode_vkBindImageMemory2(
    void* vkChannel,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindImageMemoryInfo* pBindInfos)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkBindImageMemory2(
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindImageMemoryInfo* pBindInfos)

{
    // This is the body for vkBindImageMemory2
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindImageMemory2(&validateResult, device, bindInfoCount, pBindInfos);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkBindImageMemory2(vkChannel, device, bindInfoCount, pBindInfos);
    return res;
}

static void validate_vkGetDeviceGroupPeerMemoryFeatures(
    VkResult* validateResult,
    VkDevice device,
    uint32_t heapIndex,
    uint32_t localDeviceIndex,
    uint32_t remoteDeviceIndex,
    VkPeerMemoryFeatureFlags* pPeerMemoryFeatures)
{
}

static void encode_vkGetDeviceGroupPeerMemoryFeatures(
    void* vkChannel,
    VkDevice device,
    uint32_t heapIndex,
    uint32_t localDeviceIndex,
    uint32_t remoteDeviceIndex,
    VkPeerMemoryFeatureFlags* pPeerMemoryFeatures)
{
}

void goldfish_frontend_vkGetDeviceGroupPeerMemoryFeatures(
    VkDevice device,
    uint32_t heapIndex,
    uint32_t localDeviceIndex,
    uint32_t remoteDeviceIndex,
    VkPeerMemoryFeatureFlags* pPeerMemoryFeatures)

{
    // This is the body for vkGetDeviceGroupPeerMemoryFeatures
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceGroupPeerMemoryFeatures(&validateResult, device, heapIndex, localDeviceIndex, remoteDeviceIndex, pPeerMemoryFeatures);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetDeviceGroupPeerMemoryFeatures(vkChannel, device, heapIndex, localDeviceIndex, remoteDeviceIndex, pPeerMemoryFeatures);
}

static void validate_vkCmdSetDeviceMask(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t deviceMask)
{
}

static void encode_vkCmdSetDeviceMask(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t deviceMask)
{
}

void goldfish_frontend_vkCmdSetDeviceMask(
    VkCommandBuffer commandBuffer,
    uint32_t deviceMask)

{
    // This is the body for vkCmdSetDeviceMask
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetDeviceMask(&validateResult, commandBuffer, deviceMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetDeviceMask(vkChannel, commandBuffer, deviceMask);
}

static void validate_vkCmdDispatchBase(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t baseGroupX,
    uint32_t baseGroupY,
    uint32_t baseGroupZ,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
}

static void encode_vkCmdDispatchBase(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t baseGroupX,
    uint32_t baseGroupY,
    uint32_t baseGroupZ,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
}

void goldfish_frontend_vkCmdDispatchBase(
    VkCommandBuffer commandBuffer,
    uint32_t baseGroupX,
    uint32_t baseGroupY,
    uint32_t baseGroupZ,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)

{
    // This is the body for vkCmdDispatchBase
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDispatchBase(&validateResult, commandBuffer, baseGroupX, baseGroupY, baseGroupZ, groupCountX, groupCountY, groupCountZ);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDispatchBase(vkChannel, commandBuffer, baseGroupX, baseGroupY, baseGroupZ, groupCountX, groupCountY, groupCountZ);
}

static void validate_vkEnumeratePhysicalDeviceGroups(
    VkResult* validateResult,
    VkInstance instance,
    uint32_t* pPhysicalDeviceGroupCount,
    VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties)
{
}

static VkResult encode_vkEnumeratePhysicalDeviceGroups(
    void* vkChannel,
    VkInstance instance,
    uint32_t* pPhysicalDeviceGroupCount,
    VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkEnumeratePhysicalDeviceGroups(
    VkInstance instance,
    uint32_t* pPhysicalDeviceGroupCount,
    VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties)

{
    // This is the body for vkEnumeratePhysicalDeviceGroups
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumeratePhysicalDeviceGroups(&validateResult, instance, pPhysicalDeviceGroupCount, pPhysicalDeviceGroupProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkEnumeratePhysicalDeviceGroups(vkChannel, instance, pPhysicalDeviceGroupCount, pPhysicalDeviceGroupProperties);
    return res;
}

static void validate_vkGetImageMemoryRequirements2(
    VkResult* validateResult,
    VkDevice device,
    const VkImageMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

static void encode_vkGetImageMemoryRequirements2(
    void* vkChannel,
    VkDevice device,
    const VkImageMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

void goldfish_frontend_vkGetImageMemoryRequirements2(
    VkDevice device,
    const VkImageMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)

{
    // This is the body for vkGetImageMemoryRequirements2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageMemoryRequirements2(&validateResult, device, pInfo, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetImageMemoryRequirements2(vkChannel, device, pInfo, pMemoryRequirements);
}

static void validate_vkGetBufferMemoryRequirements2(
    VkResult* validateResult,
    VkDevice device,
    const VkBufferMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

static void encode_vkGetBufferMemoryRequirements2(
    void* vkChannel,
    VkDevice device,
    const VkBufferMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

void goldfish_frontend_vkGetBufferMemoryRequirements2(
    VkDevice device,
    const VkBufferMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)

{
    // This is the body for vkGetBufferMemoryRequirements2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetBufferMemoryRequirements2(&validateResult, device, pInfo, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetBufferMemoryRequirements2(vkChannel, device, pInfo, pMemoryRequirements);
}

static void validate_vkGetImageSparseMemoryRequirements2(
    VkResult* validateResult,
    VkDevice device,
    const VkImageSparseMemoryRequirementsInfo2* pInfo,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements2* pSparseMemoryRequirements)
{
}

static void encode_vkGetImageSparseMemoryRequirements2(
    void* vkChannel,
    VkDevice device,
    const VkImageSparseMemoryRequirementsInfo2* pInfo,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements2* pSparseMemoryRequirements)
{
}

void goldfish_frontend_vkGetImageSparseMemoryRequirements2(
    VkDevice device,
    const VkImageSparseMemoryRequirementsInfo2* pInfo,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements2* pSparseMemoryRequirements)

{
    // This is the body for vkGetImageSparseMemoryRequirements2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageSparseMemoryRequirements2(&validateResult, device, pInfo, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetImageSparseMemoryRequirements2(vkChannel, device, pInfo, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
}

static void validate_vkGetPhysicalDeviceFeatures2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures2* pFeatures)
{
}

static void encode_vkGetPhysicalDeviceFeatures2(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures2* pFeatures)
{
}

void goldfish_frontend_vkGetPhysicalDeviceFeatures2(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures2* pFeatures)

{
    // This is the body for vkGetPhysicalDeviceFeatures2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFeatures2(&validateResult, physicalDevice, pFeatures);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceFeatures2(vkChannel, physicalDevice, pFeatures);
}

static void validate_vkGetPhysicalDeviceProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties2* pProperties)
{
}

static void encode_vkGetPhysicalDeviceProperties2(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties2* pProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceProperties2(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties2* pProperties)

{
    // This is the body for vkGetPhysicalDeviceProperties2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceProperties2(&validateResult, physicalDevice, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceProperties2(vkChannel, physicalDevice, pProperties);
}

static void validate_vkGetPhysicalDeviceFormatProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties2* pFormatProperties)
{
}

static void encode_vkGetPhysicalDeviceFormatProperties2(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties2* pFormatProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceFormatProperties2(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties2* pFormatProperties)

{
    // This is the body for vkGetPhysicalDeviceFormatProperties2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFormatProperties2(&validateResult, physicalDevice, format, pFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceFormatProperties2(vkChannel, physicalDevice, format, pFormatProperties);
}

static void validate_vkGetPhysicalDeviceImageFormatProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo,
    VkImageFormatProperties2* pImageFormatProperties)
{
}

static VkResult encode_vkGetPhysicalDeviceImageFormatProperties2(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo,
    VkImageFormatProperties2* pImageFormatProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceImageFormatProperties2(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo,
    VkImageFormatProperties2* pImageFormatProperties)

{
    // This is the body for vkGetPhysicalDeviceImageFormatProperties2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceImageFormatProperties2(&validateResult, physicalDevice, pImageFormatInfo, pImageFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceImageFormatProperties2(vkChannel, physicalDevice, pImageFormatInfo, pImageFormatProperties);
    return res;
}

static void validate_vkGetPhysicalDeviceQueueFamilyProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties2* pQueueFamilyProperties)
{
}

static void encode_vkGetPhysicalDeviceQueueFamilyProperties2(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties2* pQueueFamilyProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceQueueFamilyProperties2(
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties2* pQueueFamilyProperties)

{
    // This is the body for vkGetPhysicalDeviceQueueFamilyProperties2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceQueueFamilyProperties2(&validateResult, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceQueueFamilyProperties2(vkChannel, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
}

static void validate_vkGetPhysicalDeviceMemoryProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties2* pMemoryProperties)
{
}

static void encode_vkGetPhysicalDeviceMemoryProperties2(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties2* pMemoryProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceMemoryProperties2(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties2* pMemoryProperties)

{
    // This is the body for vkGetPhysicalDeviceMemoryProperties2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceMemoryProperties2(&validateResult, physicalDevice, pMemoryProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceMemoryProperties2(vkChannel, physicalDevice, pMemoryProperties);
}

static void validate_vkGetPhysicalDeviceSparseImageFormatProperties2(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties2* pProperties)
{
}

static void encode_vkGetPhysicalDeviceSparseImageFormatProperties2(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties2* pProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceSparseImageFormatProperties2(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties2* pProperties)

{
    // This is the body for vkGetPhysicalDeviceSparseImageFormatProperties2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSparseImageFormatProperties2(&validateResult, physicalDevice, pFormatInfo, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceSparseImageFormatProperties2(vkChannel, physicalDevice, pFormatInfo, pPropertyCount, pProperties);
}

static void validate_vkTrimCommandPool(
    VkResult* validateResult,
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolTrimFlags flags)
{
}

static void encode_vkTrimCommandPool(
    void* vkChannel,
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolTrimFlags flags)
{
}

void goldfish_frontend_vkTrimCommandPool(
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolTrimFlags flags)

{
    // This is the body for vkTrimCommandPool
    VkResult validateResult = VK_SUCCESS;
    validate_vkTrimCommandPool(&validateResult, device, commandPool, flags);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkTrimCommandPool(vkChannel, device, commandPool, flags);
}

static void validate_vkGetDeviceQueue2(
    VkResult* validateResult,
    VkDevice device,
    const VkDeviceQueueInfo2* pQueueInfo,
    VkQueue* pQueue)
{
}

static void encode_vkGetDeviceQueue2(
    void* vkChannel,
    VkDevice device,
    const VkDeviceQueueInfo2* pQueueInfo,
    VkQueue* pQueue)
{
}

void goldfish_frontend_vkGetDeviceQueue2(
    VkDevice device,
    const VkDeviceQueueInfo2* pQueueInfo,
    VkQueue* pQueue)

{
    // This is the body for vkGetDeviceQueue2
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceQueue2(&validateResult, device, pQueueInfo, pQueue);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetDeviceQueue2(vkChannel, device, pQueueInfo, pQueue);
}

static void validate_vkCreateSamplerYcbcrConversion(
    VkResult* validateResult,
    VkDevice device,
    const VkSamplerYcbcrConversionCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSamplerYcbcrConversion* pYcbcrConversion)
{
}

static VkResult encode_vkCreateSamplerYcbcrConversion(
    void* vkChannel,
    VkDevice device,
    const VkSamplerYcbcrConversionCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSamplerYcbcrConversion* pYcbcrConversion)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateSamplerYcbcrConversion(
    VkDevice device,
    const VkSamplerYcbcrConversionCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSamplerYcbcrConversion* pYcbcrConversion)

{
    // This is the body for vkCreateSamplerYcbcrConversion
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSamplerYcbcrConversion(&validateResult, device, pCreateInfo, pAllocator, pYcbcrConversion);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateSamplerYcbcrConversion(vkChannel, device, pCreateInfo, pAllocator, pYcbcrConversion);
    return res;
}

static void validate_vkDestroySamplerYcbcrConversion(
    VkResult* validateResult,
    VkDevice device,
    VkSamplerYcbcrConversion ycbcrConversion,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroySamplerYcbcrConversion(
    void* vkChannel,
    VkDevice device,
    VkSamplerYcbcrConversion ycbcrConversion,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroySamplerYcbcrConversion(
    VkDevice device,
    VkSamplerYcbcrConversion ycbcrConversion,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroySamplerYcbcrConversion
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySamplerYcbcrConversion(&validateResult, device, ycbcrConversion, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroySamplerYcbcrConversion(vkChannel, device, ycbcrConversion, pAllocator);
}

static void validate_vkCreateDescriptorUpdateTemplate(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorUpdateTemplateCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorUpdateTemplate* pDescriptorUpdateTemplate)
{
}

static VkResult encode_vkCreateDescriptorUpdateTemplate(
    void* vkChannel,
    VkDevice device,
    const VkDescriptorUpdateTemplateCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorUpdateTemplate* pDescriptorUpdateTemplate)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateDescriptorUpdateTemplate(
    VkDevice device,
    const VkDescriptorUpdateTemplateCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorUpdateTemplate* pDescriptorUpdateTemplate)

{
    // This is the body for vkCreateDescriptorUpdateTemplate
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDescriptorUpdateTemplate(&validateResult, device, pCreateInfo, pAllocator, pDescriptorUpdateTemplate);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateDescriptorUpdateTemplate(vkChannel, device, pCreateInfo, pAllocator, pDescriptorUpdateTemplate);
    return res;
}

static void validate_vkDestroyDescriptorUpdateTemplate(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyDescriptorUpdateTemplate(
    void* vkChannel,
    VkDevice device,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyDescriptorUpdateTemplate(
    VkDevice device,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyDescriptorUpdateTemplate
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDescriptorUpdateTemplate(&validateResult, device, descriptorUpdateTemplate, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyDescriptorUpdateTemplate(vkChannel, device, descriptorUpdateTemplate, pAllocator);
}

static void validate_vkUpdateDescriptorSetWithTemplate(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorSet descriptorSet,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const void* pData)
{
}

static void encode_vkUpdateDescriptorSetWithTemplate(
    void* vkChannel,
    VkDevice device,
    VkDescriptorSet descriptorSet,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const void* pData)
{
}

void goldfish_frontend_vkUpdateDescriptorSetWithTemplate(
    VkDevice device,
    VkDescriptorSet descriptorSet,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const void* pData)

{
    // This is the body for vkUpdateDescriptorSetWithTemplate
    VkResult validateResult = VK_SUCCESS;
    validate_vkUpdateDescriptorSetWithTemplate(&validateResult, device, descriptorSet, descriptorUpdateTemplate, pData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkUpdateDescriptorSetWithTemplate(vkChannel, device, descriptorSet, descriptorUpdateTemplate, pData);
}

static void validate_vkGetPhysicalDeviceExternalBufferProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo,
    VkExternalBufferProperties* pExternalBufferProperties)
{
}

static void encode_vkGetPhysicalDeviceExternalBufferProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo,
    VkExternalBufferProperties* pExternalBufferProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceExternalBufferProperties(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo,
    VkExternalBufferProperties* pExternalBufferProperties)

{
    // This is the body for vkGetPhysicalDeviceExternalBufferProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalBufferProperties(&validateResult, physicalDevice, pExternalBufferInfo, pExternalBufferProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceExternalBufferProperties(vkChannel, physicalDevice, pExternalBufferInfo, pExternalBufferProperties);
}

static void validate_vkGetPhysicalDeviceExternalFenceProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo,
    VkExternalFenceProperties* pExternalFenceProperties)
{
}

static void encode_vkGetPhysicalDeviceExternalFenceProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo,
    VkExternalFenceProperties* pExternalFenceProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceExternalFenceProperties(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo,
    VkExternalFenceProperties* pExternalFenceProperties)

{
    // This is the body for vkGetPhysicalDeviceExternalFenceProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalFenceProperties(&validateResult, physicalDevice, pExternalFenceInfo, pExternalFenceProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceExternalFenceProperties(vkChannel, physicalDevice, pExternalFenceInfo, pExternalFenceProperties);
}

static void validate_vkGetPhysicalDeviceExternalSemaphoreProperties(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo,
    VkExternalSemaphoreProperties* pExternalSemaphoreProperties)
{
}

static void encode_vkGetPhysicalDeviceExternalSemaphoreProperties(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo,
    VkExternalSemaphoreProperties* pExternalSemaphoreProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceExternalSemaphoreProperties(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo,
    VkExternalSemaphoreProperties* pExternalSemaphoreProperties)

{
    // This is the body for vkGetPhysicalDeviceExternalSemaphoreProperties
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalSemaphoreProperties(&validateResult, physicalDevice, pExternalSemaphoreInfo, pExternalSemaphoreProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceExternalSemaphoreProperties(vkChannel, physicalDevice, pExternalSemaphoreInfo, pExternalSemaphoreProperties);
}

static void validate_vkGetDescriptorSetLayoutSupport(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    VkDescriptorSetLayoutSupport* pSupport)
{
}

static void encode_vkGetDescriptorSetLayoutSupport(
    void* vkChannel,
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    VkDescriptorSetLayoutSupport* pSupport)
{
}

void goldfish_frontend_vkGetDescriptorSetLayoutSupport(
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    VkDescriptorSetLayoutSupport* pSupport)

{
    // This is the body for vkGetDescriptorSetLayoutSupport
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDescriptorSetLayoutSupport(&validateResult, device, pCreateInfo, pSupport);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetDescriptorSetLayoutSupport(vkChannel, device, pCreateInfo, pSupport);
}

#endif

#ifdef VK_KHR_surface

static void validate_vkDestroySurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    VkSurfaceKHR surface,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroySurfaceKHR(
    void* vkChannel,
    VkInstance instance,
    VkSurfaceKHR surface,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroySurfaceKHR(
    VkInstance instance,
    VkSurfaceKHR surface,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroySurfaceKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySurfaceKHR(&validateResult, instance, surface, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroySurfaceKHR(vkChannel, instance, surface, pAllocator);
}

static void validate_vkGetPhysicalDeviceSurfaceSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    VkSurfaceKHR surface,
    VkBool32* pSupported)
{
}

static VkResult encode_vkGetPhysicalDeviceSurfaceSupportKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    VkSurfaceKHR surface,
    VkBool32* pSupported)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    VkSurfaceKHR surface,
    VkBool32* pSupported)

{
    // This is the body for vkGetPhysicalDeviceSurfaceSupportKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceSupportKHR(&validateResult, physicalDevice, queueFamilyIndex, surface, pSupported);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceSupportKHR(vkChannel, physicalDevice, queueFamilyIndex, surface, pSupported);
    return res;
}

static void validate_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    VkSurfaceCapabilitiesKHR* pSurfaceCapabilities)
{
}

static VkResult encode_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    VkSurfaceCapabilitiesKHR* pSurfaceCapabilities)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    VkSurfaceCapabilitiesKHR* pSurfaceCapabilities)

{
    // This is the body for vkGetPhysicalDeviceSurfaceCapabilitiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(&validateResult, physicalDevice, surface, pSurfaceCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(vkChannel, physicalDevice, surface, pSurfaceCapabilities);
    return res;
}

static void validate_vkGetPhysicalDeviceSurfaceFormatsKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pSurfaceFormatCount,
    VkSurfaceFormatKHR* pSurfaceFormats)
{
}

static VkResult encode_vkGetPhysicalDeviceSurfaceFormatsKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pSurfaceFormatCount,
    VkSurfaceFormatKHR* pSurfaceFormats)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceFormatsKHR(
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pSurfaceFormatCount,
    VkSurfaceFormatKHR* pSurfaceFormats)

{
    // This is the body for vkGetPhysicalDeviceSurfaceFormatsKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceFormatsKHR(&validateResult, physicalDevice, surface, pSurfaceFormatCount, pSurfaceFormats);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceFormatsKHR(vkChannel, physicalDevice, surface, pSurfaceFormatCount, pSurfaceFormats);
    return res;
}

static void validate_vkGetPhysicalDeviceSurfacePresentModesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pPresentModeCount,
    VkPresentModeKHR* pPresentModes)
{
}

static VkResult encode_vkGetPhysicalDeviceSurfacePresentModesKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pPresentModeCount,
    VkPresentModeKHR* pPresentModes)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceSurfacePresentModesKHR(
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pPresentModeCount,
    VkPresentModeKHR* pPresentModes)

{
    // This is the body for vkGetPhysicalDeviceSurfacePresentModesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfacePresentModesKHR(&validateResult, physicalDevice, surface, pPresentModeCount, pPresentModes);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfacePresentModesKHR(vkChannel, physicalDevice, surface, pPresentModeCount, pPresentModes);
    return res;
}

#endif

#ifdef VK_KHR_swapchain

static void validate_vkCreateSwapchainKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkSwapchainCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSwapchainKHR* pSwapchain)
{
}

static VkResult encode_vkCreateSwapchainKHR(
    void* vkChannel,
    VkDevice device,
    const VkSwapchainCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSwapchainKHR* pSwapchain)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateSwapchainKHR(
    VkDevice device,
    const VkSwapchainCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSwapchainKHR* pSwapchain)

{
    // This is the body for vkCreateSwapchainKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSwapchainKHR(&validateResult, device, pCreateInfo, pAllocator, pSwapchain);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateSwapchainKHR(vkChannel, device, pCreateInfo, pAllocator, pSwapchain);
    return res;
}

static void validate_vkDestroySwapchainKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroySwapchainKHR(
    void* vkChannel,
    VkDevice device,
    VkSwapchainKHR swapchain,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroySwapchainKHR(
    VkDevice device,
    VkSwapchainKHR swapchain,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroySwapchainKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySwapchainKHR(&validateResult, device, swapchain, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroySwapchainKHR(vkChannel, device, swapchain, pAllocator);
}

static void validate_vkGetSwapchainImagesKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint32_t* pSwapchainImageCount,
    VkImage* pSwapchainImages)
{
}

static VkResult encode_vkGetSwapchainImagesKHR(
    void* vkChannel,
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint32_t* pSwapchainImageCount,
    VkImage* pSwapchainImages)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetSwapchainImagesKHR(
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint32_t* pSwapchainImageCount,
    VkImage* pSwapchainImages)

{
    // This is the body for vkGetSwapchainImagesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetSwapchainImagesKHR(&validateResult, device, swapchain, pSwapchainImageCount, pSwapchainImages);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetSwapchainImagesKHR(vkChannel, device, swapchain, pSwapchainImageCount, pSwapchainImages);
    return res;
}

static void validate_vkAcquireNextImageKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint64_t timeout,
    VkSemaphore semaphore,
    VkFence fence,
    uint32_t* pImageIndex)
{
}

static VkResult encode_vkAcquireNextImageKHR(
    void* vkChannel,
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint64_t timeout,
    VkSemaphore semaphore,
    VkFence fence,
    uint32_t* pImageIndex)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkAcquireNextImageKHR(
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint64_t timeout,
    VkSemaphore semaphore,
    VkFence fence,
    uint32_t* pImageIndex)

{
    // This is the body for vkAcquireNextImageKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkAcquireNextImageKHR(&validateResult, device, swapchain, timeout, semaphore, fence, pImageIndex);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkAcquireNextImageKHR(vkChannel, device, swapchain, timeout, semaphore, fence, pImageIndex);
    return res;
}

static void validate_vkQueuePresentKHR(
    VkResult* validateResult,
    VkQueue queue,
    const VkPresentInfoKHR* pPresentInfo)
{
}

static VkResult encode_vkQueuePresentKHR(
    void* vkChannel,
    VkQueue queue,
    const VkPresentInfoKHR* pPresentInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkQueuePresentKHR(
    VkQueue queue,
    const VkPresentInfoKHR* pPresentInfo)

{
    // This is the body for vkQueuePresentKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueuePresentKHR(&validateResult, queue, pPresentInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkQueuePresentKHR(vkChannel, queue, pPresentInfo);
    return res;
}

static void validate_vkGetDeviceGroupPresentCapabilitiesKHR(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceGroupPresentCapabilitiesKHR* pDeviceGroupPresentCapabilities)
{
}

static VkResult encode_vkGetDeviceGroupPresentCapabilitiesKHR(
    void* vkChannel,
    VkDevice device,
    VkDeviceGroupPresentCapabilitiesKHR* pDeviceGroupPresentCapabilities)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetDeviceGroupPresentCapabilitiesKHR(
    VkDevice device,
    VkDeviceGroupPresentCapabilitiesKHR* pDeviceGroupPresentCapabilities)

{
    // This is the body for vkGetDeviceGroupPresentCapabilitiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceGroupPresentCapabilitiesKHR(&validateResult, device, pDeviceGroupPresentCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetDeviceGroupPresentCapabilitiesKHR(vkChannel, device, pDeviceGroupPresentCapabilities);
    return res;
}

static void validate_vkGetDeviceGroupSurfacePresentModesKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSurfaceKHR surface,
    VkDeviceGroupPresentModeFlagsKHR* pModes)
{
}

static VkResult encode_vkGetDeviceGroupSurfacePresentModesKHR(
    void* vkChannel,
    VkDevice device,
    VkSurfaceKHR surface,
    VkDeviceGroupPresentModeFlagsKHR* pModes)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetDeviceGroupSurfacePresentModesKHR(
    VkDevice device,
    VkSurfaceKHR surface,
    VkDeviceGroupPresentModeFlagsKHR* pModes)

{
    // This is the body for vkGetDeviceGroupSurfacePresentModesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceGroupSurfacePresentModesKHR(&validateResult, device, surface, pModes);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetDeviceGroupSurfacePresentModesKHR(vkChannel, device, surface, pModes);
    return res;
}

static void validate_vkGetPhysicalDevicePresentRectanglesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pRectCount,
    VkRect2D* pRects)
{
}

static VkResult encode_vkGetPhysicalDevicePresentRectanglesKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pRectCount,
    VkRect2D* pRects)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDevicePresentRectanglesKHR(
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    uint32_t* pRectCount,
    VkRect2D* pRects)

{
    // This is the body for vkGetPhysicalDevicePresentRectanglesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDevicePresentRectanglesKHR(&validateResult, physicalDevice, surface, pRectCount, pRects);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDevicePresentRectanglesKHR(vkChannel, physicalDevice, surface, pRectCount, pRects);
    return res;
}

static void validate_vkAcquireNextImage2KHR(
    VkResult* validateResult,
    VkDevice device,
    const VkAcquireNextImageInfoKHR* pAcquireInfo,
    uint32_t* pImageIndex)
{
}

static VkResult encode_vkAcquireNextImage2KHR(
    void* vkChannel,
    VkDevice device,
    const VkAcquireNextImageInfoKHR* pAcquireInfo,
    uint32_t* pImageIndex)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkAcquireNextImage2KHR(
    VkDevice device,
    const VkAcquireNextImageInfoKHR* pAcquireInfo,
    uint32_t* pImageIndex)

{
    // This is the body for vkAcquireNextImage2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkAcquireNextImage2KHR(&validateResult, device, pAcquireInfo, pImageIndex);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkAcquireNextImage2KHR(vkChannel, device, pAcquireInfo, pImageIndex);
    return res;
}

#endif

#ifdef VK_KHR_display

static void validate_vkGetPhysicalDeviceDisplayPropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPropertiesKHR* pProperties)
{
}

static VkResult encode_vkGetPhysicalDeviceDisplayPropertiesKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPropertiesKHR* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceDisplayPropertiesKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPropertiesKHR* pProperties)

{
    // This is the body for vkGetPhysicalDeviceDisplayPropertiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceDisplayPropertiesKHR(&validateResult, physicalDevice, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceDisplayPropertiesKHR(vkChannel, physicalDevice, pPropertyCount, pProperties);
    return res;
}

static void validate_vkGetPhysicalDeviceDisplayPlanePropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPlanePropertiesKHR* pProperties)
{
}

static VkResult encode_vkGetPhysicalDeviceDisplayPlanePropertiesKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPlanePropertiesKHR* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceDisplayPlanePropertiesKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPlanePropertiesKHR* pProperties)

{
    // This is the body for vkGetPhysicalDeviceDisplayPlanePropertiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceDisplayPlanePropertiesKHR(&validateResult, physicalDevice, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceDisplayPlanePropertiesKHR(vkChannel, physicalDevice, pPropertyCount, pProperties);
    return res;
}

static void validate_vkGetDisplayPlaneSupportedDisplaysKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t planeIndex,
    uint32_t* pDisplayCount,
    VkDisplayKHR* pDisplays)
{
}

static VkResult encode_vkGetDisplayPlaneSupportedDisplaysKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t planeIndex,
    uint32_t* pDisplayCount,
    VkDisplayKHR* pDisplays)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetDisplayPlaneSupportedDisplaysKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t planeIndex,
    uint32_t* pDisplayCount,
    VkDisplayKHR* pDisplays)

{
    // This is the body for vkGetDisplayPlaneSupportedDisplaysKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDisplayPlaneSupportedDisplaysKHR(&validateResult, physicalDevice, planeIndex, pDisplayCount, pDisplays);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetDisplayPlaneSupportedDisplaysKHR(vkChannel, physicalDevice, planeIndex, pDisplayCount, pDisplays);
    return res;
}

static void validate_vkGetDisplayModePropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    uint32_t* pPropertyCount,
    VkDisplayModePropertiesKHR* pProperties)
{
}

static VkResult encode_vkGetDisplayModePropertiesKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    uint32_t* pPropertyCount,
    VkDisplayModePropertiesKHR* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetDisplayModePropertiesKHR(
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    uint32_t* pPropertyCount,
    VkDisplayModePropertiesKHR* pProperties)

{
    // This is the body for vkGetDisplayModePropertiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDisplayModePropertiesKHR(&validateResult, physicalDevice, display, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetDisplayModePropertiesKHR(vkChannel, physicalDevice, display, pPropertyCount, pProperties);
    return res;
}

static void validate_vkCreateDisplayModeKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    const VkDisplayModeCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDisplayModeKHR* pMode)
{
}

static VkResult encode_vkCreateDisplayModeKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    const VkDisplayModeCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDisplayModeKHR* pMode)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateDisplayModeKHR(
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    const VkDisplayModeCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDisplayModeKHR* pMode)

{
    // This is the body for vkCreateDisplayModeKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDisplayModeKHR(&validateResult, physicalDevice, display, pCreateInfo, pAllocator, pMode);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateDisplayModeKHR(vkChannel, physicalDevice, display, pCreateInfo, pAllocator, pMode);
    return res;
}

static void validate_vkGetDisplayPlaneCapabilitiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDisplayModeKHR mode,
    uint32_t planeIndex,
    VkDisplayPlaneCapabilitiesKHR* pCapabilities)
{
}

static VkResult encode_vkGetDisplayPlaneCapabilitiesKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkDisplayModeKHR mode,
    uint32_t planeIndex,
    VkDisplayPlaneCapabilitiesKHR* pCapabilities)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetDisplayPlaneCapabilitiesKHR(
    VkPhysicalDevice physicalDevice,
    VkDisplayModeKHR mode,
    uint32_t planeIndex,
    VkDisplayPlaneCapabilitiesKHR* pCapabilities)

{
    // This is the body for vkGetDisplayPlaneCapabilitiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDisplayPlaneCapabilitiesKHR(&validateResult, physicalDevice, mode, planeIndex, pCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetDisplayPlaneCapabilitiesKHR(vkChannel, physicalDevice, mode, planeIndex, pCapabilities);
    return res;
}

static void validate_vkCreateDisplayPlaneSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkDisplaySurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

static VkResult encode_vkCreateDisplayPlaneSurfaceKHR(
    void* vkChannel,
    VkInstance instance,
    const VkDisplaySurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateDisplayPlaneSurfaceKHR(
    VkInstance instance,
    const VkDisplaySurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)

{
    // This is the body for vkCreateDisplayPlaneSurfaceKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDisplayPlaneSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateDisplayPlaneSurfaceKHR(vkChannel, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

#endif

#ifdef VK_KHR_display_swapchain

static void validate_vkCreateSharedSwapchainsKHR(
    VkResult* validateResult,
    VkDevice device,
    uint32_t swapchainCount,
    const VkSwapchainCreateInfoKHR* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkSwapchainKHR* pSwapchains)
{
}

static VkResult encode_vkCreateSharedSwapchainsKHR(
    void* vkChannel,
    VkDevice device,
    uint32_t swapchainCount,
    const VkSwapchainCreateInfoKHR* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkSwapchainKHR* pSwapchains)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateSharedSwapchainsKHR(
    VkDevice device,
    uint32_t swapchainCount,
    const VkSwapchainCreateInfoKHR* pCreateInfos,
    const VkAllocationCallbacks* pAllocator,
    VkSwapchainKHR* pSwapchains)

{
    // This is the body for vkCreateSharedSwapchainsKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSharedSwapchainsKHR(&validateResult, device, swapchainCount, pCreateInfos, pAllocator, pSwapchains);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateSharedSwapchainsKHR(vkChannel, device, swapchainCount, pCreateInfos, pAllocator, pSwapchains);
    return res;
}

#endif

#ifdef VK_KHR_xlib_surface

static void validate_vkCreateXlibSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkXlibSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

static VkResult encode_vkCreateXlibSurfaceKHR(
    void* vkChannel,
    VkInstance instance,
    const VkXlibSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateXlibSurfaceKHR(
    VkInstance instance,
    const VkXlibSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)

{
    // This is the body for vkCreateXlibSurfaceKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateXlibSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateXlibSurfaceKHR(vkChannel, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

static void validate_vkGetPhysicalDeviceXlibPresentationSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    Display* dpy,
    VisualID visualID)
{
}

static VkBool32 encode_vkGetPhysicalDeviceXlibPresentationSupportKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    Display* dpy,
    VisualID visualID)
{
    return (VkBool32)0;
}

VkBool32 goldfish_frontend_vkGetPhysicalDeviceXlibPresentationSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    Display* dpy,
    VisualID visualID)

{
    // This is the body for vkGetPhysicalDeviceXlibPresentationSupportKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceXlibPresentationSupportKHR(&validateResult, physicalDevice, queueFamilyIndex, dpy, visualID);
    if (validateResult != VK_SUCCESS)
    {
        return (VkBool32)0;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkBool32 res = encode_vkGetPhysicalDeviceXlibPresentationSupportKHR(vkChannel, physicalDevice, queueFamilyIndex, dpy, visualID);
    return res;
}

#endif

#ifdef VK_KHR_xcb_surface

static void validate_vkCreateXcbSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkXcbSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

static VkResult encode_vkCreateXcbSurfaceKHR(
    void* vkChannel,
    VkInstance instance,
    const VkXcbSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateXcbSurfaceKHR(
    VkInstance instance,
    const VkXcbSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)

{
    // This is the body for vkCreateXcbSurfaceKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateXcbSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateXcbSurfaceKHR(vkChannel, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

static void validate_vkGetPhysicalDeviceXcbPresentationSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    xcb_connection_t* connection,
    xcb_visualid_t visual_id)
{
}

static VkBool32 encode_vkGetPhysicalDeviceXcbPresentationSupportKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    xcb_connection_t* connection,
    xcb_visualid_t visual_id)
{
    return (VkBool32)0;
}

VkBool32 goldfish_frontend_vkGetPhysicalDeviceXcbPresentationSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    xcb_connection_t* connection,
    xcb_visualid_t visual_id)

{
    // This is the body for vkGetPhysicalDeviceXcbPresentationSupportKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceXcbPresentationSupportKHR(&validateResult, physicalDevice, queueFamilyIndex, connection, visual_id);
    if (validateResult != VK_SUCCESS)
    {
        return (VkBool32)0;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkBool32 res = encode_vkGetPhysicalDeviceXcbPresentationSupportKHR(vkChannel, physicalDevice, queueFamilyIndex, connection, visual_id);
    return res;
}

#endif

#ifdef VK_KHR_wayland_surface

static void validate_vkCreateWaylandSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkWaylandSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

static VkResult encode_vkCreateWaylandSurfaceKHR(
    void* vkChannel,
    VkInstance instance,
    const VkWaylandSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateWaylandSurfaceKHR(
    VkInstance instance,
    const VkWaylandSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)

{
    // This is the body for vkCreateWaylandSurfaceKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateWaylandSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateWaylandSurfaceKHR(vkChannel, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

static void validate_vkGetPhysicalDeviceWaylandPresentationSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    struct wl_display* display)
{
}

static VkBool32 encode_vkGetPhysicalDeviceWaylandPresentationSupportKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    struct wl_display* display)
{
    return (VkBool32)0;
}

VkBool32 goldfish_frontend_vkGetPhysicalDeviceWaylandPresentationSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    struct wl_display* display)

{
    // This is the body for vkGetPhysicalDeviceWaylandPresentationSupportKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceWaylandPresentationSupportKHR(&validateResult, physicalDevice, queueFamilyIndex, display);
    if (validateResult != VK_SUCCESS)
    {
        return (VkBool32)0;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkBool32 res = encode_vkGetPhysicalDeviceWaylandPresentationSupportKHR(vkChannel, physicalDevice, queueFamilyIndex, display);
    return res;
}

#endif

#ifdef VK_KHR_mir_surface

static void validate_vkCreateMirSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkMirSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

static VkResult encode_vkCreateMirSurfaceKHR(
    void* vkChannel,
    VkInstance instance,
    const VkMirSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateMirSurfaceKHR(
    VkInstance instance,
    const VkMirSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)

{
    // This is the body for vkCreateMirSurfaceKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateMirSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateMirSurfaceKHR(vkChannel, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

static void validate_vkGetPhysicalDeviceMirPresentationSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    MirConnection* connection)
{
}

static VkBool32 encode_vkGetPhysicalDeviceMirPresentationSupportKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    MirConnection* connection)
{
    return (VkBool32)0;
}

VkBool32 goldfish_frontend_vkGetPhysicalDeviceMirPresentationSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex,
    MirConnection* connection)

{
    // This is the body for vkGetPhysicalDeviceMirPresentationSupportKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceMirPresentationSupportKHR(&validateResult, physicalDevice, queueFamilyIndex, connection);
    if (validateResult != VK_SUCCESS)
    {
        return (VkBool32)0;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkBool32 res = encode_vkGetPhysicalDeviceMirPresentationSupportKHR(vkChannel, physicalDevice, queueFamilyIndex, connection);
    return res;
}

#endif

#ifdef VK_KHR_android_surface

static void validate_vkCreateAndroidSurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkAndroidSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

static VkResult encode_vkCreateAndroidSurfaceKHR(
    void* vkChannel,
    VkInstance instance,
    const VkAndroidSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateAndroidSurfaceKHR(
    VkInstance instance,
    const VkAndroidSurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)

{
    // This is the body for vkCreateAndroidSurfaceKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateAndroidSurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateAndroidSurfaceKHR(vkChannel, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

#endif

#ifdef VK_KHR_win32_surface

static void validate_vkCreateWin32SurfaceKHR(
    VkResult* validateResult,
    VkInstance instance,
    const VkWin32SurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

static VkResult encode_vkCreateWin32SurfaceKHR(
    void* vkChannel,
    VkInstance instance,
    const VkWin32SurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateWin32SurfaceKHR(
    VkInstance instance,
    const VkWin32SurfaceCreateInfoKHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)

{
    // This is the body for vkCreateWin32SurfaceKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateWin32SurfaceKHR(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateWin32SurfaceKHR(vkChannel, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

static void validate_vkGetPhysicalDeviceWin32PresentationSupportKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex)
{
}

static VkBool32 encode_vkGetPhysicalDeviceWin32PresentationSupportKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex)
{
    return (VkBool32)0;
}

VkBool32 goldfish_frontend_vkGetPhysicalDeviceWin32PresentationSupportKHR(
    VkPhysicalDevice physicalDevice,
    uint32_t queueFamilyIndex)

{
    // This is the body for vkGetPhysicalDeviceWin32PresentationSupportKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceWin32PresentationSupportKHR(&validateResult, physicalDevice, queueFamilyIndex);
    if (validateResult != VK_SUCCESS)
    {
        return (VkBool32)0;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkBool32 res = encode_vkGetPhysicalDeviceWin32PresentationSupportKHR(vkChannel, physicalDevice, queueFamilyIndex);
    return res;
}

#endif

#ifdef VK_KHR_sampler_mirror_clamp_to_edge

#endif

#ifdef VK_KHR_multiview

#endif

#ifdef VK_KHR_get_physical_device_properties2

static void validate_vkGetPhysicalDeviceFeatures2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures2* pFeatures)
{
}

static void encode_vkGetPhysicalDeviceFeatures2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures2* pFeatures)
{
}

void goldfish_frontend_vkGetPhysicalDeviceFeatures2KHR(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceFeatures2* pFeatures)

{
    // This is the body for vkGetPhysicalDeviceFeatures2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFeatures2KHR(&validateResult, physicalDevice, pFeatures);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceFeatures2KHR(vkChannel, physicalDevice, pFeatures);
}

static void validate_vkGetPhysicalDeviceProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties2* pProperties)
{
}

static void encode_vkGetPhysicalDeviceProperties2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties2* pProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceProperties2KHR(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceProperties2* pProperties)

{
    // This is the body for vkGetPhysicalDeviceProperties2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceProperties2KHR(&validateResult, physicalDevice, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceProperties2KHR(vkChannel, physicalDevice, pProperties);
}

static void validate_vkGetPhysicalDeviceFormatProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties2* pFormatProperties)
{
}

static void encode_vkGetPhysicalDeviceFormatProperties2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties2* pFormatProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceFormatProperties2KHR(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkFormatProperties2* pFormatProperties)

{
    // This is the body for vkGetPhysicalDeviceFormatProperties2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceFormatProperties2KHR(&validateResult, physicalDevice, format, pFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceFormatProperties2KHR(vkChannel, physicalDevice, format, pFormatProperties);
}

static void validate_vkGetPhysicalDeviceImageFormatProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo,
    VkImageFormatProperties2* pImageFormatProperties)
{
}

static VkResult encode_vkGetPhysicalDeviceImageFormatProperties2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo,
    VkImageFormatProperties2* pImageFormatProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceImageFormatProperties2KHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo,
    VkImageFormatProperties2* pImageFormatProperties)

{
    // This is the body for vkGetPhysicalDeviceImageFormatProperties2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceImageFormatProperties2KHR(&validateResult, physicalDevice, pImageFormatInfo, pImageFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceImageFormatProperties2KHR(vkChannel, physicalDevice, pImageFormatInfo, pImageFormatProperties);
    return res;
}

static void validate_vkGetPhysicalDeviceQueueFamilyProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties2* pQueueFamilyProperties)
{
}

static void encode_vkGetPhysicalDeviceQueueFamilyProperties2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties2* pQueueFamilyProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceQueueFamilyProperties2KHR(
    VkPhysicalDevice physicalDevice,
    uint32_t* pQueueFamilyPropertyCount,
    VkQueueFamilyProperties2* pQueueFamilyProperties)

{
    // This is the body for vkGetPhysicalDeviceQueueFamilyProperties2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceQueueFamilyProperties2KHR(&validateResult, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceQueueFamilyProperties2KHR(vkChannel, physicalDevice, pQueueFamilyPropertyCount, pQueueFamilyProperties);
}

static void validate_vkGetPhysicalDeviceMemoryProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties2* pMemoryProperties)
{
}

static void encode_vkGetPhysicalDeviceMemoryProperties2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties2* pMemoryProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceMemoryProperties2KHR(
    VkPhysicalDevice physicalDevice,
    VkPhysicalDeviceMemoryProperties2* pMemoryProperties)

{
    // This is the body for vkGetPhysicalDeviceMemoryProperties2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceMemoryProperties2KHR(&validateResult, physicalDevice, pMemoryProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceMemoryProperties2KHR(vkChannel, physicalDevice, pMemoryProperties);
}

static void validate_vkGetPhysicalDeviceSparseImageFormatProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties2* pProperties)
{
}

static void encode_vkGetPhysicalDeviceSparseImageFormatProperties2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties2* pProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceSparseImageFormatProperties2KHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo,
    uint32_t* pPropertyCount,
    VkSparseImageFormatProperties2* pProperties)

{
    // This is the body for vkGetPhysicalDeviceSparseImageFormatProperties2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSparseImageFormatProperties2KHR(&validateResult, physicalDevice, pFormatInfo, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceSparseImageFormatProperties2KHR(vkChannel, physicalDevice, pFormatInfo, pPropertyCount, pProperties);
}

#endif

#ifdef VK_KHR_device_group

static void validate_vkGetDeviceGroupPeerMemoryFeaturesKHR(
    VkResult* validateResult,
    VkDevice device,
    uint32_t heapIndex,
    uint32_t localDeviceIndex,
    uint32_t remoteDeviceIndex,
    VkPeerMemoryFeatureFlags* pPeerMemoryFeatures)
{
}

static void encode_vkGetDeviceGroupPeerMemoryFeaturesKHR(
    void* vkChannel,
    VkDevice device,
    uint32_t heapIndex,
    uint32_t localDeviceIndex,
    uint32_t remoteDeviceIndex,
    VkPeerMemoryFeatureFlags* pPeerMemoryFeatures)
{
}

void goldfish_frontend_vkGetDeviceGroupPeerMemoryFeaturesKHR(
    VkDevice device,
    uint32_t heapIndex,
    uint32_t localDeviceIndex,
    uint32_t remoteDeviceIndex,
    VkPeerMemoryFeatureFlags* pPeerMemoryFeatures)

{
    // This is the body for vkGetDeviceGroupPeerMemoryFeaturesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDeviceGroupPeerMemoryFeaturesKHR(&validateResult, device, heapIndex, localDeviceIndex, remoteDeviceIndex, pPeerMemoryFeatures);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetDeviceGroupPeerMemoryFeaturesKHR(vkChannel, device, heapIndex, localDeviceIndex, remoteDeviceIndex, pPeerMemoryFeatures);
}

static void validate_vkCmdSetDeviceMaskKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t deviceMask)
{
}

static void encode_vkCmdSetDeviceMaskKHR(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t deviceMask)
{
}

void goldfish_frontend_vkCmdSetDeviceMaskKHR(
    VkCommandBuffer commandBuffer,
    uint32_t deviceMask)

{
    // This is the body for vkCmdSetDeviceMaskKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetDeviceMaskKHR(&validateResult, commandBuffer, deviceMask);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetDeviceMaskKHR(vkChannel, commandBuffer, deviceMask);
}

static void validate_vkCmdDispatchBaseKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t baseGroupX,
    uint32_t baseGroupY,
    uint32_t baseGroupZ,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
}

static void encode_vkCmdDispatchBaseKHR(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t baseGroupX,
    uint32_t baseGroupY,
    uint32_t baseGroupZ,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)
{
}

void goldfish_frontend_vkCmdDispatchBaseKHR(
    VkCommandBuffer commandBuffer,
    uint32_t baseGroupX,
    uint32_t baseGroupY,
    uint32_t baseGroupZ,
    uint32_t groupCountX,
    uint32_t groupCountY,
    uint32_t groupCountZ)

{
    // This is the body for vkCmdDispatchBaseKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDispatchBaseKHR(&validateResult, commandBuffer, baseGroupX, baseGroupY, baseGroupZ, groupCountX, groupCountY, groupCountZ);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDispatchBaseKHR(vkChannel, commandBuffer, baseGroupX, baseGroupY, baseGroupZ, groupCountX, groupCountY, groupCountZ);
}

#endif

#ifdef VK_KHR_shader_draw_parameters

#endif

#ifdef VK_KHR_maintenance1

static void validate_vkTrimCommandPoolKHR(
    VkResult* validateResult,
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolTrimFlags flags)
{
}

static void encode_vkTrimCommandPoolKHR(
    void* vkChannel,
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolTrimFlags flags)
{
}

void goldfish_frontend_vkTrimCommandPoolKHR(
    VkDevice device,
    VkCommandPool commandPool,
    VkCommandPoolTrimFlags flags)

{
    // This is the body for vkTrimCommandPoolKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkTrimCommandPoolKHR(&validateResult, device, commandPool, flags);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkTrimCommandPoolKHR(vkChannel, device, commandPool, flags);
}

#endif

#ifdef VK_KHR_device_group_creation

static void validate_vkEnumeratePhysicalDeviceGroupsKHR(
    VkResult* validateResult,
    VkInstance instance,
    uint32_t* pPhysicalDeviceGroupCount,
    VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties)
{
}

static VkResult encode_vkEnumeratePhysicalDeviceGroupsKHR(
    void* vkChannel,
    VkInstance instance,
    uint32_t* pPhysicalDeviceGroupCount,
    VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkEnumeratePhysicalDeviceGroupsKHR(
    VkInstance instance,
    uint32_t* pPhysicalDeviceGroupCount,
    VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties)

{
    // This is the body for vkEnumeratePhysicalDeviceGroupsKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkEnumeratePhysicalDeviceGroupsKHR(&validateResult, instance, pPhysicalDeviceGroupCount, pPhysicalDeviceGroupProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkEnumeratePhysicalDeviceGroupsKHR(vkChannel, instance, pPhysicalDeviceGroupCount, pPhysicalDeviceGroupProperties);
    return res;
}

#endif

#ifdef VK_KHR_external_memory_capabilities

static void validate_vkGetPhysicalDeviceExternalBufferPropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo,
    VkExternalBufferProperties* pExternalBufferProperties)
{
}

static void encode_vkGetPhysicalDeviceExternalBufferPropertiesKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo,
    VkExternalBufferProperties* pExternalBufferProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceExternalBufferPropertiesKHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo,
    VkExternalBufferProperties* pExternalBufferProperties)

{
    // This is the body for vkGetPhysicalDeviceExternalBufferPropertiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalBufferPropertiesKHR(&validateResult, physicalDevice, pExternalBufferInfo, pExternalBufferProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceExternalBufferPropertiesKHR(vkChannel, physicalDevice, pExternalBufferInfo, pExternalBufferProperties);
}

#endif

#ifdef VK_KHR_external_memory

#endif

#ifdef VK_KHR_external_memory_win32

static void validate_vkGetMemoryWin32HandleKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkMemoryGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
}

static VkResult encode_vkGetMemoryWin32HandleKHR(
    void* vkChannel,
    VkDevice device,
    const VkMemoryGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetMemoryWin32HandleKHR(
    VkDevice device,
    const VkMemoryGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)

{
    // This is the body for vkGetMemoryWin32HandleKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryWin32HandleKHR(&validateResult, device, pGetWin32HandleInfo, pHandle);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetMemoryWin32HandleKHR(vkChannel, device, pGetWin32HandleInfo, pHandle);
    return res;
}

static void validate_vkGetMemoryWin32HandlePropertiesKHR(
    VkResult* validateResult,
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    HANDLE handle,
    VkMemoryWin32HandlePropertiesKHR* pMemoryWin32HandleProperties)
{
}

static VkResult encode_vkGetMemoryWin32HandlePropertiesKHR(
    void* vkChannel,
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    HANDLE handle,
    VkMemoryWin32HandlePropertiesKHR* pMemoryWin32HandleProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetMemoryWin32HandlePropertiesKHR(
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    HANDLE handle,
    VkMemoryWin32HandlePropertiesKHR* pMemoryWin32HandleProperties)

{
    // This is the body for vkGetMemoryWin32HandlePropertiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryWin32HandlePropertiesKHR(&validateResult, device, handleType, handle, pMemoryWin32HandleProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetMemoryWin32HandlePropertiesKHR(vkChannel, device, handleType, handle, pMemoryWin32HandleProperties);
    return res;
}

#endif

#ifdef VK_KHR_external_memory_fd

static void validate_vkGetMemoryFdKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkMemoryGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
}

static VkResult encode_vkGetMemoryFdKHR(
    void* vkChannel,
    VkDevice device,
    const VkMemoryGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetMemoryFdKHR(
    VkDevice device,
    const VkMemoryGetFdInfoKHR* pGetFdInfo,
    int* pFd)

{
    // This is the body for vkGetMemoryFdKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryFdKHR(&validateResult, device, pGetFdInfo, pFd);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetMemoryFdKHR(vkChannel, device, pGetFdInfo, pFd);
    return res;
}

static void validate_vkGetMemoryFdPropertiesKHR(
    VkResult* validateResult,
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    int fd,
    VkMemoryFdPropertiesKHR* pMemoryFdProperties)
{
}

static VkResult encode_vkGetMemoryFdPropertiesKHR(
    void* vkChannel,
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    int fd,
    VkMemoryFdPropertiesKHR* pMemoryFdProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetMemoryFdPropertiesKHR(
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    int fd,
    VkMemoryFdPropertiesKHR* pMemoryFdProperties)

{
    // This is the body for vkGetMemoryFdPropertiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryFdPropertiesKHR(&validateResult, device, handleType, fd, pMemoryFdProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetMemoryFdPropertiesKHR(vkChannel, device, handleType, fd, pMemoryFdProperties);
    return res;
}

#endif

#ifdef VK_KHR_win32_keyed_mutex

#endif

#ifdef VK_KHR_external_semaphore_capabilities

static void validate_vkGetPhysicalDeviceExternalSemaphorePropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo,
    VkExternalSemaphoreProperties* pExternalSemaphoreProperties)
{
}

static void encode_vkGetPhysicalDeviceExternalSemaphorePropertiesKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo,
    VkExternalSemaphoreProperties* pExternalSemaphoreProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceExternalSemaphorePropertiesKHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo,
    VkExternalSemaphoreProperties* pExternalSemaphoreProperties)

{
    // This is the body for vkGetPhysicalDeviceExternalSemaphorePropertiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalSemaphorePropertiesKHR(&validateResult, physicalDevice, pExternalSemaphoreInfo, pExternalSemaphoreProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceExternalSemaphorePropertiesKHR(vkChannel, physicalDevice, pExternalSemaphoreInfo, pExternalSemaphoreProperties);
}

#endif

#ifdef VK_KHR_external_semaphore

#endif

#ifdef VK_KHR_external_semaphore_win32

static void validate_vkImportSemaphoreWin32HandleKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImportSemaphoreWin32HandleInfoKHR* pImportSemaphoreWin32HandleInfo)
{
}

static VkResult encode_vkImportSemaphoreWin32HandleKHR(
    void* vkChannel,
    VkDevice device,
    const VkImportSemaphoreWin32HandleInfoKHR* pImportSemaphoreWin32HandleInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkImportSemaphoreWin32HandleKHR(
    VkDevice device,
    const VkImportSemaphoreWin32HandleInfoKHR* pImportSemaphoreWin32HandleInfo)

{
    // This is the body for vkImportSemaphoreWin32HandleKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkImportSemaphoreWin32HandleKHR(&validateResult, device, pImportSemaphoreWin32HandleInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkImportSemaphoreWin32HandleKHR(vkChannel, device, pImportSemaphoreWin32HandleInfo);
    return res;
}

static void validate_vkGetSemaphoreWin32HandleKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkSemaphoreGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
}

static VkResult encode_vkGetSemaphoreWin32HandleKHR(
    void* vkChannel,
    VkDevice device,
    const VkSemaphoreGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetSemaphoreWin32HandleKHR(
    VkDevice device,
    const VkSemaphoreGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)

{
    // This is the body for vkGetSemaphoreWin32HandleKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetSemaphoreWin32HandleKHR(&validateResult, device, pGetWin32HandleInfo, pHandle);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetSemaphoreWin32HandleKHR(vkChannel, device, pGetWin32HandleInfo, pHandle);
    return res;
}

#endif

#ifdef VK_KHR_external_semaphore_fd

static void validate_vkImportSemaphoreFdKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImportSemaphoreFdInfoKHR* pImportSemaphoreFdInfo)
{
}

static VkResult encode_vkImportSemaphoreFdKHR(
    void* vkChannel,
    VkDevice device,
    const VkImportSemaphoreFdInfoKHR* pImportSemaphoreFdInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkImportSemaphoreFdKHR(
    VkDevice device,
    const VkImportSemaphoreFdInfoKHR* pImportSemaphoreFdInfo)

{
    // This is the body for vkImportSemaphoreFdKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkImportSemaphoreFdKHR(&validateResult, device, pImportSemaphoreFdInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkImportSemaphoreFdKHR(vkChannel, device, pImportSemaphoreFdInfo);
    return res;
}

static void validate_vkGetSemaphoreFdKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkSemaphoreGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
}

static VkResult encode_vkGetSemaphoreFdKHR(
    void* vkChannel,
    VkDevice device,
    const VkSemaphoreGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetSemaphoreFdKHR(
    VkDevice device,
    const VkSemaphoreGetFdInfoKHR* pGetFdInfo,
    int* pFd)

{
    // This is the body for vkGetSemaphoreFdKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetSemaphoreFdKHR(&validateResult, device, pGetFdInfo, pFd);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetSemaphoreFdKHR(vkChannel, device, pGetFdInfo, pFd);
    return res;
}

#endif

#ifdef VK_KHR_push_descriptor

static void validate_vkCmdPushDescriptorSetKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipelineLayout layout,
    uint32_t set,
    uint32_t descriptorWriteCount,
    const VkWriteDescriptorSet* pDescriptorWrites)
{
}

static void encode_vkCmdPushDescriptorSetKHR(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipelineLayout layout,
    uint32_t set,
    uint32_t descriptorWriteCount,
    const VkWriteDescriptorSet* pDescriptorWrites)
{
}

void goldfish_frontend_vkCmdPushDescriptorSetKHR(
    VkCommandBuffer commandBuffer,
    VkPipelineBindPoint pipelineBindPoint,
    VkPipelineLayout layout,
    uint32_t set,
    uint32_t descriptorWriteCount,
    const VkWriteDescriptorSet* pDescriptorWrites)

{
    // This is the body for vkCmdPushDescriptorSetKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdPushDescriptorSetKHR(&validateResult, commandBuffer, pipelineBindPoint, layout, set, descriptorWriteCount, pDescriptorWrites);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdPushDescriptorSetKHR(vkChannel, commandBuffer, pipelineBindPoint, layout, set, descriptorWriteCount, pDescriptorWrites);
}

static void validate_vkCmdPushDescriptorSetWithTemplateKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    VkPipelineLayout layout,
    uint32_t set,
    const void* pData)
{
}

static void encode_vkCmdPushDescriptorSetWithTemplateKHR(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    VkPipelineLayout layout,
    uint32_t set,
    const void* pData)
{
}

void goldfish_frontend_vkCmdPushDescriptorSetWithTemplateKHR(
    VkCommandBuffer commandBuffer,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    VkPipelineLayout layout,
    uint32_t set,
    const void* pData)

{
    // This is the body for vkCmdPushDescriptorSetWithTemplateKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdPushDescriptorSetWithTemplateKHR(&validateResult, commandBuffer, descriptorUpdateTemplate, layout, set, pData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdPushDescriptorSetWithTemplateKHR(vkChannel, commandBuffer, descriptorUpdateTemplate, layout, set, pData);
}

#endif

#ifdef VK_KHR_16bit_storage

#endif

#ifdef VK_KHR_incremental_present

#endif

#ifdef VK_KHR_descriptor_update_template

static void validate_vkCreateDescriptorUpdateTemplateKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorUpdateTemplateCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorUpdateTemplate* pDescriptorUpdateTemplate)
{
}

static VkResult encode_vkCreateDescriptorUpdateTemplateKHR(
    void* vkChannel,
    VkDevice device,
    const VkDescriptorUpdateTemplateCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorUpdateTemplate* pDescriptorUpdateTemplate)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateDescriptorUpdateTemplateKHR(
    VkDevice device,
    const VkDescriptorUpdateTemplateCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDescriptorUpdateTemplate* pDescriptorUpdateTemplate)

{
    // This is the body for vkCreateDescriptorUpdateTemplateKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDescriptorUpdateTemplateKHR(&validateResult, device, pCreateInfo, pAllocator, pDescriptorUpdateTemplate);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateDescriptorUpdateTemplateKHR(vkChannel, device, pCreateInfo, pAllocator, pDescriptorUpdateTemplate);
    return res;
}

static void validate_vkDestroyDescriptorUpdateTemplateKHR(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyDescriptorUpdateTemplateKHR(
    void* vkChannel,
    VkDevice device,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyDescriptorUpdateTemplateKHR(
    VkDevice device,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyDescriptorUpdateTemplateKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDescriptorUpdateTemplateKHR(&validateResult, device, descriptorUpdateTemplate, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyDescriptorUpdateTemplateKHR(vkChannel, device, descriptorUpdateTemplate, pAllocator);
}

static void validate_vkUpdateDescriptorSetWithTemplateKHR(
    VkResult* validateResult,
    VkDevice device,
    VkDescriptorSet descriptorSet,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const void* pData)
{
}

static void encode_vkUpdateDescriptorSetWithTemplateKHR(
    void* vkChannel,
    VkDevice device,
    VkDescriptorSet descriptorSet,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const void* pData)
{
}

void goldfish_frontend_vkUpdateDescriptorSetWithTemplateKHR(
    VkDevice device,
    VkDescriptorSet descriptorSet,
    VkDescriptorUpdateTemplate descriptorUpdateTemplate,
    const void* pData)

{
    // This is the body for vkUpdateDescriptorSetWithTemplateKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkUpdateDescriptorSetWithTemplateKHR(&validateResult, device, descriptorSet, descriptorUpdateTemplate, pData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkUpdateDescriptorSetWithTemplateKHR(vkChannel, device, descriptorSet, descriptorUpdateTemplate, pData);
}

#endif

#ifdef VK_KHR_create_renderpass2

static void validate_vkCreateRenderPass2KHR(
    VkResult* validateResult,
    VkDevice device,
    const VkRenderPassCreateInfo2KHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkRenderPass* pRenderPass)
{
}

static VkResult encode_vkCreateRenderPass2KHR(
    void* vkChannel,
    VkDevice device,
    const VkRenderPassCreateInfo2KHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkRenderPass* pRenderPass)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateRenderPass2KHR(
    VkDevice device,
    const VkRenderPassCreateInfo2KHR* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkRenderPass* pRenderPass)

{
    // This is the body for vkCreateRenderPass2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateRenderPass2KHR(&validateResult, device, pCreateInfo, pAllocator, pRenderPass);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateRenderPass2KHR(vkChannel, device, pCreateInfo, pAllocator, pRenderPass);
    return res;
}

static void validate_vkCmdBeginRenderPass2KHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkRenderPassBeginInfo*      pRenderPassBegin,
    const VkSubpassBeginInfoKHR*      pSubpassBeginInfo)
{
}

static void encode_vkCmdBeginRenderPass2KHR(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkRenderPassBeginInfo*      pRenderPassBegin,
    const VkSubpassBeginInfoKHR*      pSubpassBeginInfo)
{
}

void goldfish_frontend_vkCmdBeginRenderPass2KHR(
    VkCommandBuffer commandBuffer,
    const VkRenderPassBeginInfo*      pRenderPassBegin,
    const VkSubpassBeginInfoKHR*      pSubpassBeginInfo)

{
    // This is the body for vkCmdBeginRenderPass2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBeginRenderPass2KHR(&validateResult, commandBuffer, pRenderPassBegin, pSubpassBeginInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdBeginRenderPass2KHR(vkChannel, commandBuffer, pRenderPassBegin, pSubpassBeginInfo);
}

static void validate_vkCmdNextSubpass2KHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkSubpassBeginInfoKHR*      pSubpassBeginInfo,
    const VkSubpassEndInfoKHR*        pSubpassEndInfo)
{
}

static void encode_vkCmdNextSubpass2KHR(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkSubpassBeginInfoKHR*      pSubpassBeginInfo,
    const VkSubpassEndInfoKHR*        pSubpassEndInfo)
{
}

void goldfish_frontend_vkCmdNextSubpass2KHR(
    VkCommandBuffer commandBuffer,
    const VkSubpassBeginInfoKHR*      pSubpassBeginInfo,
    const VkSubpassEndInfoKHR*        pSubpassEndInfo)

{
    // This is the body for vkCmdNextSubpass2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdNextSubpass2KHR(&validateResult, commandBuffer, pSubpassBeginInfo, pSubpassEndInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdNextSubpass2KHR(vkChannel, commandBuffer, pSubpassBeginInfo, pSubpassEndInfo);
}

static void validate_vkCmdEndRenderPass2KHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkSubpassEndInfoKHR*        pSubpassEndInfo)
{
}

static void encode_vkCmdEndRenderPass2KHR(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkSubpassEndInfoKHR*        pSubpassEndInfo)
{
}

void goldfish_frontend_vkCmdEndRenderPass2KHR(
    VkCommandBuffer commandBuffer,
    const VkSubpassEndInfoKHR*        pSubpassEndInfo)

{
    // This is the body for vkCmdEndRenderPass2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdEndRenderPass2KHR(&validateResult, commandBuffer, pSubpassEndInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdEndRenderPass2KHR(vkChannel, commandBuffer, pSubpassEndInfo);
}

#endif

#ifdef VK_KHR_shared_presentable_image

static void validate_vkGetSwapchainStatusKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain)
{
}

static VkResult encode_vkGetSwapchainStatusKHR(
    void* vkChannel,
    VkDevice device,
    VkSwapchainKHR swapchain)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetSwapchainStatusKHR(
    VkDevice device,
    VkSwapchainKHR swapchain)

{
    // This is the body for vkGetSwapchainStatusKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetSwapchainStatusKHR(&validateResult, device, swapchain);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetSwapchainStatusKHR(vkChannel, device, swapchain);
    return res;
}

#endif

#ifdef VK_KHR_external_fence_capabilities

static void validate_vkGetPhysicalDeviceExternalFencePropertiesKHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo,
    VkExternalFenceProperties* pExternalFenceProperties)
{
}

static void encode_vkGetPhysicalDeviceExternalFencePropertiesKHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo,
    VkExternalFenceProperties* pExternalFenceProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceExternalFencePropertiesKHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo,
    VkExternalFenceProperties* pExternalFenceProperties)

{
    // This is the body for vkGetPhysicalDeviceExternalFencePropertiesKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalFencePropertiesKHR(&validateResult, physicalDevice, pExternalFenceInfo, pExternalFenceProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceExternalFencePropertiesKHR(vkChannel, physicalDevice, pExternalFenceInfo, pExternalFenceProperties);
}

#endif

#ifdef VK_KHR_external_fence

#endif

#ifdef VK_KHR_external_fence_win32

static void validate_vkImportFenceWin32HandleKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImportFenceWin32HandleInfoKHR* pImportFenceWin32HandleInfo)
{
}

static VkResult encode_vkImportFenceWin32HandleKHR(
    void* vkChannel,
    VkDevice device,
    const VkImportFenceWin32HandleInfoKHR* pImportFenceWin32HandleInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkImportFenceWin32HandleKHR(
    VkDevice device,
    const VkImportFenceWin32HandleInfoKHR* pImportFenceWin32HandleInfo)

{
    // This is the body for vkImportFenceWin32HandleKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkImportFenceWin32HandleKHR(&validateResult, device, pImportFenceWin32HandleInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkImportFenceWin32HandleKHR(vkChannel, device, pImportFenceWin32HandleInfo);
    return res;
}

static void validate_vkGetFenceWin32HandleKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkFenceGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
}

static VkResult encode_vkGetFenceWin32HandleKHR(
    void* vkChannel,
    VkDevice device,
    const VkFenceGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetFenceWin32HandleKHR(
    VkDevice device,
    const VkFenceGetWin32HandleInfoKHR* pGetWin32HandleInfo,
    HANDLE* pHandle)

{
    // This is the body for vkGetFenceWin32HandleKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetFenceWin32HandleKHR(&validateResult, device, pGetWin32HandleInfo, pHandle);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetFenceWin32HandleKHR(vkChannel, device, pGetWin32HandleInfo, pHandle);
    return res;
}

#endif

#ifdef VK_KHR_external_fence_fd

static void validate_vkImportFenceFdKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImportFenceFdInfoKHR* pImportFenceFdInfo)
{
}

static VkResult encode_vkImportFenceFdKHR(
    void* vkChannel,
    VkDevice device,
    const VkImportFenceFdInfoKHR* pImportFenceFdInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkImportFenceFdKHR(
    VkDevice device,
    const VkImportFenceFdInfoKHR* pImportFenceFdInfo)

{
    // This is the body for vkImportFenceFdKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkImportFenceFdKHR(&validateResult, device, pImportFenceFdInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkImportFenceFdKHR(vkChannel, device, pImportFenceFdInfo);
    return res;
}

static void validate_vkGetFenceFdKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkFenceGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
}

static VkResult encode_vkGetFenceFdKHR(
    void* vkChannel,
    VkDevice device,
    const VkFenceGetFdInfoKHR* pGetFdInfo,
    int* pFd)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetFenceFdKHR(
    VkDevice device,
    const VkFenceGetFdInfoKHR* pGetFdInfo,
    int* pFd)

{
    // This is the body for vkGetFenceFdKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetFenceFdKHR(&validateResult, device, pGetFdInfo, pFd);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetFenceFdKHR(vkChannel, device, pGetFdInfo, pFd);
    return res;
}

#endif

#ifdef VK_KHR_maintenance2

#endif

#ifdef VK_KHR_get_surface_capabilities2

static void validate_vkGetPhysicalDeviceSurfaceCapabilities2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSurfaceInfo2KHR* pSurfaceInfo,
    VkSurfaceCapabilities2KHR* pSurfaceCapabilities)
{
}

static VkResult encode_vkGetPhysicalDeviceSurfaceCapabilities2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSurfaceInfo2KHR* pSurfaceInfo,
    VkSurfaceCapabilities2KHR* pSurfaceCapabilities)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceCapabilities2KHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSurfaceInfo2KHR* pSurfaceInfo,
    VkSurfaceCapabilities2KHR* pSurfaceCapabilities)

{
    // This is the body for vkGetPhysicalDeviceSurfaceCapabilities2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceCapabilities2KHR(&validateResult, physicalDevice, pSurfaceInfo, pSurfaceCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceCapabilities2KHR(vkChannel, physicalDevice, pSurfaceInfo, pSurfaceCapabilities);
    return res;
}

static void validate_vkGetPhysicalDeviceSurfaceFormats2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSurfaceInfo2KHR* pSurfaceInfo,
    uint32_t* pSurfaceFormatCount,
    VkSurfaceFormat2KHR* pSurfaceFormats)
{
}

static VkResult encode_vkGetPhysicalDeviceSurfaceFormats2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSurfaceInfo2KHR* pSurfaceInfo,
    uint32_t* pSurfaceFormatCount,
    VkSurfaceFormat2KHR* pSurfaceFormats)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceFormats2KHR(
    VkPhysicalDevice physicalDevice,
    const VkPhysicalDeviceSurfaceInfo2KHR* pSurfaceInfo,
    uint32_t* pSurfaceFormatCount,
    VkSurfaceFormat2KHR* pSurfaceFormats)

{
    // This is the body for vkGetPhysicalDeviceSurfaceFormats2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceFormats2KHR(&validateResult, physicalDevice, pSurfaceInfo, pSurfaceFormatCount, pSurfaceFormats);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceFormats2KHR(vkChannel, physicalDevice, pSurfaceInfo, pSurfaceFormatCount, pSurfaceFormats);
    return res;
}

#endif

#ifdef VK_KHR_variable_pointers

#endif

#ifdef VK_KHR_get_display_properties2

static void validate_vkGetPhysicalDeviceDisplayProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayProperties2KHR* pProperties)
{
}

static VkResult encode_vkGetPhysicalDeviceDisplayProperties2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayProperties2KHR* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceDisplayProperties2KHR(
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayProperties2KHR* pProperties)

{
    // This is the body for vkGetPhysicalDeviceDisplayProperties2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceDisplayProperties2KHR(&validateResult, physicalDevice, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceDisplayProperties2KHR(vkChannel, physicalDevice, pPropertyCount, pProperties);
    return res;
}

static void validate_vkGetPhysicalDeviceDisplayPlaneProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPlaneProperties2KHR* pProperties)
{
}

static VkResult encode_vkGetPhysicalDeviceDisplayPlaneProperties2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPlaneProperties2KHR* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceDisplayPlaneProperties2KHR(
    VkPhysicalDevice physicalDevice,
    uint32_t* pPropertyCount,
    VkDisplayPlaneProperties2KHR* pProperties)

{
    // This is the body for vkGetPhysicalDeviceDisplayPlaneProperties2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceDisplayPlaneProperties2KHR(&validateResult, physicalDevice, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceDisplayPlaneProperties2KHR(vkChannel, physicalDevice, pPropertyCount, pProperties);
    return res;
}

static void validate_vkGetDisplayModeProperties2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    uint32_t* pPropertyCount,
    VkDisplayModeProperties2KHR* pProperties)
{
}

static VkResult encode_vkGetDisplayModeProperties2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    uint32_t* pPropertyCount,
    VkDisplayModeProperties2KHR* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetDisplayModeProperties2KHR(
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display,
    uint32_t* pPropertyCount,
    VkDisplayModeProperties2KHR* pProperties)

{
    // This is the body for vkGetDisplayModeProperties2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDisplayModeProperties2KHR(&validateResult, physicalDevice, display, pPropertyCount, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetDisplayModeProperties2KHR(vkChannel, physicalDevice, display, pPropertyCount, pProperties);
    return res;
}

static void validate_vkGetDisplayPlaneCapabilities2KHR(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    const VkDisplayPlaneInfo2KHR* pDisplayPlaneInfo,
    VkDisplayPlaneCapabilities2KHR* pCapabilities)
{
}

static VkResult encode_vkGetDisplayPlaneCapabilities2KHR(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    const VkDisplayPlaneInfo2KHR* pDisplayPlaneInfo,
    VkDisplayPlaneCapabilities2KHR* pCapabilities)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetDisplayPlaneCapabilities2KHR(
    VkPhysicalDevice physicalDevice,
    const VkDisplayPlaneInfo2KHR* pDisplayPlaneInfo,
    VkDisplayPlaneCapabilities2KHR* pCapabilities)

{
    // This is the body for vkGetDisplayPlaneCapabilities2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDisplayPlaneCapabilities2KHR(&validateResult, physicalDevice, pDisplayPlaneInfo, pCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetDisplayPlaneCapabilities2KHR(vkChannel, physicalDevice, pDisplayPlaneInfo, pCapabilities);
    return res;
}

#endif

#ifdef VK_KHR_dedicated_allocation

#endif

#ifdef VK_KHR_storage_buffer_storage_class

#endif

#ifdef VK_KHR_relaxed_block_layout

#endif

#ifdef VK_KHR_get_memory_requirements2

static void validate_vkGetImageMemoryRequirements2KHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImageMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

static void encode_vkGetImageMemoryRequirements2KHR(
    void* vkChannel,
    VkDevice device,
    const VkImageMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

void goldfish_frontend_vkGetImageMemoryRequirements2KHR(
    VkDevice device,
    const VkImageMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)

{
    // This is the body for vkGetImageMemoryRequirements2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageMemoryRequirements2KHR(&validateResult, device, pInfo, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetImageMemoryRequirements2KHR(vkChannel, device, pInfo, pMemoryRequirements);
}

static void validate_vkGetBufferMemoryRequirements2KHR(
    VkResult* validateResult,
    VkDevice device,
    const VkBufferMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

static void encode_vkGetBufferMemoryRequirements2KHR(
    void* vkChannel,
    VkDevice device,
    const VkBufferMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)
{
}

void goldfish_frontend_vkGetBufferMemoryRequirements2KHR(
    VkDevice device,
    const VkBufferMemoryRequirementsInfo2* pInfo,
    VkMemoryRequirements2* pMemoryRequirements)

{
    // This is the body for vkGetBufferMemoryRequirements2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetBufferMemoryRequirements2KHR(&validateResult, device, pInfo, pMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetBufferMemoryRequirements2KHR(vkChannel, device, pInfo, pMemoryRequirements);
}

static void validate_vkGetImageSparseMemoryRequirements2KHR(
    VkResult* validateResult,
    VkDevice device,
    const VkImageSparseMemoryRequirementsInfo2* pInfo,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements2* pSparseMemoryRequirements)
{
}

static void encode_vkGetImageSparseMemoryRequirements2KHR(
    void* vkChannel,
    VkDevice device,
    const VkImageSparseMemoryRequirementsInfo2* pInfo,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements2* pSparseMemoryRequirements)
{
}

void goldfish_frontend_vkGetImageSparseMemoryRequirements2KHR(
    VkDevice device,
    const VkImageSparseMemoryRequirementsInfo2* pInfo,
    uint32_t* pSparseMemoryRequirementCount,
    VkSparseImageMemoryRequirements2* pSparseMemoryRequirements)

{
    // This is the body for vkGetImageSparseMemoryRequirements2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetImageSparseMemoryRequirements2KHR(&validateResult, device, pInfo, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetImageSparseMemoryRequirements2KHR(vkChannel, device, pInfo, pSparseMemoryRequirementCount, pSparseMemoryRequirements);
}

#endif

#ifdef VK_KHR_image_format_list

#endif

#ifdef VK_KHR_sampler_ycbcr_conversion

static void validate_vkCreateSamplerYcbcrConversionKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkSamplerYcbcrConversionCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSamplerYcbcrConversion* pYcbcrConversion)
{
}

static VkResult encode_vkCreateSamplerYcbcrConversionKHR(
    void* vkChannel,
    VkDevice device,
    const VkSamplerYcbcrConversionCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSamplerYcbcrConversion* pYcbcrConversion)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateSamplerYcbcrConversionKHR(
    VkDevice device,
    const VkSamplerYcbcrConversionCreateInfo* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSamplerYcbcrConversion* pYcbcrConversion)

{
    // This is the body for vkCreateSamplerYcbcrConversionKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateSamplerYcbcrConversionKHR(&validateResult, device, pCreateInfo, pAllocator, pYcbcrConversion);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateSamplerYcbcrConversionKHR(vkChannel, device, pCreateInfo, pAllocator, pYcbcrConversion);
    return res;
}

static void validate_vkDestroySamplerYcbcrConversionKHR(
    VkResult* validateResult,
    VkDevice device,
    VkSamplerYcbcrConversion ycbcrConversion,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroySamplerYcbcrConversionKHR(
    void* vkChannel,
    VkDevice device,
    VkSamplerYcbcrConversion ycbcrConversion,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroySamplerYcbcrConversionKHR(
    VkDevice device,
    VkSamplerYcbcrConversion ycbcrConversion,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroySamplerYcbcrConversionKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroySamplerYcbcrConversionKHR(&validateResult, device, ycbcrConversion, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroySamplerYcbcrConversionKHR(vkChannel, device, ycbcrConversion, pAllocator);
}

#endif

#ifdef VK_KHR_bind_memory2

static void validate_vkBindBufferMemory2KHR(
    VkResult* validateResult,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindBufferMemoryInfo* pBindInfos)
{
}

static VkResult encode_vkBindBufferMemory2KHR(
    void* vkChannel,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindBufferMemoryInfo* pBindInfos)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkBindBufferMemory2KHR(
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindBufferMemoryInfo* pBindInfos)

{
    // This is the body for vkBindBufferMemory2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindBufferMemory2KHR(&validateResult, device, bindInfoCount, pBindInfos);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkBindBufferMemory2KHR(vkChannel, device, bindInfoCount, pBindInfos);
    return res;
}

static void validate_vkBindImageMemory2KHR(
    VkResult* validateResult,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindImageMemoryInfo* pBindInfos)
{
}

static VkResult encode_vkBindImageMemory2KHR(
    void* vkChannel,
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindImageMemoryInfo* pBindInfos)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkBindImageMemory2KHR(
    VkDevice device,
    uint32_t bindInfoCount,
    const VkBindImageMemoryInfo* pBindInfos)

{
    // This is the body for vkBindImageMemory2KHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkBindImageMemory2KHR(&validateResult, device, bindInfoCount, pBindInfos);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkBindImageMemory2KHR(vkChannel, device, bindInfoCount, pBindInfos);
    return res;
}

#endif

#ifdef VK_KHR_maintenance3

static void validate_vkGetDescriptorSetLayoutSupportKHR(
    VkResult* validateResult,
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    VkDescriptorSetLayoutSupport* pSupport)
{
}

static void encode_vkGetDescriptorSetLayoutSupportKHR(
    void* vkChannel,
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    VkDescriptorSetLayoutSupport* pSupport)
{
}

void goldfish_frontend_vkGetDescriptorSetLayoutSupportKHR(
    VkDevice device,
    const VkDescriptorSetLayoutCreateInfo* pCreateInfo,
    VkDescriptorSetLayoutSupport* pSupport)

{
    // This is the body for vkGetDescriptorSetLayoutSupportKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetDescriptorSetLayoutSupportKHR(&validateResult, device, pCreateInfo, pSupport);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetDescriptorSetLayoutSupportKHR(vkChannel, device, pCreateInfo, pSupport);
}

#endif

#ifdef VK_KHR_draw_indirect_count

static void validate_vkCmdDrawIndirectCountKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

static void encode_vkCmdDrawIndirectCountKHR(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

void goldfish_frontend_vkCmdDrawIndirectCountKHR(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)

{
    // This is the body for vkCmdDrawIndirectCountKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndirectCountKHR(&validateResult, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDrawIndirectCountKHR(vkChannel, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
}

static void validate_vkCmdDrawIndexedIndirectCountKHR(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

static void encode_vkCmdDrawIndexedIndirectCountKHR(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

void goldfish_frontend_vkCmdDrawIndexedIndirectCountKHR(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)

{
    // This is the body for vkCmdDrawIndexedIndirectCountKHR
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndexedIndirectCountKHR(&validateResult, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDrawIndexedIndirectCountKHR(vkChannel, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
}

#endif

#ifdef VK_KHR_8bit_storage

#endif

#ifdef VK_EXT_debug_report

static void validate_vkCreateDebugReportCallbackEXT(
    VkResult* validateResult,
    VkInstance instance,
    const VkDebugReportCallbackCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDebugReportCallbackEXT* pCallback)
{
}

static VkResult encode_vkCreateDebugReportCallbackEXT(
    void* vkChannel,
    VkInstance instance,
    const VkDebugReportCallbackCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDebugReportCallbackEXT* pCallback)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateDebugReportCallbackEXT(
    VkInstance instance,
    const VkDebugReportCallbackCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDebugReportCallbackEXT* pCallback)

{
    // This is the body for vkCreateDebugReportCallbackEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDebugReportCallbackEXT(&validateResult, instance, pCreateInfo, pAllocator, pCallback);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateDebugReportCallbackEXT(vkChannel, instance, pCreateInfo, pAllocator, pCallback);
    return res;
}

static void validate_vkDestroyDebugReportCallbackEXT(
    VkResult* validateResult,
    VkInstance instance,
    VkDebugReportCallbackEXT callback,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyDebugReportCallbackEXT(
    void* vkChannel,
    VkInstance instance,
    VkDebugReportCallbackEXT callback,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyDebugReportCallbackEXT(
    VkInstance instance,
    VkDebugReportCallbackEXT callback,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyDebugReportCallbackEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDebugReportCallbackEXT(&validateResult, instance, callback, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyDebugReportCallbackEXT(vkChannel, instance, callback, pAllocator);
}

static void validate_vkDebugReportMessageEXT(
    VkResult* validateResult,
    VkInstance instance,
    VkDebugReportFlagsEXT flags,
    VkDebugReportObjectTypeEXT objectType,
    uint64_t object,
    size_t location,
    int32_t messageCode,
    const char* pLayerPrefix,
    const char* pMessage)
{
}

static void encode_vkDebugReportMessageEXT(
    void* vkChannel,
    VkInstance instance,
    VkDebugReportFlagsEXT flags,
    VkDebugReportObjectTypeEXT objectType,
    uint64_t object,
    size_t location,
    int32_t messageCode,
    const char* pLayerPrefix,
    const char* pMessage)
{
}

void goldfish_frontend_vkDebugReportMessageEXT(
    VkInstance instance,
    VkDebugReportFlagsEXT flags,
    VkDebugReportObjectTypeEXT objectType,
    uint64_t object,
    size_t location,
    int32_t messageCode,
    const char* pLayerPrefix,
    const char* pMessage)

{
    // This is the body for vkDebugReportMessageEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkDebugReportMessageEXT(&validateResult, instance, flags, objectType, object, location, messageCode, pLayerPrefix, pMessage);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDebugReportMessageEXT(vkChannel, instance, flags, objectType, object, location, messageCode, pLayerPrefix, pMessage);
}

#endif

#ifdef VK_NV_glsl_shader

#endif

#ifdef VK_EXT_depth_range_unrestricted

#endif

#ifdef VK_IMG_filter_cubic

#endif

#ifdef VK_AMD_rasterization_order

#endif

#ifdef VK_AMD_shader_trinary_minmax

#endif

#ifdef VK_AMD_shader_explicit_vertex_parameter

#endif

#ifdef VK_EXT_debug_marker

static void validate_vkDebugMarkerSetObjectTagEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkDebugMarkerObjectTagInfoEXT* pTagInfo)
{
}

static VkResult encode_vkDebugMarkerSetObjectTagEXT(
    void* vkChannel,
    VkDevice device,
    const VkDebugMarkerObjectTagInfoEXT* pTagInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkDebugMarkerSetObjectTagEXT(
    VkDevice device,
    const VkDebugMarkerObjectTagInfoEXT* pTagInfo)

{
    // This is the body for vkDebugMarkerSetObjectTagEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkDebugMarkerSetObjectTagEXT(&validateResult, device, pTagInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkDebugMarkerSetObjectTagEXT(vkChannel, device, pTagInfo);
    return res;
}

static void validate_vkDebugMarkerSetObjectNameEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkDebugMarkerObjectNameInfoEXT* pNameInfo)
{
}

static VkResult encode_vkDebugMarkerSetObjectNameEXT(
    void* vkChannel,
    VkDevice device,
    const VkDebugMarkerObjectNameInfoEXT* pNameInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkDebugMarkerSetObjectNameEXT(
    VkDevice device,
    const VkDebugMarkerObjectNameInfoEXT* pNameInfo)

{
    // This is the body for vkDebugMarkerSetObjectNameEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkDebugMarkerSetObjectNameEXT(&validateResult, device, pNameInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkDebugMarkerSetObjectNameEXT(vkChannel, device, pNameInfo);
    return res;
}

static void validate_vkCmdDebugMarkerBeginEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkDebugMarkerMarkerInfoEXT* pMarkerInfo)
{
}

static void encode_vkCmdDebugMarkerBeginEXT(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkDebugMarkerMarkerInfoEXT* pMarkerInfo)
{
}

void goldfish_frontend_vkCmdDebugMarkerBeginEXT(
    VkCommandBuffer commandBuffer,
    const VkDebugMarkerMarkerInfoEXT* pMarkerInfo)

{
    // This is the body for vkCmdDebugMarkerBeginEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDebugMarkerBeginEXT(&validateResult, commandBuffer, pMarkerInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDebugMarkerBeginEXT(vkChannel, commandBuffer, pMarkerInfo);
}

static void validate_vkCmdDebugMarkerEndEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer)
{
}

static void encode_vkCmdDebugMarkerEndEXT(
    void* vkChannel,
    VkCommandBuffer commandBuffer)
{
}

void goldfish_frontend_vkCmdDebugMarkerEndEXT(
    VkCommandBuffer commandBuffer)

{
    // This is the body for vkCmdDebugMarkerEndEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDebugMarkerEndEXT(&validateResult, commandBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDebugMarkerEndEXT(vkChannel, commandBuffer);
}

static void validate_vkCmdDebugMarkerInsertEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkDebugMarkerMarkerInfoEXT* pMarkerInfo)
{
}

static void encode_vkCmdDebugMarkerInsertEXT(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkDebugMarkerMarkerInfoEXT* pMarkerInfo)
{
}

void goldfish_frontend_vkCmdDebugMarkerInsertEXT(
    VkCommandBuffer commandBuffer,
    const VkDebugMarkerMarkerInfoEXT* pMarkerInfo)

{
    // This is the body for vkCmdDebugMarkerInsertEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDebugMarkerInsertEXT(&validateResult, commandBuffer, pMarkerInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDebugMarkerInsertEXT(vkChannel, commandBuffer, pMarkerInfo);
}

#endif

#ifdef VK_AMD_gcn_shader

#endif

#ifdef VK_NV_dedicated_allocation

#endif

#ifdef VK_AMD_draw_indirect_count

static void validate_vkCmdDrawIndirectCountAMD(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

static void encode_vkCmdDrawIndirectCountAMD(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

void goldfish_frontend_vkCmdDrawIndirectCountAMD(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)

{
    // This is the body for vkCmdDrawIndirectCountAMD
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndirectCountAMD(&validateResult, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDrawIndirectCountAMD(vkChannel, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
}

static void validate_vkCmdDrawIndexedIndirectCountAMD(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

static void encode_vkCmdDrawIndexedIndirectCountAMD(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)
{
}

void goldfish_frontend_vkCmdDrawIndexedIndirectCountAMD(
    VkCommandBuffer commandBuffer,
    VkBuffer buffer,
    VkDeviceSize offset,
    VkBuffer countBuffer,
    VkDeviceSize countBufferOffset,
    uint32_t maxDrawCount,
    uint32_t stride)

{
    // This is the body for vkCmdDrawIndexedIndirectCountAMD
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdDrawIndexedIndirectCountAMD(&validateResult, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdDrawIndexedIndirectCountAMD(vkChannel, commandBuffer, buffer, offset, countBuffer, countBufferOffset, maxDrawCount, stride);
}

#endif

#ifdef VK_AMD_negative_viewport_height

#endif

#ifdef VK_AMD_gpu_shader_half_float

#endif

#ifdef VK_AMD_shader_ballot

#endif

#ifdef VK_AMD_texture_gather_bias_lod

#endif

#ifdef VK_AMD_shader_info

static void validate_vkGetShaderInfoAMD(
    VkResult* validateResult,
    VkDevice device,
    VkPipeline pipeline,
    VkShaderStageFlagBits shaderStage,
    VkShaderInfoTypeAMD infoType,
    size_t* pInfoSize,
    void* pInfo)
{
}

static VkResult encode_vkGetShaderInfoAMD(
    void* vkChannel,
    VkDevice device,
    VkPipeline pipeline,
    VkShaderStageFlagBits shaderStage,
    VkShaderInfoTypeAMD infoType,
    size_t* pInfoSize,
    void* pInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetShaderInfoAMD(
    VkDevice device,
    VkPipeline pipeline,
    VkShaderStageFlagBits shaderStage,
    VkShaderInfoTypeAMD infoType,
    size_t* pInfoSize,
    void* pInfo)

{
    // This is the body for vkGetShaderInfoAMD
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetShaderInfoAMD(&validateResult, device, pipeline, shaderStage, infoType, pInfoSize, pInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetShaderInfoAMD(vkChannel, device, pipeline, shaderStage, infoType, pInfoSize, pInfo);
    return res;
}

#endif

#ifdef VK_AMD_shader_image_load_store_lod

#endif

#ifdef VK_IMG_format_pvrtc

#endif

#ifdef VK_NV_external_memory_capabilities

static void validate_vkGetPhysicalDeviceExternalImageFormatPropertiesNV(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkImageTiling tiling,
    VkImageUsageFlags usage,
    VkImageCreateFlags flags,
    VkExternalMemoryHandleTypeFlagsNV externalHandleType,
    VkExternalImageFormatPropertiesNV* pExternalImageFormatProperties)
{
}

static VkResult encode_vkGetPhysicalDeviceExternalImageFormatPropertiesNV(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkImageTiling tiling,
    VkImageUsageFlags usage,
    VkImageCreateFlags flags,
    VkExternalMemoryHandleTypeFlagsNV externalHandleType,
    VkExternalImageFormatPropertiesNV* pExternalImageFormatProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceExternalImageFormatPropertiesNV(
    VkPhysicalDevice physicalDevice,
    VkFormat format,
    VkImageType type,
    VkImageTiling tiling,
    VkImageUsageFlags usage,
    VkImageCreateFlags flags,
    VkExternalMemoryHandleTypeFlagsNV externalHandleType,
    VkExternalImageFormatPropertiesNV* pExternalImageFormatProperties)

{
    // This is the body for vkGetPhysicalDeviceExternalImageFormatPropertiesNV
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceExternalImageFormatPropertiesNV(&validateResult, physicalDevice, format, type, tiling, usage, flags, externalHandleType, pExternalImageFormatProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceExternalImageFormatPropertiesNV(vkChannel, physicalDevice, format, type, tiling, usage, flags, externalHandleType, pExternalImageFormatProperties);
    return res;
}

#endif

#ifdef VK_NV_external_memory

#endif

#ifdef VK_NV_external_memory_win32

static void validate_vkGetMemoryWin32HandleNV(
    VkResult* validateResult,
    VkDevice device,
    VkDeviceMemory memory,
    VkExternalMemoryHandleTypeFlagsNV handleType,
    HANDLE* pHandle)
{
}

static VkResult encode_vkGetMemoryWin32HandleNV(
    void* vkChannel,
    VkDevice device,
    VkDeviceMemory memory,
    VkExternalMemoryHandleTypeFlagsNV handleType,
    HANDLE* pHandle)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetMemoryWin32HandleNV(
    VkDevice device,
    VkDeviceMemory memory,
    VkExternalMemoryHandleTypeFlagsNV handleType,
    HANDLE* pHandle)

{
    // This is the body for vkGetMemoryWin32HandleNV
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryWin32HandleNV(&validateResult, device, memory, handleType, pHandle);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetMemoryWin32HandleNV(vkChannel, device, memory, handleType, pHandle);
    return res;
}

#endif

#ifdef VK_NV_win32_keyed_mutex

#endif

#ifdef VK_EXT_validation_flags

#endif

#ifdef VK_NN_vi_surface

static void validate_vkCreateViSurfaceNN(
    VkResult* validateResult,
    VkInstance instance,
    const VkViSurfaceCreateInfoNN* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

static VkResult encode_vkCreateViSurfaceNN(
    void* vkChannel,
    VkInstance instance,
    const VkViSurfaceCreateInfoNN* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateViSurfaceNN(
    VkInstance instance,
    const VkViSurfaceCreateInfoNN* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)

{
    // This is the body for vkCreateViSurfaceNN
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateViSurfaceNN(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateViSurfaceNN(vkChannel, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

#endif

#ifdef VK_EXT_shader_subgroup_ballot

#endif

#ifdef VK_EXT_shader_subgroup_vote

#endif

#ifdef VK_EXT_conditional_rendering

static void validate_vkCmdBeginConditionalRenderingEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkConditionalRenderingBeginInfoEXT* pConditionalRenderingBegin)
{
}

static void encode_vkCmdBeginConditionalRenderingEXT(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkConditionalRenderingBeginInfoEXT* pConditionalRenderingBegin)
{
}

void goldfish_frontend_vkCmdBeginConditionalRenderingEXT(
    VkCommandBuffer commandBuffer,
    const VkConditionalRenderingBeginInfoEXT* pConditionalRenderingBegin)

{
    // This is the body for vkCmdBeginConditionalRenderingEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBeginConditionalRenderingEXT(&validateResult, commandBuffer, pConditionalRenderingBegin);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdBeginConditionalRenderingEXT(vkChannel, commandBuffer, pConditionalRenderingBegin);
}

static void validate_vkCmdEndConditionalRenderingEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer)
{
}

static void encode_vkCmdEndConditionalRenderingEXT(
    void* vkChannel,
    VkCommandBuffer commandBuffer)
{
}

void goldfish_frontend_vkCmdEndConditionalRenderingEXT(
    VkCommandBuffer commandBuffer)

{
    // This is the body for vkCmdEndConditionalRenderingEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdEndConditionalRenderingEXT(&validateResult, commandBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdEndConditionalRenderingEXT(vkChannel, commandBuffer);
}

#endif

#ifdef VK_NVX_device_generated_commands

static void validate_vkCmdProcessCommandsNVX(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkCmdProcessCommandsInfoNVX* pProcessCommandsInfo)
{
}

static void encode_vkCmdProcessCommandsNVX(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkCmdProcessCommandsInfoNVX* pProcessCommandsInfo)
{
}

void goldfish_frontend_vkCmdProcessCommandsNVX(
    VkCommandBuffer commandBuffer,
    const VkCmdProcessCommandsInfoNVX* pProcessCommandsInfo)

{
    // This is the body for vkCmdProcessCommandsNVX
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdProcessCommandsNVX(&validateResult, commandBuffer, pProcessCommandsInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdProcessCommandsNVX(vkChannel, commandBuffer, pProcessCommandsInfo);
}

static void validate_vkCmdReserveSpaceForCommandsNVX(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkCmdReserveSpaceForCommandsInfoNVX* pReserveSpaceInfo)
{
}

static void encode_vkCmdReserveSpaceForCommandsNVX(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkCmdReserveSpaceForCommandsInfoNVX* pReserveSpaceInfo)
{
}

void goldfish_frontend_vkCmdReserveSpaceForCommandsNVX(
    VkCommandBuffer commandBuffer,
    const VkCmdReserveSpaceForCommandsInfoNVX* pReserveSpaceInfo)

{
    // This is the body for vkCmdReserveSpaceForCommandsNVX
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdReserveSpaceForCommandsNVX(&validateResult, commandBuffer, pReserveSpaceInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdReserveSpaceForCommandsNVX(vkChannel, commandBuffer, pReserveSpaceInfo);
}

static void validate_vkCreateIndirectCommandsLayoutNVX(
    VkResult* validateResult,
    VkDevice device,
    const VkIndirectCommandsLayoutCreateInfoNVX* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkIndirectCommandsLayoutNVX* pIndirectCommandsLayout)
{
}

static VkResult encode_vkCreateIndirectCommandsLayoutNVX(
    void* vkChannel,
    VkDevice device,
    const VkIndirectCommandsLayoutCreateInfoNVX* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkIndirectCommandsLayoutNVX* pIndirectCommandsLayout)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateIndirectCommandsLayoutNVX(
    VkDevice device,
    const VkIndirectCommandsLayoutCreateInfoNVX* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkIndirectCommandsLayoutNVX* pIndirectCommandsLayout)

{
    // This is the body for vkCreateIndirectCommandsLayoutNVX
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateIndirectCommandsLayoutNVX(&validateResult, device, pCreateInfo, pAllocator, pIndirectCommandsLayout);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateIndirectCommandsLayoutNVX(vkChannel, device, pCreateInfo, pAllocator, pIndirectCommandsLayout);
    return res;
}

static void validate_vkDestroyIndirectCommandsLayoutNVX(
    VkResult* validateResult,
    VkDevice device,
    VkIndirectCommandsLayoutNVX indirectCommandsLayout,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyIndirectCommandsLayoutNVX(
    void* vkChannel,
    VkDevice device,
    VkIndirectCommandsLayoutNVX indirectCommandsLayout,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyIndirectCommandsLayoutNVX(
    VkDevice device,
    VkIndirectCommandsLayoutNVX indirectCommandsLayout,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyIndirectCommandsLayoutNVX
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyIndirectCommandsLayoutNVX(&validateResult, device, indirectCommandsLayout, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyIndirectCommandsLayoutNVX(vkChannel, device, indirectCommandsLayout, pAllocator);
}

static void validate_vkCreateObjectTableNVX(
    VkResult* validateResult,
    VkDevice device,
    const VkObjectTableCreateInfoNVX* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkObjectTableNVX* pObjectTable)
{
}

static VkResult encode_vkCreateObjectTableNVX(
    void* vkChannel,
    VkDevice device,
    const VkObjectTableCreateInfoNVX* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkObjectTableNVX* pObjectTable)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateObjectTableNVX(
    VkDevice device,
    const VkObjectTableCreateInfoNVX* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkObjectTableNVX* pObjectTable)

{
    // This is the body for vkCreateObjectTableNVX
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateObjectTableNVX(&validateResult, device, pCreateInfo, pAllocator, pObjectTable);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateObjectTableNVX(vkChannel, device, pCreateInfo, pAllocator, pObjectTable);
    return res;
}

static void validate_vkDestroyObjectTableNVX(
    VkResult* validateResult,
    VkDevice device,
    VkObjectTableNVX objectTable,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyObjectTableNVX(
    void* vkChannel,
    VkDevice device,
    VkObjectTableNVX objectTable,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyObjectTableNVX(
    VkDevice device,
    VkObjectTableNVX objectTable,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyObjectTableNVX
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyObjectTableNVX(&validateResult, device, objectTable, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyObjectTableNVX(vkChannel, device, objectTable, pAllocator);
}

static void validate_vkRegisterObjectsNVX(
    VkResult* validateResult,
    VkDevice device,
    VkObjectTableNVX objectTable,
    uint32_t objectCount,
    const VkObjectTableEntryNVX* const*    ppObjectTableEntries,
    const uint32_t* pObjectIndices)
{
}

static VkResult encode_vkRegisterObjectsNVX(
    void* vkChannel,
    VkDevice device,
    VkObjectTableNVX objectTable,
    uint32_t objectCount,
    const VkObjectTableEntryNVX* const*    ppObjectTableEntries,
    const uint32_t* pObjectIndices)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkRegisterObjectsNVX(
    VkDevice device,
    VkObjectTableNVX objectTable,
    uint32_t objectCount,
    const VkObjectTableEntryNVX* const*    ppObjectTableEntries,
    const uint32_t* pObjectIndices)

{
    // This is the body for vkRegisterObjectsNVX
    VkResult validateResult = VK_SUCCESS;
    validate_vkRegisterObjectsNVX(&validateResult, device, objectTable, objectCount, ppObjectTableEntries, pObjectIndices);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkRegisterObjectsNVX(vkChannel, device, objectTable, objectCount, ppObjectTableEntries, pObjectIndices);
    return res;
}

static void validate_vkUnregisterObjectsNVX(
    VkResult* validateResult,
    VkDevice device,
    VkObjectTableNVX objectTable,
    uint32_t objectCount,
    const VkObjectEntryTypeNVX* pObjectEntryTypes,
    const uint32_t* pObjectIndices)
{
}

static VkResult encode_vkUnregisterObjectsNVX(
    void* vkChannel,
    VkDevice device,
    VkObjectTableNVX objectTable,
    uint32_t objectCount,
    const VkObjectEntryTypeNVX* pObjectEntryTypes,
    const uint32_t* pObjectIndices)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkUnregisterObjectsNVX(
    VkDevice device,
    VkObjectTableNVX objectTable,
    uint32_t objectCount,
    const VkObjectEntryTypeNVX* pObjectEntryTypes,
    const uint32_t* pObjectIndices)

{
    // This is the body for vkUnregisterObjectsNVX
    VkResult validateResult = VK_SUCCESS;
    validate_vkUnregisterObjectsNVX(&validateResult, device, objectTable, objectCount, pObjectEntryTypes, pObjectIndices);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkUnregisterObjectsNVX(vkChannel, device, objectTable, objectCount, pObjectEntryTypes, pObjectIndices);
    return res;
}

static void validate_vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDeviceGeneratedCommandsFeaturesNVX* pFeatures,
    VkDeviceGeneratedCommandsLimitsNVX* pLimits)
{
}

static void encode_vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkDeviceGeneratedCommandsFeaturesNVX* pFeatures,
    VkDeviceGeneratedCommandsLimitsNVX* pLimits)
{
}

void goldfish_frontend_vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX(
    VkPhysicalDevice physicalDevice,
    VkDeviceGeneratedCommandsFeaturesNVX* pFeatures,
    VkDeviceGeneratedCommandsLimitsNVX* pLimits)

{
    // This is the body for vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX(&validateResult, physicalDevice, pFeatures, pLimits);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceGeneratedCommandsPropertiesNVX(vkChannel, physicalDevice, pFeatures, pLimits);
}

#endif

#ifdef VK_NV_clip_space_w_scaling

static void validate_vkCmdSetViewportWScalingNV(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t firstViewport,
    uint32_t viewportCount,
    const VkViewportWScalingNV* pViewportWScalings)
{
}

static void encode_vkCmdSetViewportWScalingNV(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t firstViewport,
    uint32_t viewportCount,
    const VkViewportWScalingNV* pViewportWScalings)
{
}

void goldfish_frontend_vkCmdSetViewportWScalingNV(
    VkCommandBuffer commandBuffer,
    uint32_t firstViewport,
    uint32_t viewportCount,
    const VkViewportWScalingNV* pViewportWScalings)

{
    // This is the body for vkCmdSetViewportWScalingNV
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetViewportWScalingNV(&validateResult, commandBuffer, firstViewport, viewportCount, pViewportWScalings);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetViewportWScalingNV(vkChannel, commandBuffer, firstViewport, viewportCount, pViewportWScalings);
}

#endif

#ifdef VK_EXT_direct_mode_display

static void validate_vkReleaseDisplayEXT(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display)
{
}

static VkResult encode_vkReleaseDisplayEXT(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkReleaseDisplayEXT(
    VkPhysicalDevice physicalDevice,
    VkDisplayKHR display)

{
    // This is the body for vkReleaseDisplayEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkReleaseDisplayEXT(&validateResult, physicalDevice, display);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkReleaseDisplayEXT(vkChannel, physicalDevice, display);
    return res;
}

#endif

#ifdef VK_EXT_acquire_xlib_display

static void validate_vkAcquireXlibDisplayEXT(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    Display* dpy,
    VkDisplayKHR display)
{
}

static VkResult encode_vkAcquireXlibDisplayEXT(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    Display* dpy,
    VkDisplayKHR display)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkAcquireXlibDisplayEXT(
    VkPhysicalDevice physicalDevice,
    Display* dpy,
    VkDisplayKHR display)

{
    // This is the body for vkAcquireXlibDisplayEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkAcquireXlibDisplayEXT(&validateResult, physicalDevice, dpy, display);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkAcquireXlibDisplayEXT(vkChannel, physicalDevice, dpy, display);
    return res;
}

static void validate_vkGetRandROutputDisplayEXT(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    Display* dpy,
    RROutput rrOutput,
    VkDisplayKHR* pDisplay)
{
}

static VkResult encode_vkGetRandROutputDisplayEXT(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    Display* dpy,
    RROutput rrOutput,
    VkDisplayKHR* pDisplay)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetRandROutputDisplayEXT(
    VkPhysicalDevice physicalDevice,
    Display* dpy,
    RROutput rrOutput,
    VkDisplayKHR* pDisplay)

{
    // This is the body for vkGetRandROutputDisplayEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetRandROutputDisplayEXT(&validateResult, physicalDevice, dpy, rrOutput, pDisplay);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetRandROutputDisplayEXT(vkChannel, physicalDevice, dpy, rrOutput, pDisplay);
    return res;
}

#endif

#ifdef VK_EXT_display_surface_counter

static void validate_vkGetPhysicalDeviceSurfaceCapabilities2EXT(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    VkSurfaceCapabilities2EXT* pSurfaceCapabilities)
{
}

static VkResult encode_vkGetPhysicalDeviceSurfaceCapabilities2EXT(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    VkSurfaceCapabilities2EXT* pSurfaceCapabilities)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPhysicalDeviceSurfaceCapabilities2EXT(
    VkPhysicalDevice physicalDevice,
    VkSurfaceKHR surface,
    VkSurfaceCapabilities2EXT* pSurfaceCapabilities)

{
    // This is the body for vkGetPhysicalDeviceSurfaceCapabilities2EXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceSurfaceCapabilities2EXT(&validateResult, physicalDevice, surface, pSurfaceCapabilities);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPhysicalDeviceSurfaceCapabilities2EXT(vkChannel, physicalDevice, surface, pSurfaceCapabilities);
    return res;
}

#endif

#ifdef VK_EXT_display_control

static void validate_vkDisplayPowerControlEXT(
    VkResult* validateResult,
    VkDevice device,
    VkDisplayKHR display,
    const VkDisplayPowerInfoEXT* pDisplayPowerInfo)
{
}

static VkResult encode_vkDisplayPowerControlEXT(
    void* vkChannel,
    VkDevice device,
    VkDisplayKHR display,
    const VkDisplayPowerInfoEXT* pDisplayPowerInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkDisplayPowerControlEXT(
    VkDevice device,
    VkDisplayKHR display,
    const VkDisplayPowerInfoEXT* pDisplayPowerInfo)

{
    // This is the body for vkDisplayPowerControlEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkDisplayPowerControlEXT(&validateResult, device, display, pDisplayPowerInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkDisplayPowerControlEXT(vkChannel, device, display, pDisplayPowerInfo);
    return res;
}

static void validate_vkRegisterDeviceEventEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkDeviceEventInfoEXT* pDeviceEventInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
}

static VkResult encode_vkRegisterDeviceEventEXT(
    void* vkChannel,
    VkDevice device,
    const VkDeviceEventInfoEXT* pDeviceEventInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkRegisterDeviceEventEXT(
    VkDevice device,
    const VkDeviceEventInfoEXT* pDeviceEventInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)

{
    // This is the body for vkRegisterDeviceEventEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkRegisterDeviceEventEXT(&validateResult, device, pDeviceEventInfo, pAllocator, pFence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkRegisterDeviceEventEXT(vkChannel, device, pDeviceEventInfo, pAllocator, pFence);
    return res;
}

static void validate_vkRegisterDisplayEventEXT(
    VkResult* validateResult,
    VkDevice device,
    VkDisplayKHR display,
    const VkDisplayEventInfoEXT* pDisplayEventInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
}

static VkResult encode_vkRegisterDisplayEventEXT(
    void* vkChannel,
    VkDevice device,
    VkDisplayKHR display,
    const VkDisplayEventInfoEXT* pDisplayEventInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkRegisterDisplayEventEXT(
    VkDevice device,
    VkDisplayKHR display,
    const VkDisplayEventInfoEXT* pDisplayEventInfo,
    const VkAllocationCallbacks* pAllocator,
    VkFence* pFence)

{
    // This is the body for vkRegisterDisplayEventEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkRegisterDisplayEventEXT(&validateResult, device, display, pDisplayEventInfo, pAllocator, pFence);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkRegisterDisplayEventEXT(vkChannel, device, display, pDisplayEventInfo, pAllocator, pFence);
    return res;
}

static void validate_vkGetSwapchainCounterEXT(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    VkSurfaceCounterFlagBitsEXT counter,
    uint64_t* pCounterValue)
{
}

static VkResult encode_vkGetSwapchainCounterEXT(
    void* vkChannel,
    VkDevice device,
    VkSwapchainKHR swapchain,
    VkSurfaceCounterFlagBitsEXT counter,
    uint64_t* pCounterValue)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetSwapchainCounterEXT(
    VkDevice device,
    VkSwapchainKHR swapchain,
    VkSurfaceCounterFlagBitsEXT counter,
    uint64_t* pCounterValue)

{
    // This is the body for vkGetSwapchainCounterEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetSwapchainCounterEXT(&validateResult, device, swapchain, counter, pCounterValue);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetSwapchainCounterEXT(vkChannel, device, swapchain, counter, pCounterValue);
    return res;
}

#endif

#ifdef VK_GOOGLE_display_timing

static void validate_vkGetRefreshCycleDurationGOOGLE(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    VkRefreshCycleDurationGOOGLE* pDisplayTimingProperties)
{
}

static VkResult encode_vkGetRefreshCycleDurationGOOGLE(
    void* vkChannel,
    VkDevice device,
    VkSwapchainKHR swapchain,
    VkRefreshCycleDurationGOOGLE* pDisplayTimingProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetRefreshCycleDurationGOOGLE(
    VkDevice device,
    VkSwapchainKHR swapchain,
    VkRefreshCycleDurationGOOGLE* pDisplayTimingProperties)

{
    // This is the body for vkGetRefreshCycleDurationGOOGLE
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetRefreshCycleDurationGOOGLE(&validateResult, device, swapchain, pDisplayTimingProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetRefreshCycleDurationGOOGLE(vkChannel, device, swapchain, pDisplayTimingProperties);
    return res;
}

static void validate_vkGetPastPresentationTimingGOOGLE(
    VkResult* validateResult,
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint32_t* pPresentationTimingCount,
    VkPastPresentationTimingGOOGLE* pPresentationTimings)
{
}

static VkResult encode_vkGetPastPresentationTimingGOOGLE(
    void* vkChannel,
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint32_t* pPresentationTimingCount,
    VkPastPresentationTimingGOOGLE* pPresentationTimings)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetPastPresentationTimingGOOGLE(
    VkDevice device,
    VkSwapchainKHR swapchain,
    uint32_t* pPresentationTimingCount,
    VkPastPresentationTimingGOOGLE* pPresentationTimings)

{
    // This is the body for vkGetPastPresentationTimingGOOGLE
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPastPresentationTimingGOOGLE(&validateResult, device, swapchain, pPresentationTimingCount, pPresentationTimings);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetPastPresentationTimingGOOGLE(vkChannel, device, swapchain, pPresentationTimingCount, pPresentationTimings);
    return res;
}

#endif

#ifdef VK_NV_sample_mask_override_coverage

#endif

#ifdef VK_NV_geometry_shader_passthrough

#endif

#ifdef VK_NV_viewport_array2

#endif

#ifdef VK_NVX_multiview_per_view_attributes

#endif

#ifdef VK_NV_viewport_swizzle

#endif

#ifdef VK_EXT_discard_rectangles

static void validate_vkCmdSetDiscardRectangleEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    uint32_t firstDiscardRectangle,
    uint32_t discardRectangleCount,
    const VkRect2D* pDiscardRectangles)
{
}

static void encode_vkCmdSetDiscardRectangleEXT(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    uint32_t firstDiscardRectangle,
    uint32_t discardRectangleCount,
    const VkRect2D* pDiscardRectangles)
{
}

void goldfish_frontend_vkCmdSetDiscardRectangleEXT(
    VkCommandBuffer commandBuffer,
    uint32_t firstDiscardRectangle,
    uint32_t discardRectangleCount,
    const VkRect2D* pDiscardRectangles)

{
    // This is the body for vkCmdSetDiscardRectangleEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetDiscardRectangleEXT(&validateResult, commandBuffer, firstDiscardRectangle, discardRectangleCount, pDiscardRectangles);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetDiscardRectangleEXT(vkChannel, commandBuffer, firstDiscardRectangle, discardRectangleCount, pDiscardRectangles);
}

#endif

#ifdef VK_EXT_conservative_rasterization

#endif

#ifdef VK_EXT_swapchain_colorspace

#endif

#ifdef VK_EXT_hdr_metadata

static void validate_vkSetHdrMetadataEXT(
    VkResult* validateResult,
    VkDevice device,
    uint32_t swapchainCount,
    const VkSwapchainKHR* pSwapchains,
    const VkHdrMetadataEXT* pMetadata)
{
}

static void encode_vkSetHdrMetadataEXT(
    void* vkChannel,
    VkDevice device,
    uint32_t swapchainCount,
    const VkSwapchainKHR* pSwapchains,
    const VkHdrMetadataEXT* pMetadata)
{
}

void goldfish_frontend_vkSetHdrMetadataEXT(
    VkDevice device,
    uint32_t swapchainCount,
    const VkSwapchainKHR* pSwapchains,
    const VkHdrMetadataEXT* pMetadata)

{
    // This is the body for vkSetHdrMetadataEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkSetHdrMetadataEXT(&validateResult, device, swapchainCount, pSwapchains, pMetadata);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkSetHdrMetadataEXT(vkChannel, device, swapchainCount, pSwapchains, pMetadata);
}

#endif

#ifdef VK_MVK_ios_surface

static void validate_vkCreateIOSSurfaceMVK(
    VkResult* validateResult,
    VkInstance instance,
    const VkIOSSurfaceCreateInfoMVK* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

static VkResult encode_vkCreateIOSSurfaceMVK(
    void* vkChannel,
    VkInstance instance,
    const VkIOSSurfaceCreateInfoMVK* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateIOSSurfaceMVK(
    VkInstance instance,
    const VkIOSSurfaceCreateInfoMVK* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)

{
    // This is the body for vkCreateIOSSurfaceMVK
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateIOSSurfaceMVK(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateIOSSurfaceMVK(vkChannel, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

#endif

#ifdef VK_MVK_macos_surface

static void validate_vkCreateMacOSSurfaceMVK(
    VkResult* validateResult,
    VkInstance instance,
    const VkMacOSSurfaceCreateInfoMVK* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
}

static VkResult encode_vkCreateMacOSSurfaceMVK(
    void* vkChannel,
    VkInstance instance,
    const VkMacOSSurfaceCreateInfoMVK* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateMacOSSurfaceMVK(
    VkInstance instance,
    const VkMacOSSurfaceCreateInfoMVK* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkSurfaceKHR* pSurface)

{
    // This is the body for vkCreateMacOSSurfaceMVK
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateMacOSSurfaceMVK(&validateResult, instance, pCreateInfo, pAllocator, pSurface);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateMacOSSurfaceMVK(vkChannel, instance, pCreateInfo, pAllocator, pSurface);
    return res;
}

#endif

#ifdef VK_EXT_external_memory_dma_buf

#endif

#ifdef VK_EXT_queue_family_foreign

#endif

#ifdef VK_EXT_debug_utils

static void validate_vkSetDebugUtilsObjectNameEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkDebugUtilsObjectNameInfoEXT* pNameInfo)
{
}

static VkResult encode_vkSetDebugUtilsObjectNameEXT(
    void* vkChannel,
    VkDevice device,
    const VkDebugUtilsObjectNameInfoEXT* pNameInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkSetDebugUtilsObjectNameEXT(
    VkDevice device,
    const VkDebugUtilsObjectNameInfoEXT* pNameInfo)

{
    // This is the body for vkSetDebugUtilsObjectNameEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkSetDebugUtilsObjectNameEXT(&validateResult, device, pNameInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkSetDebugUtilsObjectNameEXT(vkChannel, device, pNameInfo);
    return res;
}

static void validate_vkSetDebugUtilsObjectTagEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkDebugUtilsObjectTagInfoEXT* pTagInfo)
{
}

static VkResult encode_vkSetDebugUtilsObjectTagEXT(
    void* vkChannel,
    VkDevice device,
    const VkDebugUtilsObjectTagInfoEXT* pTagInfo)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkSetDebugUtilsObjectTagEXT(
    VkDevice device,
    const VkDebugUtilsObjectTagInfoEXT* pTagInfo)

{
    // This is the body for vkSetDebugUtilsObjectTagEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkSetDebugUtilsObjectTagEXT(&validateResult, device, pTagInfo);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkSetDebugUtilsObjectTagEXT(vkChannel, device, pTagInfo);
    return res;
}

static void validate_vkQueueBeginDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkQueue queue,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

static void encode_vkQueueBeginDebugUtilsLabelEXT(
    void* vkChannel,
    VkQueue queue,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

void goldfish_frontend_vkQueueBeginDebugUtilsLabelEXT(
    VkQueue queue,
    const VkDebugUtilsLabelEXT* pLabelInfo)

{
    // This is the body for vkQueueBeginDebugUtilsLabelEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueBeginDebugUtilsLabelEXT(&validateResult, queue, pLabelInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkQueueBeginDebugUtilsLabelEXT(vkChannel, queue, pLabelInfo);
}

static void validate_vkQueueEndDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkQueue queue)
{
}

static void encode_vkQueueEndDebugUtilsLabelEXT(
    void* vkChannel,
    VkQueue queue)
{
}

void goldfish_frontend_vkQueueEndDebugUtilsLabelEXT(
    VkQueue queue)

{
    // This is the body for vkQueueEndDebugUtilsLabelEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueEndDebugUtilsLabelEXT(&validateResult, queue);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkQueueEndDebugUtilsLabelEXT(vkChannel, queue);
}

static void validate_vkQueueInsertDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkQueue queue,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

static void encode_vkQueueInsertDebugUtilsLabelEXT(
    void* vkChannel,
    VkQueue queue,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

void goldfish_frontend_vkQueueInsertDebugUtilsLabelEXT(
    VkQueue queue,
    const VkDebugUtilsLabelEXT* pLabelInfo)

{
    // This is the body for vkQueueInsertDebugUtilsLabelEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkQueueInsertDebugUtilsLabelEXT(&validateResult, queue, pLabelInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkQueueInsertDebugUtilsLabelEXT(vkChannel, queue, pLabelInfo);
}

static void validate_vkCmdBeginDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

static void encode_vkCmdBeginDebugUtilsLabelEXT(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

void goldfish_frontend_vkCmdBeginDebugUtilsLabelEXT(
    VkCommandBuffer commandBuffer,
    const VkDebugUtilsLabelEXT* pLabelInfo)

{
    // This is the body for vkCmdBeginDebugUtilsLabelEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdBeginDebugUtilsLabelEXT(&validateResult, commandBuffer, pLabelInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdBeginDebugUtilsLabelEXT(vkChannel, commandBuffer, pLabelInfo);
}

static void validate_vkCmdEndDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer)
{
}

static void encode_vkCmdEndDebugUtilsLabelEXT(
    void* vkChannel,
    VkCommandBuffer commandBuffer)
{
}

void goldfish_frontend_vkCmdEndDebugUtilsLabelEXT(
    VkCommandBuffer commandBuffer)

{
    // This is the body for vkCmdEndDebugUtilsLabelEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdEndDebugUtilsLabelEXT(&validateResult, commandBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdEndDebugUtilsLabelEXT(vkChannel, commandBuffer);
}

static void validate_vkCmdInsertDebugUtilsLabelEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

static void encode_vkCmdInsertDebugUtilsLabelEXT(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkDebugUtilsLabelEXT* pLabelInfo)
{
}

void goldfish_frontend_vkCmdInsertDebugUtilsLabelEXT(
    VkCommandBuffer commandBuffer,
    const VkDebugUtilsLabelEXT* pLabelInfo)

{
    // This is the body for vkCmdInsertDebugUtilsLabelEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdInsertDebugUtilsLabelEXT(&validateResult, commandBuffer, pLabelInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdInsertDebugUtilsLabelEXT(vkChannel, commandBuffer, pLabelInfo);
}

static void validate_vkCreateDebugUtilsMessengerEXT(
    VkResult* validateResult,
    VkInstance instance,
    const VkDebugUtilsMessengerCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDebugUtilsMessengerEXT* pMessenger)
{
}

static VkResult encode_vkCreateDebugUtilsMessengerEXT(
    void* vkChannel,
    VkInstance instance,
    const VkDebugUtilsMessengerCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDebugUtilsMessengerEXT* pMessenger)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateDebugUtilsMessengerEXT(
    VkInstance instance,
    const VkDebugUtilsMessengerCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkDebugUtilsMessengerEXT* pMessenger)

{
    // This is the body for vkCreateDebugUtilsMessengerEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateDebugUtilsMessengerEXT(&validateResult, instance, pCreateInfo, pAllocator, pMessenger);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateDebugUtilsMessengerEXT(vkChannel, instance, pCreateInfo, pAllocator, pMessenger);
    return res;
}

static void validate_vkDestroyDebugUtilsMessengerEXT(
    VkResult* validateResult,
    VkInstance instance,
    VkDebugUtilsMessengerEXT messenger,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyDebugUtilsMessengerEXT(
    void* vkChannel,
    VkInstance instance,
    VkDebugUtilsMessengerEXT messenger,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyDebugUtilsMessengerEXT(
    VkInstance instance,
    VkDebugUtilsMessengerEXT messenger,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyDebugUtilsMessengerEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyDebugUtilsMessengerEXT(&validateResult, instance, messenger, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyDebugUtilsMessengerEXT(vkChannel, instance, messenger, pAllocator);
}

static void validate_vkSubmitDebugUtilsMessageEXT(
    VkResult* validateResult,
    VkInstance instance,
    VkDebugUtilsMessageSeverityFlagBitsEXT messageSeverity,
    VkDebugUtilsMessageTypeFlagsEXT messageTypes,
    const VkDebugUtilsMessengerCallbackDataEXT* pCallbackData)
{
}

static void encode_vkSubmitDebugUtilsMessageEXT(
    void* vkChannel,
    VkInstance instance,
    VkDebugUtilsMessageSeverityFlagBitsEXT messageSeverity,
    VkDebugUtilsMessageTypeFlagsEXT messageTypes,
    const VkDebugUtilsMessengerCallbackDataEXT* pCallbackData)
{
}

void goldfish_frontend_vkSubmitDebugUtilsMessageEXT(
    VkInstance instance,
    VkDebugUtilsMessageSeverityFlagBitsEXT messageSeverity,
    VkDebugUtilsMessageTypeFlagsEXT messageTypes,
    const VkDebugUtilsMessengerCallbackDataEXT* pCallbackData)

{
    // This is the body for vkSubmitDebugUtilsMessageEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkSubmitDebugUtilsMessageEXT(&validateResult, instance, messageSeverity, messageTypes, pCallbackData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkSubmitDebugUtilsMessageEXT(vkChannel, instance, messageSeverity, messageTypes, pCallbackData);
}

#endif

#ifdef VK_ANDROID_external_memory_android_hardware_buffer

static void validate_vkGetAndroidHardwareBufferPropertiesANDROID(
    VkResult* validateResult,
    VkDevice device,
    const struct AHardwareBuffer* buffer,
    VkAndroidHardwareBufferPropertiesANDROID* pProperties)
{
}

static VkResult encode_vkGetAndroidHardwareBufferPropertiesANDROID(
    void* vkChannel,
    VkDevice device,
    const struct AHardwareBuffer* buffer,
    VkAndroidHardwareBufferPropertiesANDROID* pProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetAndroidHardwareBufferPropertiesANDROID(
    VkDevice device,
    const struct AHardwareBuffer* buffer,
    VkAndroidHardwareBufferPropertiesANDROID* pProperties)

{
    // This is the body for vkGetAndroidHardwareBufferPropertiesANDROID
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetAndroidHardwareBufferPropertiesANDROID(&validateResult, device, buffer, pProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetAndroidHardwareBufferPropertiesANDROID(vkChannel, device, buffer, pProperties);
    return res;
}

static void validate_vkGetMemoryAndroidHardwareBufferANDROID(
    VkResult* validateResult,
    VkDevice device,
    const VkMemoryGetAndroidHardwareBufferInfoANDROID* pInfo,
    struct AHardwareBuffer** pBuffer)
{
}

static VkResult encode_vkGetMemoryAndroidHardwareBufferANDROID(
    void* vkChannel,
    VkDevice device,
    const VkMemoryGetAndroidHardwareBufferInfoANDROID* pInfo,
    struct AHardwareBuffer** pBuffer)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetMemoryAndroidHardwareBufferANDROID(
    VkDevice device,
    const VkMemoryGetAndroidHardwareBufferInfoANDROID* pInfo,
    struct AHardwareBuffer** pBuffer)

{
    // This is the body for vkGetMemoryAndroidHardwareBufferANDROID
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryAndroidHardwareBufferANDROID(&validateResult, device, pInfo, pBuffer);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetMemoryAndroidHardwareBufferANDROID(vkChannel, device, pInfo, pBuffer);
    return res;
}

#endif

#ifdef VK_EXT_sampler_filter_minmax

#endif

#ifdef VK_AMD_gpu_shader_int16

#endif

#ifdef VK_AMD_mixed_attachment_samples

#endif

#ifdef VK_AMD_shader_fragment_mask

#endif

#ifdef VK_EXT_shader_stencil_export

#endif

#ifdef VK_EXT_sample_locations

static void validate_vkCmdSetSampleLocationsEXT(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const VkSampleLocationsInfoEXT* pSampleLocationsInfo)
{
}

static void encode_vkCmdSetSampleLocationsEXT(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const VkSampleLocationsInfoEXT* pSampleLocationsInfo)
{
}

void goldfish_frontend_vkCmdSetSampleLocationsEXT(
    VkCommandBuffer commandBuffer,
    const VkSampleLocationsInfoEXT* pSampleLocationsInfo)

{
    // This is the body for vkCmdSetSampleLocationsEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetSampleLocationsEXT(&validateResult, commandBuffer, pSampleLocationsInfo);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetSampleLocationsEXT(vkChannel, commandBuffer, pSampleLocationsInfo);
}

static void validate_vkGetPhysicalDeviceMultisamplePropertiesEXT(
    VkResult* validateResult,
    VkPhysicalDevice physicalDevice,
    VkSampleCountFlagBits samples,
    VkMultisamplePropertiesEXT* pMultisampleProperties)
{
}

static void encode_vkGetPhysicalDeviceMultisamplePropertiesEXT(
    void* vkChannel,
    VkPhysicalDevice physicalDevice,
    VkSampleCountFlagBits samples,
    VkMultisamplePropertiesEXT* pMultisampleProperties)
{
}

void goldfish_frontend_vkGetPhysicalDeviceMultisamplePropertiesEXT(
    VkPhysicalDevice physicalDevice,
    VkSampleCountFlagBits samples,
    VkMultisamplePropertiesEXT* pMultisampleProperties)

{
    // This is the body for vkGetPhysicalDeviceMultisamplePropertiesEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetPhysicalDeviceMultisamplePropertiesEXT(&validateResult, physicalDevice, samples, pMultisampleProperties);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetPhysicalDeviceMultisamplePropertiesEXT(vkChannel, physicalDevice, samples, pMultisampleProperties);
}

#endif

#ifdef VK_EXT_blend_operation_advanced

#endif

#ifdef VK_NV_fragment_coverage_to_color

#endif

#ifdef VK_NV_framebuffer_mixed_samples

#endif

#ifdef VK_NV_fill_rectangle

#endif

#ifdef VK_EXT_post_depth_coverage

#endif

#ifdef VK_EXT_validation_cache

static void validate_vkCreateValidationCacheEXT(
    VkResult* validateResult,
    VkDevice device,
    const VkValidationCacheCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkValidationCacheEXT* pValidationCache)
{
}

static VkResult encode_vkCreateValidationCacheEXT(
    void* vkChannel,
    VkDevice device,
    const VkValidationCacheCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkValidationCacheEXT* pValidationCache)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkCreateValidationCacheEXT(
    VkDevice device,
    const VkValidationCacheCreateInfoEXT* pCreateInfo,
    const VkAllocationCallbacks* pAllocator,
    VkValidationCacheEXT* pValidationCache)

{
    // This is the body for vkCreateValidationCacheEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkCreateValidationCacheEXT(&validateResult, device, pCreateInfo, pAllocator, pValidationCache);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkCreateValidationCacheEXT(vkChannel, device, pCreateInfo, pAllocator, pValidationCache);
    return res;
}

static void validate_vkDestroyValidationCacheEXT(
    VkResult* validateResult,
    VkDevice device,
    VkValidationCacheEXT validationCache,
    const VkAllocationCallbacks* pAllocator)
{
}

static void encode_vkDestroyValidationCacheEXT(
    void* vkChannel,
    VkDevice device,
    VkValidationCacheEXT validationCache,
    const VkAllocationCallbacks* pAllocator)
{
}

void goldfish_frontend_vkDestroyValidationCacheEXT(
    VkDevice device,
    VkValidationCacheEXT validationCache,
    const VkAllocationCallbacks* pAllocator)

{
    // This is the body for vkDestroyValidationCacheEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkDestroyValidationCacheEXT(&validateResult, device, validationCache, pAllocator);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkDestroyValidationCacheEXT(vkChannel, device, validationCache, pAllocator);
}

static void validate_vkMergeValidationCachesEXT(
    VkResult* validateResult,
    VkDevice device,
    VkValidationCacheEXT dstCache,
    uint32_t srcCacheCount,
    const VkValidationCacheEXT* pSrcCaches)
{
}

static VkResult encode_vkMergeValidationCachesEXT(
    void* vkChannel,
    VkDevice device,
    VkValidationCacheEXT dstCache,
    uint32_t srcCacheCount,
    const VkValidationCacheEXT* pSrcCaches)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkMergeValidationCachesEXT(
    VkDevice device,
    VkValidationCacheEXT dstCache,
    uint32_t srcCacheCount,
    const VkValidationCacheEXT* pSrcCaches)

{
    // This is the body for vkMergeValidationCachesEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkMergeValidationCachesEXT(&validateResult, device, dstCache, srcCacheCount, pSrcCaches);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkMergeValidationCachesEXT(vkChannel, device, dstCache, srcCacheCount, pSrcCaches);
    return res;
}

static void validate_vkGetValidationCacheDataEXT(
    VkResult* validateResult,
    VkDevice device,
    VkValidationCacheEXT validationCache,
    size_t* pDataSize,
    void* pData)
{
}

static VkResult encode_vkGetValidationCacheDataEXT(
    void* vkChannel,
    VkDevice device,
    VkValidationCacheEXT validationCache,
    size_t* pDataSize,
    void* pData)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetValidationCacheDataEXT(
    VkDevice device,
    VkValidationCacheEXT validationCache,
    size_t* pDataSize,
    void* pData)

{
    // This is the body for vkGetValidationCacheDataEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetValidationCacheDataEXT(&validateResult, device, validationCache, pDataSize, pData);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetValidationCacheDataEXT(vkChannel, device, validationCache, pDataSize, pData);
    return res;
}

#endif

#ifdef VK_EXT_descriptor_indexing

#endif

#ifdef VK_EXT_shader_viewport_index_layer

#endif

#ifdef VK_EXT_global_priority

#endif

#ifdef VK_EXT_external_memory_host

static void validate_vkGetMemoryHostPointerPropertiesEXT(
    VkResult* validateResult,
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    const void* pHostPointer,
    VkMemoryHostPointerPropertiesEXT* pMemoryHostPointerProperties)
{
}

static VkResult encode_vkGetMemoryHostPointerPropertiesEXT(
    void* vkChannel,
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    const void* pHostPointer,
    VkMemoryHostPointerPropertiesEXT* pMemoryHostPointerProperties)
{
    return (VkResult)0;
}

VkResult goldfish_frontend_vkGetMemoryHostPointerPropertiesEXT(
    VkDevice device,
    VkExternalMemoryHandleTypeFlagBits handleType,
    const void* pHostPointer,
    VkMemoryHostPointerPropertiesEXT* pMemoryHostPointerProperties)

{
    // This is the body for vkGetMemoryHostPointerPropertiesEXT
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetMemoryHostPointerPropertiesEXT(&validateResult, device, handleType, pHostPointer, pMemoryHostPointerProperties);
    if (validateResult != VK_SUCCESS)
    {
        return validateResult;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    VkResult res = encode_vkGetMemoryHostPointerPropertiesEXT(vkChannel, device, handleType, pHostPointer, pMemoryHostPointerProperties);
    return res;
}

#endif

#ifdef VK_AMD_buffer_marker

static void validate_vkCmdWriteBufferMarkerAMD(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlagBits pipelineStage,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    uint32_t marker)
{
}

static void encode_vkCmdWriteBufferMarkerAMD(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlagBits pipelineStage,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    uint32_t marker)
{
}

void goldfish_frontend_vkCmdWriteBufferMarkerAMD(
    VkCommandBuffer commandBuffer,
    VkPipelineStageFlagBits pipelineStage,
    VkBuffer dstBuffer,
    VkDeviceSize dstOffset,
    uint32_t marker)

{
    // This is the body for vkCmdWriteBufferMarkerAMD
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdWriteBufferMarkerAMD(&validateResult, commandBuffer, pipelineStage, dstBuffer, dstOffset, marker);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdWriteBufferMarkerAMD(vkChannel, commandBuffer, pipelineStage, dstBuffer, dstOffset, marker);
}

#endif

#ifdef VK_AMD_shader_core_properties

#endif

#ifdef VK_EXT_vertex_attribute_divisor

#endif

#ifdef VK_NV_shader_subgroup_partitioned

#endif

#ifdef VK_NV_device_diagnostic_checkpoints

static void validate_vkCmdSetCheckpointNV(
    VkResult* validateResult,
    VkCommandBuffer commandBuffer,
    const void* pCheckpointMarker)
{
}

static void encode_vkCmdSetCheckpointNV(
    void* vkChannel,
    VkCommandBuffer commandBuffer,
    const void* pCheckpointMarker)
{
}

void goldfish_frontend_vkCmdSetCheckpointNV(
    VkCommandBuffer commandBuffer,
    const void* pCheckpointMarker)

{
    // This is the body for vkCmdSetCheckpointNV
    VkResult validateResult = VK_SUCCESS;
    validate_vkCmdSetCheckpointNV(&validateResult, commandBuffer, pCheckpointMarker);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkCmdSetCheckpointNV(vkChannel, commandBuffer, pCheckpointMarker);
}

static void validate_vkGetQueueCheckpointDataNV(
    VkResult* validateResult,
    VkQueue queue,
    uint32_t* pCheckpointDataCount,
    VkCheckpointDataNV* pCheckpointData)
{
}

static void encode_vkGetQueueCheckpointDataNV(
    void* vkChannel,
    VkQueue queue,
    uint32_t* pCheckpointDataCount,
    VkCheckpointDataNV* pCheckpointData)
{
}

void goldfish_frontend_vkGetQueueCheckpointDataNV(
    VkQueue queue,
    uint32_t* pCheckpointDataCount,
    VkCheckpointDataNV* pCheckpointData)

{
    // This is the body for vkGetQueueCheckpointDataNV
    VkResult validateResult = VK_SUCCESS;
    validate_vkGetQueueCheckpointDataNV(&validateResult, queue, pCheckpointDataCount, pCheckpointData);
    if (validateResult != VK_SUCCESS)
    {
        return;
    }
    // VULKAN_CHANNEL_GET();
    void* vkChannel = 0;
    encode_vkGetQueueCheckpointDataNV(vkChannel, queue, pCheckpointDataCount, pCheckpointData);
}

#endif

