diff --git a/meson.build b/meson.build
index 7e7d818..55ceaaa 100644
--- a/meson.build
+++ b/meson.build
@@ -80,6 +80,10 @@ elif host_system == 'haiku'
   platform_deps += [
     cc.find_library('network')
   ]
+elif host_system == 'linux'
+  platform_deps += [
+    dependency('threads')
+  ]
 endif
 
 cargs = [
